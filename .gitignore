# Runtime data
data/
chat_uploads/
*.db
*.db-shm
*.db-wal
*.db-journal
*.sqlite
*.sqlite3

# Local configuration and secrets
config.yaml
config.local.yaml
config.yaml.backup
*.backup
.env
.env.*
!.env.example
*.pem
*.key
*.crt

# Upgrade / settings backups
.upgrade-backup/

# Eino runtime task board (under skills_dir)
skills/.eino/

# Build outputs
cyberstrike-ai
cyberstrike-ai.exe
*.test
bin/
dist/
build/
.build/
target/
.gradle/
coverage.out
coverage.html

# Logs and temporary files
*.log
*.bak
*~
tmp/
temp/

# Python
venv/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Go
vendor/

# Node / browser extension
node_modules/

# Desktop packaging (electron-builder)
desktop/node_modules/
desktop/dist/
desktop/build/
# CyberStrikeAI Windows desktop
runtime/

# macOS / Windows / editors
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# Local AI tooling configs (per-machine, not part of CyberStrikeAI source)
.agents/
.claude/
.cline/
.codebuddy/
.codegraph/
.continue/
.junie/
.kiro/
.ignore
.mcp.json
skills-lock.json

# Temporary Go build caches (created by agent sessions due to corrupted default GOCACHE)
.gocache-audit/
server.exe
mcp-stdio.exe
