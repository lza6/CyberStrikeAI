# CyberStrikeAI 对标分析与优化指南（结果计划指南）

> 本文档基于「主项目 CyberStrikeAI」与「参考项目根（342 个项目）」的自动扫描、识别、筛选、差距分析得出。
> 生成日期：2026-09-02。遵循 spec.md 契约与 workflow_status.md 台账。
> 级别标签：`已验证`（实际运行观察）/ `静态确认`（读代码确认）/ `合理推断`（有间接证据）/ `待验证`（缺环境/权限）。

---

## 摘要

CyberStrikeAI 是一个 **AI 原生网络安全智能中枢**（Go 1.25 + CloudWeGo Eino ADK + MCP + Gin + SQLite/CGO + 原生 JS 前端 + Electron 31 桌面壳），已在生产环境部署。本轮对标 342 个参考项目，提炼可迁移的设计思想（不照搬代码），并产出 P0/P1/P2 路线图与实施方案。

**核心结论**：
1. 主项目工程化已较成熟（245 测试文件、go vet clean、CGO sqlite、安全头/RBAC/限流/HITL 齐备），I 批次（参考项目设计迁移）与 J 批次大部分节点已 done。
2. 剩余差距集中在**前端巨型文件治理**（chat.js 11188 行、console.* 258 处、CSP 仍用 'unsafe-inline'）、**单轮入口缺失**、**blackboard 持久化缺口**、**CGO 验证基础设施缺失**。
3. 参考项目最高价值迁移：`agentmemory`（4 层记忆 + 三流检索 + provenance）、`agent-orchestrator-main`（8 插件槽 + reactions.yaml + 3288 测试）、`Pentest-Swarm-AI`（Go 同栈，已移植 shellsafe/scope）、`context-mode`（SQLite+FTS5 token 高效索引）。
4. 不建议迁移：整体推翻重写、引入与 Go 生态不匹配的复杂度、实验性方案。

---

## 一、项目识别结果

### 1.1 主项目识别

| 维度 | 结论 | 证据 |
|------|------|------|
| 项目类型 | AI 原生网络安全智能中枢（前后端分离 + 桌面壳 + 多 agent 编排 + 工具治理 + C2/WebShell） | `README_CN.md`、`spec.md` |
| 技术栈 | Go 1.25 + CloudWeGo Eino ADK + MCP(go-sdk) + Gin + SQLite(CGO/WAL) + Redis(可选) + 原生 JS + Electron 31 + Python 3.13.5(工具进程) | `go.mod`、`config.example.yaml` |
| 语言/框架 | Go（后端）+ 原生 JS（前端，ES module + 兼容回退）+ Electron（桌面） | `web/static/js/`、`desktop/src/` |
| 包管理 | Go modules（`go.mod`/`go.sum`）+ npm（`web/tests/e2e/package.json`）+ skills-lock.json | `go.mod`、`skills-lock.json` |
| 构建方式 | `go build -o cyberstrike-ai.exe cmd/server/main.go`（需 CGO_ENABLED=1 + mingw gcc）+ NSIS 打包 | `Makefile`、`Dockerfile`、`spec.md` |
| 运行入口 | `cmd/server/main.go`（`-config config.yaml --https/--http`） | `cmd/server/main.go` |
| 模块划分 | `internal/` 38 包：agent/multiagent/mcp/security/llm/knowledge/c2/blackboard/sarif/metrics/cache/capability/skillpackage/... | `ls internal/` |
| 核心业务流 | 自然语言意图 → Eino 多代理编排（deep/plan_execute/supervisor）→ MCP 工具执行（106 工具）→ HITL 审批 → 证据沉淀（SQLite + blackboard）→ 攻击链建模 | `internal/multiagent/runner.go`、`internal/security/executor.go` |
| API 结构 | Gin 路由集中在 `internal/app/app.go` setupRoutes（31 形参，~580 行），前缀 `/api/*`（auth/rbac/eino-agent/multi-agent/assets/batch/monitor/config/.../c2/webshell/...） | `internal/app/app.go:1011-1590` |
| 数据访问 | `mattn/go-sqlite3`（CGO）+ WAL；会话库 `data/conversations.db`、知识库 `data/knowledge.db` | `internal/database/`、`internal/app/app.go:124` |
| 配置管理 | `config.yaml`（运行态，不提交）+ `config.example.yaml`（模板）+ 环境变量（`CYBERSTRIKEAI_HOME`/`CYBERSTRIKE_NO_AUTO_OPEN`） | `internal/config/config.go`、`config.example.yaml` |
| 日志/异常 | `go.uber.org/zap`（JSON 编码）+ `internal/logger`；异常 `fmt.Errorf("%w", err)` 链式 | `internal/logger/logger.go` |
| 测试结构 | 245 个 `*_test.go`；非 CGO 包 25 个全过；CGO 包受限于无 mingw gcc | `find internal -name "*_test.go"` |
| 部署 | 原生二进制 + NSIS 安装包 + Dockerfile（多阶段） + Electron 桌面壳 | `Dockerfile`、`desktop/` |
| DB/缓存/MQ | SQLite（主）+ Redis（可选 cache.driver=redis，降级 memory）+ 无 MQ（进程内 blackboard pub/sub） | `internal/cache/cache.go`、`internal/blackboard/` |
| 前端/后端/脚本边界 | `web/static/js/`（前端）+ `internal/`+`cmd/`（后端）+ `scripts/`+`tools/`（YAML 工具配方）+ `desktop/`（Electron） | 目录结构 |

### 1.2 参考项目扫描结果

参考项目根 `C:\Users\Administrator.DESKTOP-EGNE9ND\Desktop\智能渗透\参考项目` 下共 **342 个项目**，分三组由子代理扫描：

- **AI 编排/记忆/知识库组**（子代理完整返回）：覆盖 OpenHands/SWE-agent/SuperAGI/crewAI/autogen/OpenSwarm/OpenMAIC/agency-agents/agent-orchestrator(两个不同项目)/agentchat/solo-orchestrator/all-agentic-architectures/open-multi-agent/claude-context/context-hub/context-mode/claude-token-efficient/token-optimizer(+mcp)/token-savior/mem0/claude-mem/claude-subconscious/LightRAG/txtai/agentmemory/context-engineering-intro/deepseek-harness/learn-harness-engineering/LongHorizon-Harness/OpenHarness。
- **安全渗透组**（子代理因 API 403 失败，但 spec.md 已记录迁移完成）：Pentest-Swarm-AI/strix/caveman/VulnClaw/bettercap/mcpstrike/AutoPentestX/pentest-harness/pentest-agents/Pentaract/Raccoon/Claude-BugHunter/CyberSecurity-Skills/Anthropic-Cybersecurity-Skills 等。
- **工程规范/桌面壳/CI 组**（子代理完整返回）：LongHorizon-Harness/OpenHarness-main/ECC/everything-claude-code-main/awesome-claude-code-main/gsd-2-main/superpowers-main/context-mode/claude-code-best-practice-main 等。

**重复副本识别**（去重）：`agency-agents`=main、`claude-context`=master、`context-mode`=main、`claude-mem`=main 均为同一上游的重复拷贝，保留非 `-main` 版本。`agent-orchestrator`（Untrivial-ai，Go）与 `agent-orchestrator-main`（ComposioHQ，TS）是**两个不同项目**，都保留。

### 1.3 候选参考项目清单（按总分排序）

| 排名 | 项目 | 技术栈 | 业务相似 | 架构参考 | 工程成熟 | 可迁移 | 总分 |
|------|------|--------|---------|---------|---------|--------|------|
| 1 | agentmemory-main | TS+MCP（41 工具，零外部 DB） | 5 | 5 | 5 | 5 | **20** |
| 2 | agent-orchestrator-main | TS+pnpm（3288 测试） | 4 | 5 | 5 | 5 | **19** |
| 3 | mem0 | Python | 4 | 5 | 5 | 5 | **19** |
| 4 | LightRAG | Python（多后端） | 4 | 5 | 5 | 5 | **19** |
| 5 | context-mode | TS+Bun+SQLite+FTS5 | 4 | 5 | 4 | 5 | **18** |
| 6 | OpenHands-main | Python SDK | 3 | 5 | 5 | 4 | **17** |
| 7 | open-multi-agent-main | TS（3 依赖，27 文件） | 4 | 5 | 3 | 5 | **17** |
| 8 | Pentest-Swarm-AI | **Go 同栈** | 5 | 4 | 4 | 4 | **17** |
| 9 | agent-orchestrator(Untrivial) | **Go+sqlc+SQLite** | 4 | 5 | 5 | 4 | **18** |
| 10 | OpenHarness-main | Python+React/Ink（61 测试） | 4 | 5 | 4 | 5 | **18** |

### 1.4 最终选中的高价值参考项目及原因

**Top 5**：

1. **agentmemory-main**（20/25）— 与主项目"记忆 + RAG KB"半边需求重叠度最高：跨 agent 持久记忆、BM25+vector+graph 三流检索、4 层记忆生命周期（working→episodic→semantic→procedural + 强度衰减）、provenance 引用、级联过期。零外部 DB，MCP server 形态，直接兼容主项目 SQLite 选择。
2. **agent-orchestrator-main**（19/25）— 8 个 PluginSlot（runtime/agent/workspace/tracker/scm/notifier/terminal + LifecycleManager）+ 反应式 `reactions.yaml`（ci-failed/changes-requested → auto send-to-agent + retries/escalate）+ 3288 测试。对主项目 J8/J9 拆分与"安全事件→自动动作"最有借鉴。
3. **Pentest-Swarm-AI**（17/25，Go 同栈）— 唯一与主项目同语言的安全 swarm。`internal/scope/`、`internal/agent/exploit/shellparse`、`executor_allowlist/safemode` 已被 spec.md I1 移植为 `shellsafe.go`/`scope.go`。继续可借鉴其 swarm 分工与 report/dedup。
4. **context-mode**（18/25）— SQLite+FTS5 作为 token 高效事件/会话索引 + "think in code" 范式（agent 跑查询只返回 verdict，不读数据进上下文）。直接兼容主项目 SQLite，解 J12 console.* 同源的 token 浪费。
5. **open-multi-agent-main**（17/25，27 文件/3 依赖）— 最干净的 Eino 可移植编排模式：coordinator 自动分解 goal → title 依赖 DAG → 并发 dispatch → synthesis + agent 间消息总线 + Zod 结构化输出（auto-retry）+ onTrace。

**Honorable mentions**：`agent-orchestrator`(Untrivial, Go+sqlc，唯一 Go+SQLite 编排守护，技术栈模板)、`mem0`/`LightRAG`（KB/记忆检索算法）、`OpenHarness`（whole-product 包边界：engine/memory/swarm/mcp/permissions）。

---

## 二、主项目现状总结

### 2.1 架构特点

- **分层清晰**：`cmd/`（入口）→ `internal/app/`（装配 + 路由）→ `internal/handler/`（HTTP）→ `internal/multiagent/`+`internal/agent/`（Eino 编排）→ `internal/security/`（executor + RBAC + shellsafe + scope）→ `internal/mcp/`（MCP 桥）→ `internal/database/`+`internal/blackboard/`+`internal/cache/`（存储）。
- **Eino ADK 多代理**：deep / plan_execute / supervisor 三模式，`runEinoADKAgentLoop` 是所有路径（Web SSE / 机器人 / 批量 / 桌面）汇聚的公共执行点。
- **工具治理五闸**：shellsafe（quote-aware 元字符拒绝）→ scope（CIDR/Domain/Port/Excluded）→ HIGH_IMPACT（破坏性工具第二道标记闸 + audit）→ HITL 审批（白名单免审批）→ TurnToolCallLimiter（单 turn 工具调用上限 + tool_call_ids 唯一）。
- **可选 Redis Cache-Aside**：`cache.driver: memory|redis`，未配置时零开销降级 memory + Warn。
- **Electron 外壳增强**：托盘 + 启动画面 + 原生对话框 + 单实例锁 + 关窗最小化 + local_mode 免登录。

### 2.2 当前优点

- 工程化基线扎实：`go vet ./...` clean、245 测试文件、Makefile/golangci/CI/Dockerfile 齐备。
- 安全防御纵深：SecureHeaders（CSP 骨架 + nosniff/DENY/Referrer/Permissions + HSTS）+ SetTrustedProxies(nil) + 全局限流 600/min + urlguard SSRF 过滤 + payload_builder GOOS/GOARCH 白名单 + payload_oneliner isSafeHostToken。
- 真实闭环：I 批次全节点 done（shellsafe 24 case、skills-lock 28 skill 0 违规、verbs-gate、cache -race、9 无线工具、Electron 外壳、ADR×6 + SOP）。
- 跨会话记忆：`storage.MigrateLegacyData`（幂等 + SQLite WAL sidecar 友好）+ `StorageConfig.home_dir`。
- 文档体系：README 中文化 + docs/zh-CN + docs/en-US 双语 + ADR×6 + SOP + ONBOARDING。

### 2.3 当前主要问题

| 问题 | 级别 | 证据 |
|------|------|------|
| **chat.js 巨型文件**（11188 行） | P2 | `web/static/js/chat.js`（J10 pending） |
| **app.go 巨型文件**（2424 行，New() 606 行，setupRoutes 31 形参 ~580 行） | P2（有意 skipped） | `internal/app/app.go`（J8） |
| **runner.go 巨型函数**（RunDeepAgent ~590 行） | P2（有意 skipped） | `internal/multiagent/runner.go:65-657`（J9） |
| **CSP 仍用 'unsafe-inline'**（2 模板 inline onclick，258 处 console.*） | P2 | `internal/security/secureheaders.go`（J11/J12 pending） |
| **无统一 RunModelTurn 入口** | P2 | `grep RunModelTurn` 无匹配（J16 pending） |
| **blackboard 纯内存**，进程重启丢失 | P2 | `internal/blackboard/memory_board.go`（J1 SQLite 缺口） |
| **$CYBERSTRIKEAI_HOME 闲置**：`storage.HomeDir()` 无人调用，仅 `cfg.Storage.HomeDir` 显式非空才迁移 | P2 | `internal/storage/migrate.go:23`、`app.go:132` |
| **CGO 验证基础设施缺失**：本机无 mingw gcc，全量 go test 13 包 FAIL（sqlite stub） | P1（环境） | `go test ./...` |
| **evals Tier3 未实现**（行为评测需真实 LLM 在环） | P3 | `internal/skillpackage/evals/evals.go`（标 TODO 不伪造） |

### 2.4 风险点

- **生产环境部署**：用户已在生产部署，一切改动须向后兼容、可回滚、不破坏现有 106 工具/74 skill/16 agent 加载。
- **CGO 依赖**：`mattn/go-sqlite3` 强依赖 mingw gcc；本机无 gcc 导致 CI/本地全量测试阻塞。
- **并发会话竞争**：本轮发现 `internal/security/scope_block.go`（16:51 新建，未跟踪）是并发会话写的 J4 execute 级 scope 扩展，瞬时半成品状态曾导致 vet 报错（后自愈）。
- **前端巨型文件拆分风险**：chat.js 全局符号 + 事件绑定顺序 + 依赖 monitor.js 的 handleStreamEvent，拆分需保 DOM 结构/i18n/事件不动。

### 2.5 技术债

- J8/J9 有意 skipped（拆分破坏路由顺序，保持不拆是合理决策，但文件仍超大）。
- J10-J12 前端治理 pending（P2 单独批次）。
- J16 单入口 pending（P2）。
- blackboard 持久化缺口（J1 只做内存版）。
- home 目录机制存在但未默认接入。

---

## 三、参考项目亮点提炼

### 3.1 按项目分别列出亮点

#### agentmemory-main（20/25，最高价值）
1. **4 层记忆生命周期 + 强度衰减**（working→episodic→semantic→procedural）— 高。迁移：CyberStrikeAI 记忆分层（session→target fact→general intel→procedure）+ 衰减规则。
2. **三流检索融合（BM25 + vector + 知识图谱）**— 高。迁移：SQLite FTS5 + 向量 + 实体图混合 recall。
3. **级联过期传播到图邻居**— 高。迁移：target fact 被 supersede 时，标记依赖 KB 节点 stale。
4. **每条记忆 provenance 引用**— 高。迁移：每条 exploit/intel 带来源（工具/session/CVE）。
5. **P2P mesh 同步**— 中。迁移：多 worker 间记忆同步。

#### agent-orchestrator-main（19/25）
1. **8 个 PluginSlot 全在 types.ts 定义，core lifecycle 固定**— 高。迁移：定义 6-8 个 CyberStrikeAI slot（Tool/Memory/KB/Agent/Notifier/UI）为 Go interface，Eino graph 保持非可插。
2. **反应式 `reactions.yaml`**（ci-failed/changes-requested → auto action + retries/escalate）— 高。迁移：`reactions.yaml` 把安全事件（scan fail/verdict request）→ 自动动作。
3. **3288 测试 + mutation/coverage/security/integration workflow**— 高。迁移：照搬质量门。
4. **Convention over configuration + 单一事实源（config 在 repo，runtime data 在 ~/.agent-orchestrator/）**— 高。迁移：统一 home 目录默认接入（解 J18 闲置）。

#### Pentest-Swarm-AI（17/25，Go 同栈）
1. **shellsafe quote-aware 元字符拒绝**— 高。已迁移（I1，24 case PASS）。
2. **scope validator（CIDR/Domain/Port/Excluded）**— 高。已迁移（I1 + J4 全链路接入）。
3. **executor_allowlist + safemode**— 高。已迁移（HIGH_IMPACT + HITL 白名单）。
4. **swarm 分工（recon/exploit/report 子代理 + 协调者）**— 中。迁移：sub_agents Markdown 已有，可补 report/dedup 子代理。
5. **bounty/dedup/roi 报告**— 中。迁移：攻击链导出 + 去重。

#### context-mode（18/25）
1. **SQLite + FTS5 作为 token 高效事件/会话索引**— 高。迁移：给会话事件加 FTS5 索引，compact 时 BM25 只取相关。
2. **"think in code" 范式**（agent 跑查询只返 verdict）— 高。迁移：暴露 `ctx_execute` 工具，agent 不读原始数据。
3. **sandbox 工具保 huge payload 不进 context**（315KB→5.4KB，98% 削减）— 高。迁移：扫描输出落 SQLite/artifacts，只给 agent 5KB 摘要。
4. **多客户端 + OpenClaw gateway**— 中。迁移：一个 context/state 服务喂多前端。

#### open-multi-agent-main（17/25）
1. **coordinator 自动分解 goal → title 依赖 DAG → 并发 dispatch → synthesis**— 高。迁移：Eino Graph 里 coordinator 分解安全目标，独立子任务并发。
2. **title 作为依赖 token 解析（无需手连图）**— 高。迁移：prompt 里按标题引用任务，解析为 ID。
3. **agent 间消息总线（点对点 + 广播）**— 高。迁移：Go pub/sub bus。
4. **结构化输出 Zod 解析/校验/auto-retry 一次**— 高。迁移：typed tool result + 单次 retry。

#### mem0（19/25）
1. **单遍 ADD-only 抽取（永不覆盖，事实累积）**— 高。迁移：append-only target facts，supersede 走版本不删。
2. **多信号融合检索（semantic + BM25 + entity）**— 高。
3. **时序推理（dated instance 排序 current/past/planned）**— 高。迁移：目标生命周期时序（已 patch vs 历史 CVE）。
4. **agent 确认的事实同等权重存储**— 中。

#### LightRAG（19/25）
1. **图存储抽象后端可换（Neo4j↔Postgres↔memory）**— 高。迁移：KB 图层可换，默认 SQLite/Postgres。
2. **双层检索：local（实体）+ global（主题）**— 高。迁移："此 CVE 细节" vs "目标跨趋势"。
3. **增量图更新**— 高。

#### OpenHands-main（17/25）
1. **microagent 作为可插拔上下文单元**— 高。迁移：RAG profile/target knowledge 作为 loadable bundle。
2. **EventStream pub/sub + Recall action 一等公民**— 高。迁移：Go typed event bus 喂 SQLite + agent。
3. **Prompt assembly as struct**（ConversationInstructions/RepositoryInfo/RuntimeInfo）— 高。

#### agent-orchestrator（Untrivial，Go+sqlc，18/25）
1. **Go + sqlc SQLite backend 跑编排守护**— 高。迁移：技术栈最接近，daemon 模型（event watcher → state → agent actions）。
2. **CDC + status-derivation 活看板**— 高。迁移：从持久变更流派生攻击状态板。
3. **worker 隔离（own worktree/branch/PR/CI）**— 高。迁移：每个 pentest task = 隔离 context + own session + own artifact scope。

#### OpenHarness-main（18/25）
1. **whole-product 特征图匹配**（tools+skills+memory+MCP+multi-agent+TUI+permissions+sandbox）— 高。迁移：照搬包边界（engine/memory/swarm/mcp/permissions/skill）。
2. **swarm 双后端（in-process + subprocess）+ worktree + mailbox + registry**— 高。
3. **cost_tracker + mcp client + permissions 独立关注**— 高。

### 3.2 共性优点

1. **单一事实源**：config 在 repo，runtime data 在统一 home（agent-orchestrator/context-mode/learn-harness）。
2. **事件驱动 + pub/sub**：EventStream（OpenHands）/ message bus（open-multi-agent）/ FTS5 索引（context-mode）。
3. **结构化输出 + auto-retry**：Zod（open-multi-agent）/ Pydantic（mem0）/ typed tool result。
4. **provenance/citation**：agentmemory/mem0/token-optimizer-mcp 都给每条记忆/claim 带来源。
5. **可插拔后端**：LightRAG 图存储 / agent-orchestrator-main PluginSlot。
6. **质量门**：3288 测试 + mutation + coverage + security workflow（agent-orchestrator-main）。

### 3.3 对主项目最有价值的亮点

| 排名 | 亮点 | 来源 | 迁移性 | 主项目落点 |
|------|------|------|--------|-----------|
| 1 | 4 层记忆 + 三流检索 + provenance | agentmemory | 高 | blackboard SQLite 持久化 + KB 检索升级 |
| 2 | 8 PluginSlot + reactions.yaml | agent-orchestrator-main | 高 | app.go 拆分 + 安全事件反应式 |
| 3 | SQLite+FTS5 token 高效索引 + think-in-code | context-mode | 高 | 会话压缩 + ctx_execute 工具 |
| 4 | coordinator→DAG→并发 dispatch | open-multi-agent | 高 | Eino supervisor 增强 |
| 5 | shellsafe/scope/allowlist（已迁移） | Pentest-Swarm-AI | 已完成 | I1+J4 |
| 6 | Convention over configuration + 统一 home | agent-orchestrator | 高 | J18 默认接入 |
| 7 | ADD-only + 时序推理 | mem0 | 高 | blackboard append-only |
| 8 | CDC + status-derivation 活看板 | agent-orchestrator(Untrivial) | 中 | 攻击状态板 |

---

## 四、差距分析

### 4.1 功能层

| 差距 | 主项目现状 | 参考项目实践 | 可迁移性 |
|------|-----------|-------------|---------|
| blackboard 持久化 | 纯内存（J1 缺口） | agentmemory 4 层 + SQLite | 高 |
| KB 三流检索 | 向量 + 精排 | agentmemory BM25+vector+graph | 高 |
| 时序推理 | 无 | mem0 dated instance 排序 | 中 |
| 反应式安全事件 | 无 | agent-orchestrator reactions.yaml | 高 |
| ctx_execute think-in-code | 无 | context-mode | 高 |
| 统一 home 默认 | 机制有但默认不接入 | agent-orchestrator convention | 高 |
| evals Tier3 行为 | 标 TODO | token-savior assert 工具被调用 | 中 |

### 4.2 架构层

| 差距 | 现状 | 参考 | 迁移性 |
|------|------|------|--------|
| 巨型文件 | app.go 2424 / runner.go 1142 / chat.js 11188 | agent-orchestrator-main 8 slot 分离 | 中（J8/J9 有意 skipped） |
| 单轮入口 | 无 RunModelTurn | context-mode executor.ts | 高（J16） |
| 可插拔后端 | Eino graph 固定 | agent-orchestrator-main PluginSlot | 中 |
| agent 间消息总线 | 无（直接 SSE） | open-multi-agent bus | 高 |
| CDC + 状态派生 | 无 | agent-orchestrator(Untrivial) | 中 |

### 4.3 工程层

| 差距 | 现状 | 参考 | 迁移性 |
|------|------|------|--------|
| 测试覆盖 | 245 文件，3 包无测试（已补） | agent-orchestrator-main 3288 测试 | 高（本轮已补 J15） |
| mutation 测试 | 无 | token-optimizer-mcp | 中 |
| 前端 E2E | 3 用例（smoke） | OpenHarness e2e suites | 高（J13） |
| evals harness | Tier1/2 | token-savior assert 工具调用 | 中（J14） |
| CGO 验证 | 无 mingw gcc | — | 高（环境阻塞） |

### 4.4 性能层

| 差距 | 现状 | 参考 | 迁移性 |
|------|------|------|--------|
| token 浪费 | 258 console.* + 原始 payload 进 context | context-mode 98% 削减 | 高（J12） |
| 会话压缩 | summarization middleware | context-mode FTS5 BM25 | 中 |
| token 仪表盘 | 无 | token-optimizer live dashboard | 中 |
| 缓存 | Cache-Aside memory/redis | — | 已完成 |

### 4.5 安全层

| 差距 | 现状 | 参考 | 迁移性 |
|------|------|------|--------|
| CSP nonce | 'unsafe-inline' | — | 高（J11） |
| console.* 泄露 | 258 处 | — | 高（J12） |
| provenance | 无 | agentmemory/mem0 | 高 |
| 级联过期 | 无 | agentmemory | 中 |

### 4.6 运维层

| 差距 | 现状 | 参考 | 迁移性 |
|------|------|------|--------|
| 统一 home | 机制有默认不接 | agent-orchestrator convention | 高（J18） |
| 反应式 runbook | 无 | agent-orchestrator reactions | 中 |
| 活看板 | 静态 | agent-orchestrator CDC | 中 |
| Dockerfile | done | — | 已完成 |

---

## 五、可迁移 / 半可迁移 / 不建议迁移

### 5.1 可迁移（高收益、低成本、兼容现有）

| 项 | 来源 | 预期收益 | 实施成本 | 风险 | 适配思路 |
|----|------|---------|---------|------|---------|
| blackboard SQLite 持久化 | agentmemory | 进程重启不丢 findings | 低（加 db.go） | 低 | MemoryBoard 加 SQLite 后端，Board interface 不变 |
| KB 三流检索 | agentmemory/mem0 | recall 质量提升 | 中 | 低 | 向量 + FTS5 + 实体图，Retriever 加 fused |
| reactions.yaml | agent-orchestrator-main | 安全事件自动响应 | 中 | 中 | 新 config 段 + 反应引擎 |
| ctx_execute | context-mode | token 大幅节省 | 中 | 低 | 新 MCP 工具，agent 跑查询返 verdict |
| 统一 home 默认接入 | agent-orchestrator | 部署一致性 | 低 | 低 | app.go 用 storage.HomeDir() 派生路径 |
| provenance 引用 | agentmemory | 审计可追溯 | 中 | 低 | Finding/漏洞加 source 字段 |
| ADD-only + supersede | mem0 | 历史可追溯 | 低 | 低 | blackboard 已有 Supersede，补版本链 |
| 前端 E2E 补全 | OpenHarness | 关键流回归保障 | 中 | 低 | J13 补对话/配置/审批链路 |

### 5.2 半可迁移（需适配或局部借鉴）

| 项 | 原因 | 预期收益 | 实施成本 | 风险 | 适配思路 |
|----|------|---------|---------|------|---------|
| 8 PluginSlot | Eino graph 非可插，全改破坏大 | 解耦 | 高 | 高 | 只抽 Tool/Memory/Notifier 三槽为 interface |
| coordinator DAG | Eino 已有 supervisor | 并发增强 | 中 | 中 | supervisor 模式加 title 依赖解析 |
| 消息总线 | 当前 SSE 直连 | 解耦 | 中 | 中 | 加 in-memory pub/sub，不引 MQ |
| CDC 活看板 | 无变更流 | 状态板 | 高 | 中 | 从 blackboard events 派生 |
| chat.js 拆分 | 全局符号 + 事件顺序 | 可维护 | 高 | 中 | 按 13 模块拆，保 DOM/i18n |
| CSP nonce | 258 inline onclick | 安全 | 中 | 低 | 渐进 nonce 化 |
| evals Tier3 | 需真实 LLM 在环 | 路由质量 | 高 | 中 | 用 glm-5.2 本地代理 |

### 5.3 不建议迁移

| 项 | 原因 |
|----|------|
| 整体推翻重写 | 主项目生产部署，工程化已成熟 |
| 引入 Neo4j/Postgres 替换 SQLite | 破坏单机部署 + CGO 兼容 |
| 引入 MQ（Kafka/RabbitMQ） | 单机桌面场景过度 |
| 全盘 TypeScript 重写前端 | 原生 JS + i18n 已稳，重写风险大 |
| 实验性方案（P2P mesh） | 单机足够，分布式非当前阶段 |
| Magentic-One 完整复刻 | Eino ADK 已有等价编排 |

---

## 六、优先级优化路线图

### P0（必须先做，影响稳定性/安全性/核心体验）

| 项 | 做什么 | 为什么 | 改哪些模块 | 预期收益 | 风险 | 数据迁移 | 兼容性 |
|----|--------|--------|-----------|---------|------|---------|--------|
| CGO 验证基础设施 | 下载 mingw-w64 gcc + 配 CGO_ENABLED=1 | 解锁全量 go test（13 包阻塞） | 无代码改动 | 验证闭环 | 无 | 无 | 无 |
| scope_block.go 编译闭环 | 修 3 处编译错误（net.ParseIP/looksLikeHostname/s==nil） | 当前并发会话半成品 | internal/security | build 绿 | 低（外部会话） | 无 | 无 |
| 已部署安全基线复验 | 起 exe + curl 实测安全头/限流/SSRF | 生产部署回归 | 无 | 信心 | 无 | 无 | 无 |

> **P0 说明**：主项目生产部署，安全/稳定已达标。P0 主要是**验证基础设施**而非代码改造。

### P1（高价值改造，明显提升质量和可维护性）

| 项 | 做什么 | 为什么 | 改哪些模块 | 预期收益 | 风险 | 数据迁移 | 兼容性 |
|----|--------|--------|-----------|---------|------|---------|--------|
| blackboard SQLite 持久化 | MemoryBoard 加 SQLite 后端 | 进程重启丢 findings | internal/blackboard | 数据不丢 | 低 | 自动（内存→db） | Board interface 不变 |
| 统一 home 默认接入 | app.go 用 storage.HomeDir() 派生路径 | 部署一致性 | internal/app、config | 路径统一 | 低 | 可选（用户启用才迁） | 向后兼容（默认仍 data/） |
| reactions.yaml | 安全事件→自动动作 | 运维自动化 | internal/新包 + config | 自动响应 | 中 | 无 | 新增配置段 |
| provenance 引用 | Finding/漏洞加 source 字段 | 审计可追溯 | internal/blackboard + audit | 可追溯 | 低 | schema 加列 | 向后兼容 |
| chat.js 拆分（J10） | 按 13 模块拆到 web/static/js/chat/ | 可维护 | web/static/js | 可维护 | 中（全局符号） | 无 | DOM/i18n/事件不动 |
| 前端 E2E 补全（J13） | 补对话/配置/审批链路用例 | 关键流回归 | web/tests/e2e | 回归保障 | 低 | 无 | 无 |

### P2（锦上添花，非当前阶段必须）

| 项 | 做什么 | 为什么 | 改哪些模块 | 预期收益 | 风险 | 数据迁移 | 兼容性 |
|----|--------|--------|-----------|---------|------|---------|--------|
| KB 三流检索 | 向量+FTS5+实体图融合 | recall 质量 | internal/knowledge | 提升 | 中 | 无 | 向后兼容 |
| ctx_execute 工具 | agent 跑查询返 verdict | token 节省 | internal/mcp/builtin | 省token | 低 | 无 | 新工具 |
| CSP nonce 化（J11） | 258 inline onclick 迁 nonce | 安全收紧 | web/templates + secureheaders | 安全 | 中 | 无 | 渐进 |
| console.* 清理（J12） | 258 处→可选 logger | token+整洁 | web/static/js | 整洁 | 低 | 无 | 无 |
| RunModelTurn 单入口（J16） | 抽统一 turn 入口 | 收敛 | internal/multiagent | 一致 | 中 | 无 | 向后兼容 |
| evals Tier3 | 行为评测 + assert 工具调用 | 路由质量 | internal/skillpackage/evals | 质量 | 中 | 无 | 无 |
| app.go 拆分（J8） | 抽 routes/webshell_tools/knowledge_init | 可维护 | internal/app | 可维护 | 高（路由顺序） | 无 | 保路由 |
| runner.go 拆分（J9） | 抽 orchestration/subagents/graph | 可维护 | internal/multiagent | 可维护 | 高 | 无 | 保行为 |

### P3（长期优化建议，只记录不占用交付时间）

- agent 间消息总线（open-multi-agent bus）
- CDC + 活看板（agent-orchestrator Untrivial）
- mutation 测试（token-optimizer-mcp）
- P2P mesh 同步（agentmemory）

---

## 七、实施方案

### 7.1 前端改造建议

1. **chat.js 拆分（J10）**：按 13 模块拆到 `web/static/js/chat/`（chat-core/input/mention/render/process-details/mcp/history/attack-chain/session-settings/sidebar-filter/context-menu/batch-manage/i18n-refresh）。**注意**：`handleStreamEvent`/`addProgressMessage`/`registerProgressTask`/`processSseDataLinesYielding` 在 monitor.js 不在 chat.js，拆分不要重复抽取。SSE 消费逻辑可单独抽 stream.js，与 monitor.js handleStreamEvent 保持契约。
2. **CSP nonce 化（J11）**：secureheaders.go 生成 per-request nonce，templates 的 2 处 inline onclick 迁 nonce，258 处 console.* 同源清理（J12）。
3. **console.* 清理（J12）**：258 处→可选 logger（前端可接 /api/log 落盘）或直接删除 debug 级。
4. **E2E 补全（J13）**：web/tests/e2e 已有 3 smoke，补对话 SSE/配置/审批拦截/HITL 链路。

### 7.2 后端改造建议

1. **blackboard SQLite 持久化**：`internal/blackboard/` 加 sqlite_board.go，Board interface 不变，NewMemoryBoard/NewSQLiteBoard 二选一（config 开关）。
2. **统一 home 默认接入**：app.go 的 dbPath/知识库 DB/data//tmp/ 一律从 `storage.HomeDir()` 派生，缺省回退 CWD 相对路径（兼容现有）。`data/conversation_artifacts`/`tmp/reduction`/`tmp/workspace`/`tmp/c2`/`plantask` 默认值集中到 `internal/paths` 或 `storage` 包。
3. **reactions.yaml**：新增 `internal/reactions/` 包，config 加 `reactions:` 段，引擎订阅 blackboard/audit 事件 → 匹配规则 → 触发动作（如 scan fail → 通知 + 重试上限）。
4. **RunModelTurn 单入口（J16）**：在 `runEinoADKAgentLoop` 之上抽 `RunModelTurn(ctx, req, mode)`，handler 层统一 prepareMultiAgentSession/SSE/结果持久化；multiagent 层把 `RunEinoSingleChatModelAgent` 与 `RunDeepAgent` 合并成 `RunAgentTurn(agentKind, ...)`。
5. **app.go 拆分（J8，P2 谨慎）**：抽 routes.go（setupRoutes ~580 行）/webshell_tools.go（~590 行）/knowledge_init.go（~280 行）/cors.go（~70 行）。setupRoutes 31 形参改 App 方法。**风险**：路由顺序，须保 31 形参语义不变。

### 7.3 数据层改造建议

1. **blackboard 持久化**：SQLite schema `findings(id, project_id, content, source, superseded_by, created_at)` + 索引。
2. **provenance 字段**：findings + vulnerabilities 加 `source(tool/session/cve)` + `verified_at`。
3. **KB 三流**：FTS5 索引 + 实体图（SQLite adjacency list）+ 现有向量。
4. **统一 home**：MigrateLegacyData 扩展源目录集合（tmp/prompts/playbooks/agents）。

### 7.4 接口层改造建议

1. **reactions.yaml** 新增 `/api/reactions` CRUD（RBAC: admin）。
2. **ctx_execute** 新 MCP 工具（builtin），参数 `{query, lang}`，返 verdict。
3. **统一 turn**：`/api/eino-agent/stream` 与 `/api/multi-agent/stream` 内部统一调 RunModelTurn。

### 7.5 配置管理建议

1. **reactions 段**：`reactions: [{when: "scan_fail", action: "notify+retry", max: 3}]`。
2. **blackboard.driver**：`memory|sqlite`（默认 memory 向后兼容）。
3. **storage.home_dir**：默认接入 `storage.HomeDir()`（`$CYBERSTRIKEAI_HOME` → `~/.cyberstrikeai`），仍可 config 覆盖。
4. **config.example.yaml 同步**（不动 config.yaml 运行态）。

### 7.6 日志与监控建议

1. **provenance**：每条 finding/漏洞带 source，审计可查。
2. **token 仪表盘**：借鉴 token-optimizer，前端显示 per-run token/$/turn。
3. **级联过期**：supersede 时标记依赖 KB 节点 stale + Warn。
4. **反应式 runbook**：reactions 触发记 audit。

### 7.7 测试策略建议

1. **CGO 基础设施**（P0）：下载 mingw-w64 gcc，配 CGO_ENABLED=1，解锁 13 包。
2. **新纯函数 100% 单测**（table-driven，匹配 internal/security 风格）。
3. **E2E**：curl 实测（限流 429/安全头/高危审批 403）+ Playwright（J13 补全）。
4. **evals**：Tier1/2 已有，Tier3 用 glm-5.2 本地代理 + assert 工具调用。
5. **回归底线**：不新增任何 FAIL（基线 FAIL 集合记录于 workflow_status.md）。

### 7.8 部署与运维建议

1. **Dockerfile** 已 done（多阶段），补 CGO_ENABLED=1 + mingw 在镜像内。
2. **NSIS** 打包含 local_mode + 无 TLS + 原生菜单（已 done）。
3. **统一 home**：安装包默认 `~/.cyberstrikeai`，升级脚本 `upgrade.sh` 备份迁移。
4. **CI**：`.github/workflows/ci.yml` 配 CGO_ENABLED=1 + mingw setup action。

---

## 八、修改前清单（真正改代码前输出）

### 计划修改的文件列表

| 文件 | 修改目的 | 改动范围 | 兼容性影响 | 验证方式 | 回滚方式 |
|------|---------|---------|-----------|---------|---------|
| internal/blackboard/sqlite_board.go（新） | J1 SQLite 持久化 | 新增 | Board interface 不变 | go test ./internal/blackboard/ | 删文件 |
| internal/app/app.go | 统一 home 默认接入 | L124-155 派生路径 | 向后兼容（默认 data/） | go vet + 起 exe + curl | git checkout |
| internal/storage/migrate.go | 扩展源目录集合 | MigrateLegacyData 源 | 向后兼容 | go test | git checkout |
| internal/reactions/（新包） | reactions.yaml 引擎 | 新增 | 新配置段 | go test + curl | 删包 |
| web/static/js/chat/（新目录） | J10 拆分 | 新增 13 文件 | DOM/i18n/事件不动 | node --check + Playwright | 删目录 + 恢复 chat.js |
| web/templates/*.html | J11 nonce | 2 处 onclick | CSP 收紧 | curl -I + Playwright | git checkout |
| web/static/js/*.js | J12 console.* | 258 处 | 无 | node --check | git checkout |

### 遵守规则

- 一次只做一批高相关改动
- 尽量小步提交
- 保持现有风格一致
- 不做无关重构
- 必须考虑异常处理、边界条件、日志、类型/注释、测试
- 不输出只有概念没有落地路径的建议

---

## 九、需补充的信息

1. **「结果计划指南.md」的确切角色**：本轮我按 spec.md 推进，本文档是事后整合。若你预期它是前置输入（如某参考项目里的指南），请给出路径，我再对照基准。
2. **CGO 验证基础设施决策**：本机无 mingw gcc，curl 走代理 TLS 失败，git 协议可达但 release asset 不可用。是否接受"非 CGO 包测试 + 真实 E2E 用现有 exe 起服务"作为本轮验证基线？还是必须下载 gcc（需你提供可达的下载源或手动放置 zip）？
3. **并发会话协调**：发现 `internal/security/scope_block.go`（16:51 新建，未跟踪）是并发会话写的 J4 扩展，瞬时半成品状态曾导致 vet 报错（后自愈）。是否需要我回避 internal/security 包的修改，避免与并发会话冲突？
4. **J8/J9 拆分决策**：台账标记"有意 skipped"（拆分破坏路由顺序）。是否本轮要推进，还是保持 skipped？
5. **reactions.yaml 优先级**：是否本轮纳入 P1，还是 P2？

---

## 附录 A：J 批次节点终态台账

| 节点 | 内容 | 状态 | 证据 |
|------|------|------|------|
| J1 blackboard | MemoryBoard+SQLite | done(内存版)/SQLite 缺口 | `internal/blackboard/memory_board.go` |
| J2 SARIF | attackchain + CWE 归一 | done | `internal/sarif/sarif.go` |
| J3 Prometheus | /metrics + otel | done | `internal/metrics/` |
| J4 scope 全链路 | executor 调用前过 scope | done（f678afb）+ 并发会话扩展中 | `internal/security/scope.go` + `scope_block.go` |
| J5 Capability Provider | plan/validate/rollback | done（f678afb） | `internal/capability/` |
| J6 HIGH_IMPACT 审批集 | 破坏性工具第二道闸 | done | `internal/security/highimpact.go` |
| J7 TurnLimiter + tool_call_ids | 防退化卡死 | done | `eino_turn_limiter*.go` 6 case PASS |
| J8 app.go 拆分 | 2424 行 | skipped（有意） | 路由顺序风险 |
| J9 RunDeepAgent 拆分 | 1142 行 | skipped（有意） | 同上 |
| J10 chat.js 拆分 | 11188 行 | pending | P2 单独批次 |
| J11 CSP nonce | 2 模板 inline | pending | P2 |
| J12 console.* 清理 | 258 处 | pending | P2 |
| J13 前端 Playwright E2E | 3 smoke 已有 | partial | web/tests/e2e |
| J14 三层 evals | Tier1/2 done | partial | Tier3 TODO |
| J15 测试补齐 | logger/authctx/projectprompt | **本轮 done** | 3 包新增测试全过 |
| J16 RunModelTurn 单入口 | 无 | pending | runEinoADKAgentLoop 是汇聚点 |
| J17 Dockerfile | 多阶段 | done | `Dockerfile` |
| J18 统一 home | 机制有默认不接 | done(机制)/缺口(默认) | `storage/migrate.go` |
| J-E2E | 6 项全链路 | done | workflow_status.md |
| J-push | 提交推送 + NSIS | done | cbc35dd + f678afb |

## 附录 B：验证证据

- `go vet ./...` exit 0（clean）— 已验证
- 25 个非 CGO 包 `go test` 全 ok — 已验证
- 3 个无测试包（logger/authctx/projectprompt）本轮新增测试全过 — 已验证
- 13 个 CGO 包 FAIL 全为环境性（sqlite stub + /bin/sh 缺失）— 已验证
- spec.md I/J 批次契约 — 静态确认
- workflow_status.md 恢复自 git 5bb244e — 已验证

---

*本指南遵循 spec-driven-development 工作流。所有建议基于实际目录和文件证据，不臆造。未验证项明确标注。*
