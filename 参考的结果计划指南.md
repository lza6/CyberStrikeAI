# CyberStrikeAI 对标分析与渐进改造路线图

> **文档状态**：Phase A（ANALYSIS_ONLY）完成产物。只读分析，未改源码/配置/依赖。
> **生成时间**：2026-09-01
> **主项目**：`C:\Users\Administrator.DESKTOP-EGNE9ND\Desktop\智能渗透\CyberStrikeAI`
> **参考项目根**：`C:\Users\Administrator.DESKTOP-EGNE9ND\Desktop\智能渗透\参考项目`（130+ 项目，三路 scout 调研 26 个高价值）
> **配套状态文件**：`workflow_status.md`（单一事实源，本指南是其 B1 交付物的完整版）

---

## 目录

1. [决策摘要](#1-决策摘要)
2. [项目识别结果](#2-项目识别结果)
3. [主项目现状总结](#3-主项目现状总结)
4. [参考项目亮点提炼](#4-参考项目亮点提炼)
5. [差距分析](#5-差距分析)
6. [可迁移 / 半可迁移 / 不建议迁移](#6-可迁移--半可迁移--不建议迁移)
7. [优先级优化路线图（P0/P1/P2）](#7-优先级优化路线图p0p1p2)
8. [全栈实施方案](#8-全栈实施方案)
9. [实施授权状态](#9-实施授权状态)
10. [需要我补充的信息](#10-需要我补充的信息)
11. [一页速查：你到底要参考什么](#11-一页速查你到底要参考什么)

---

## 1. 决策摘要

| 项 | 内容 |
|----|------|
| 复杂度 | 深度型（跨模块对标 + 26 参考项目 + 证据驱动差距分析 + 路线图） |
| 理由 | 主项目 Go+Eino+MCP+Gin+Electron 大型渗透平台（31 内部包、2254 行 app.go、11190 行 chat.js、223 测试无真 E2E、无 CI/无 Dockerfile），参考项目 130+ 个需递归筛选 |
| 当前授权 | ANALYSIS_ONLY —— 只读分析 + 复核既有台账，不改动源码/配置/依赖。Phase B 需用户确认路线图后启动 |
| 已知约束 | 不覆盖用户已有改动；不把参考项目脚本当执行授权；不动 config.yaml（local_mode: true 是运行态） |
| 关键未知项 | 用户最终想选哪些路线图批次实施（本报告给出建议优先级） |
| 最脆弱隐藏假设 | workflow_status.md 把若干 CRITICAL/HIGH 标为 done，但复核发现部分实际未真正修复——已在第 3 段诚实披露 |
| 工作流 | A1/A2/A3 复核 → 三路 scout 并行 → 整合 → 9 段报告 + P0/P1/P2 路线图 → 停在实施审批门 |
| 主要验证方式 | grep/Read 代码证据 + wc -l 行数 + 子代理只读调研 |
| 阻塞项 | 无 |

---

## 2. 项目识别结果

### 2.1 主项目识别结论

CyberStrikeAI 是一个 **AI 原生渗透测试平台**，Go 1.25 + Eino ADK 多代理 + MCP SDK + Gin + SQLite/CGO + 原生 JS 前端 + Electron 31 NSIS 桌面壳（内嵌 Python 3.13.5）。

| 维度 | 识别结论 | 证据 |
|------|---------|------|
| 架构形态 | 单体后端 + 原生 JS 前端 + Electron 桌面壳，前后端分离但同仓 | `go.mod:1` + `desktop/package.json` name=cyberstrike-ai-desktop |
| 语言/框架 | Go 1.25 + Eino 0.9.14/adk + MCP SDK 1.3.1 + Gin 1.9.1 + SQLite(mattn/go-sqlite3 CGO) | `go.mod:6-46` |
| 包管理 | Go modules | `go.mod`/`go.sum` |
| 构建/运行入口 | `cmd/server/main.go`（主）、`cmd/mcp-stdio/`、`cmd/test-config/`、`cmd/test-external-mcp/`、`cmd/test-sse-mcp-server/` | `cmd/` 目录 |
| 模块边界 | 31 个 internal 包 | `internal/`：agent/agents/app/attackchain/audit/authctx/c2/config/database/einomcp/einoobserve/handler/hitl/knowledge/llm/logger/mcp/monitor/multiagent/openai/project/projectprompt/reasoning/robot/security/skillpackage/termout/tooloutput/vision/workflow |
| 核心业务流程 | 规划→执行→人机监督→证据→回放可审计工作空间 | `README.md:18-19` |
| API/路由 | Gin，4 个路由组（auth/robot/protected/openapi） | `app.go:911-934` |
| 数据访问 | SQLite/CGO + WAL + schema_migrations 版本表 | `internal/database/` 30 文件 |
| 配置管理 | YAML + env，`config.example.yaml`(36KB) 含 17 个 AI channel + vision + 4 大情报 API | `config.example.yaml` |
| 日志/异常 | zap 结构化 + `internal/logger` | `go.mod:40 go.uber.org/zap` |
| 可观测性 | OpenTelemetry 已引入（trace + stdout），无 metrics/Prometheus | `internal/einoobserve/`(attach/otel) + `internal/monitor/`(reconcile/retention) |
| 测试 | 223 个 `*_test.go`，但真正含 E2E 字样仅 1 个；前端无任何测试目录 | `find *_test.go` |
| 部署 | 原生二进制（cystrike-ai.exe 150MB）+ NSIS 安装包，**无 Dockerfile/Makefile** | `ls Dockerfile*` 为空 |
| 资产 | agents 16 / skills 31 / tools 108 / roles 14 / playbooks 9 | `ls agents skills tools roles playbooks` |
| 当前未提交改动 | 仅 `workflow_status.md`（本任务状态文件） | `git status -s` |

### 2.2 参考项目扫描结果（26 个高价值，三路 scout 调研）

按主题分三组，每项标注 业务相似度/架构参考/可迁移性（0-5）：

#### 多代理编排组（8 个）

| 项目 | 栈 | 业务 | 架构 | 迁移 | 一句话定位 |
|------|----|------|------|------|-----------|
| Pentest-Swarm-AI | Go1.24+PG+Redis | 5 | 5 | 4 | stigmergic blackboard + pheromone decay 真正 swarm |
| autopentest-go | Go1.26+Wails | 5 | 5 | 5 | 单 tool-using LLM runtime，桌面+CLI 共享引擎，**栈最接近** |
| VulnClaw | Python+MCP | 5 | 5 | 3 | 模型主导 solve 引擎，证据级反幻觉闸门 |
| strix | Python+openai-agents | 4 | 4 | 3 | AgentCoordinator 地址化 agent 图 + 预算 hooks |
| AutoPentestX | Python 单体 | 3 | 1 | 1 | 传统线性渗透流水线，无 AI |
| mcpstrike | Python+FastMCP | 3 | 3 | 2 | 三层 client→MCP→backend + 安全栈 |
| arsenal-ng | Go+Bubbletea | 2 | 2 | 3 | 渗透命令速查启动器，embed.FS + 占位符 |
| bettercap | Go1.25 | 1 | 2 | 1 | 纯网络/MITM 框架，Module interface 范本 |

#### skill/工程框架组（9 个）

| 项目 | 栈 | 业务 | 架构 | 迁移 | 一句话定位 |
|------|----|------|------|------|-----------|
| ECC | TS+Py+Rust | 2 | 5 | 4 | Claude Code 插件 OS，manifest/profile 分层安装 |
| superpowers | Markdown+bash | 1 | 5 | 4 | TDD for skills + SDO 发现优化 + Match the Form |
| caveman/2 | Go+TS | 1 | 5 | 5 | skills-lock.json + verbs-gate 漂移门 + prompt_byte_budget |
| SkillClaw | Python+FastAPI | 1 | 4 | 3 | skill 自动进化，SkillIDRegistry 版本/哈希/历史 |
| agent-skills | Markdown+node | 1 | 5 | 4 | 三层 evals（结构/触发路由/行为）+ references 共享 |
| CyberSecurity-Skills | Markdown+Python CLI | 5 | 4 | 4 | 39 模块 195 skill，PTES/OWASP/MITRE 标准 + index.json |
| SecSkills | README 目录 | 5 | 1 | 1 | 54 外部 skill 链接目录，无框架 |
| book-to-skill | Python | 1 | 4 | 4 | 文档转 skill，注入扫描 + 多 lens 校验 + 信任边界 |
| nuwa-skill | Markdown+Python | 2 | 4 | 3 | 人物蒸馏，fidelity-scorecard 五维 + 双 agent 评分 |

#### 逆向/浏览器/MCP 工具链组（9 个）

| 项目 | 栈 | 业务 | 架构 | 迁移 | 一句话定位 |
|------|----|------|------|------|-----------|
| drission-rs | Rust1.85+rmcp | 5 | 5 | 5 | **与主项目 Go 栈最对齐**，Agent Runtime/BrowserPool/Session/IdentityGate |
| chrome-cdp-skill | Node22 | 5 | 5 | 5 | 连 live Chrome，daemon-per-tab，工具粒度干净 |
| reverse-skill | Markdown+PS/sh | 5 | 5 | 4 | 路由器+作战契约，Evidence→Finding→Path 三段式 |
| PE-reverse-skill | Go+Python | 4 | 5 | 4 | Capability Provider 生命周期 Protocol + Evidence Manifest |
| js-reverse-skill | Markdown+Node | 3 | 5 | 3 | 状态机+GATE 硬门禁+三档确认 A/B/C |
| stealth-browser-mcp | Python+nodriver | 4 | 5 | 4 | 97 工具 11 分区，分区化 + idle reaper + bearer auth |
| browser-use | Python async | 3 | 4 | 3 | 14 watchdog 事件总线 + Registry @action |
| x64dbg-mcp-server | Zig | 2 | 4 | 3 | 状态优先工作流 + 双 transport(HTTP+SSE) + 84 工具 |
| android-reverse-eng-skill | Bash+PS+jadx | 3 | 3 | 2 | Phase 0 指纹分诊 + Kotlin metadata 恢复 |

### 2.3 排除目录及原因

- `.git_clone_logs/`、`CloudFlare说明.txt`：克隆日志/说明文本，非项目。
- 全部项目的 `node_modules/`/`vendor/`/`venv/`/`dist/`/`build/`/`target/`：构建产物，跳过。
- `bettercap` 的 `js/` 插件、`x64dbg` 的 `.dp32/.dp64` 插件：特定宿主插件，不作为独立项目。
- 大量"纯链接目录"（SecSkills 等）或"通用工程方法论"（superpowers/ECC 业务相似度低）：降级为局部参考，不进入高价值清单。

### 2.4 候选项目评分汇总

最终选中的 **高价值参考项目**（综合三维评分 ≥ 13 或迁移性=5）：

1. **autopentest-go**（5/5/5）——栈最接近，agentloop 单入口 + memory 三投影 + hooks 16 事件
2. **drission-rs**（5/5/5）——Agent Runtime + BrowserPool + Session 模式 + env 门控
3. **chrome-cdp-skill**（5/5/5）——连 live Chrome + daemon-per-tab + 工具粒度
4. **Pentest-Swarm-AI**（5/5/4）——stigmergic blackboard + pheromone + shellsafe + scope
5. **VulnClaw**（5/5/3）——证据级反幻觉 + 确定性 digest + skill 确定性路由
6. **caveman/2**（1/5/5）——skills-lock + verbs-gate + prompt_byte_budget
7. **reverse-skill**（5/5/4）——路由器契约 + Evidence→Finding→Path
8. **PE-reverse-skill**（4/5/4）——Capability Provider 生命周期 Protocol
9. **stealth-browser-mcp**（4/5/4）——分区化工具面 + idle reaper + bearer auth
10. **agent-skills**（1/5/4）——三层 evals + references 共享目录
11. **CyberSecurity-Skills**（5/4/4）——PTES/OWASP/MITRE 标准声明 + index.json
12. **strix**（4/4/3）——TurnToolCallLimiter + tool_call_ids + SARIF

**选择原因**：这些项目要么业务相似度=5（同为渗透/AI 安全），要么架构参考价值=5（工程设计教科书级），要么可迁移性=5（Go 同栈或设计可直接复刻）。其余项目仅作局部参考。

**判断置信度**：高。三维评分均有 file:line 证据支撑（scout 报告已附关键路径索引），主项目落地点已逐一 grep 确认存在。

---

## 3. 主项目现状总结

### 3.1 当前架构特点

- **AI 原生**：Eino ADK 四模式编排（单代理/Deep/Plan-Execute/Supervisor），`internal/multiagent/` 103 源文件 + 90 测试，已有 90+ `eino_*` 中间件（checkpoint/resume、tool_call sanitization、context overflow retry、summarize、RBAC、HITL、plan_execute）。
- **MCP 原生**：`modelcontextprotocol/go-sdk` 1.3.1，HTTP/stdio/SSE/外部联邦 + 动态工具发现，`internal/mcp/`(server.go 964 行 + execution_service + external_manager)。
- **资产自动加载**：`LoadMarkdownAgentsDir`（agents）+ `skill.NewBackendFromFilesystem`（skills 渐进披露池）+ `LoadToolsFromDir`（tools→MCP 池）+ `LoadRolesFromDir`（roles）+ 4 编排模式对话页可选。
- **桌面单机优先**：Electron 31 + NSIS，`local_mode` 免登录直进（G1-G3 已落地：内置 admin 全权限，双击 exe→BrowserWindow 加载 127.0.0.1:8080）。
- **原生 JS 前端**：无框架，53 个 JS 文件，`chat.js` 11190 行（DOMPurify+marked，XSS 防护链完整）。

### 3.2 当前优点

- ✅ AI 通道端到端打通：glm-5.2 经本地代理，`/api/eino-agent/stream` 收到 reasoning_chain_stream_delta + assistant 响应（C3 验证日志）。
- ✅ `apiFetch` 401 统一处理 + 前端 AI 通道校验 + 后端双步 + `escapeHtml` 工具 + vendor 本地化无 CDN。
- ✅ `local_mode` 免登录闭环已验证（/api/auth/validate 不带 token → 200，/api/config /api/skills /api/conversations 全 200）。
- ✅ F1 修复批次扎实：SetTrustedProxies(nil)、MCP MaxBytesReader(10MB)、payload_builder GOOS/GOARCH 白名单 + binName filepath.Base、payload_oneliner isSafeHostToken、vulnclaw-core 7 个 .md→子目录/SKILL.md、idor-bola-tester 去误报、ai-config.js id 清洗 + isKeyUnconfigured 扩检测、main.js stdout 落盘 + 错误透传、i18n rbac.resourceTypes.asset 补齐。
- ✅ otel trace 已引入，`internal/einoobserve/attach.go` + `internal/monitor/reconcile.go`。
- ✅ RBAC 中间件 + deny hook（`app.go:934 RBACMiddlewareWithDenyHook`）。

### 3.3 当前主要问题（已确认，带代码证据）

> ⚠️ **重要**：复核 workflow_status 既有"done"标记时，发现台账与真实代码不一致。以下"真实状态"列以代码证据为准。

| 级别 | 问题 | 证据 | 台账状态 | 真实状态 |
|------|------|------|---------|---------|
| CRITICAL | app.go 2254 行 / New() 517 行 超大文件 | `wc -l internal/app/app.go` | C1 标注但未修 | ❌ open |
| CRITICAL | WebShell SSRF 无私有 IP 过滤 | 全仓无 `isPrivateIP`/`net.IsLoopback` | C2 标 done | ❌ **仍 open** |
| CRITICAL | 405 处 handler 层 `err.Error()` 裸露 | `grep -rln 'c.JSON.*err.Error()' internal/handler/` | C3 未列必修 | ❌ 仍 open |
| CRITICAL | URL token + 无 CSRF | `auth.go` validate 路由 | C4 未修 | ❌ open |
| CRITICAL | **无安全响应头中间件**（CSP/X-Frame-Options/Strict-Transport/Referrer-Policy/Permissions-Policy 全缺失） | `grep Content-Security-Policy internal/` 空 | H1 未修 | ❌ open |
| HIGH | 66 处 `go func()`，部分无 ctx/无退出 | `grep "go func()" internal/` | H6 未修 | ❌ open |
| HIGH | `sessions map[string]Session` 无 LRU/上限 | `auth_manager.go:38,137,202` | H7 未修 | ❌ open |
| HIGH | RunDeepAgent 582 行 | `runner.go:65-647` | H8 未修 | ❌ open |
| HIGH | 仅 login+robot 限流，其余端点无限流 | `app.go:909-924` | H10 未修 | ❌ open |
| HIGH | MCP 全局访问全权限（toolAuthorizer 单点） | `server.go:55,515,916` | H11 未修 | ❌ open |
| HIGH | 知识库索引逻辑重复 | `internal/knowledge/` | H12 未修 | ❌ open |
| MEDIUM | chat.js 11190 行 / renderAttackChain 515 行 | `wc -l chat.js` | M1/M2 未修 | ❌ open |
| MEDIUM | 265 处 console.* | `grep console web/static/js/` | M3 未修 | ❌ open |
| 风险 | `local_mode: true` 运行态开着 + 无公网误开防护 | `config.yaml:36` | G1 引入新风险 | ⚠️ 需加启动断言 |

### 3.4 风险点 / 技术债

- **无 CI/CD**（`.github/workflows` 不存在）、**无 Dockerfile/Makefile**、**无静态检查配置**（golangci/gosec/staticcheck 均无）——工程成熟度缺口，回归全靠人工。
- **测试覆盖空洞**：`audit`(9 源/0 测试)、`robot`(13 源/0 测试)、`skillpackage`(7 源/0 测试) 三个关键包零测试；前端无任何测试目录。
- **223 个 `*_test.go` 但真正 E2E 性质仅 1 个**——渗透平台缺少真实用户路径验证（登录→配置→对话→工具调用→结果回显）。
- **observability 仅 trace/stdout**，无 metrics/Prometheus 导出，生产可观测性不足。

### 3.5 已确认事实 vs 推断的区分

- **已确认（DIRECT 证据）**：上述所有"问题"均有 grep/wc 命令输出支撑。
- **推断（INFERENCE）**：`local_mode` 若误暴露公网风险（需网络绑定校验确认是否真有防护，待 Phase B 验证）。
- **待验证（UNVERIFIED）**：WebShell handler 是否在请求处理路径里真的无 IP 过滤（只确认了"全仓无过滤函数"，未逐行读 webshell.go 全部 handler——但 absence of filter function 是强证据）。

---

## 4. 参考项目亮点提炼

### 4.1 按项目分别列出亮点（精选高价值项目）

#### autopentest-go（栈最接近，可迁移性 5）
- `RunModelTurn` + `TurnSink` 单入口：CLI 和桌面共用同一 turn 引擎，消除重复（`internal/agentloop/agentloop.go:1-100`）。
- `ToolExecutionResult` 三投影：Display（UI）/Evidence（内存）/API，让 UI 流、证据流、API 投影流解耦（`internal/services/tool_execution.go:50-120`）。
- `ToolResultImage` 自动从 base64 data-URI 抽图（`tool_execution.go`）。
- MCP multi-transport loader：`McpConfigLoader.ParseConfig` 把 interface 解码成具体 transport（`internal/services/mcp/manager.go:15-45`）。
- memory schema + PRAGMA WAL + schema_migrations 版本表（`internal/memory/schema.go`）。
- hooks 16 事件 + Registry（`internal/hooks/hooks.go:20-50`），与 Claude Code hooks 协议同构。
- 统一 home `~/.autopentest-go/` + 旧路径迁移（`internal/storage/migrate.go`）。

#### drission-rs（与主项目 Go 栈最对齐，可迁移性 5）
- Agent Runtime 语义动作：`observe()` 输出 accessibility outline（role+name+ref）而非 HTML，`SemanticTarget` 隐藏 CSS 让 LLM 只做语义决策（`src/agent/mod.rs:1-80`）。
- BrowserPool 高并发：信号量限流 + 每任务代理/指纹轮换 + RetryPolicy + 健康自愈 + Checkpoint 断点续抓（`src/pool/mod.rs:1-10`）。
- Session 模式 + 浏览器→HTTP cookie 交接：过盾/登录后 cookie 灌给 Session，之后翻页/详情全走 HTTP 降负载（`src/session/mod.rs:1-22`）。
- Context + 磁盘 Profile：按名隔离 Cookie/代理/UA（`README.md:55-65`）。
- MCP 工具集 env 门控：`DRS_MCP_TOOLS=core|browser|all`（`crates/drission-cli/src/mcp.rs:25-40`）。
- IdentityGate 指纹漂移/关联性检测：多账号渗透防关联（`identity_cmd.rs:1-40`）。

#### Pentest-Swarm-AI（业务最相似，可迁移性 4）
- stigmergic blackboard：`Board` 接口 + `MemoryBoard` 实现，`Subscribe` 返回 `<-chan Finding` at-least-once + cursor exactly-once（`internal/swarm/blackboard/board.go:9-50`）。
- Agent 契约 `Trigger()/Handle()/MaxConcurrency()`：每个 agent 自带 trigger predicate，scheduler 只做并发/预算/关停不做规划（`internal/swarm/agent.go:18-35`）。
- pheromone decay + exploration-bias：per-finding-type `base` + `half_life_sec` + `--exploration-bias` 全局乘子（`pheromones_default.yaml` + `tuning.go`）。
- `shellsafe.Parse`：quote-aware 拒绝未引用 shell 元字符（`internal/shellsafe/shellsafe.go:23-40`）。
- scope validator：CIDR/Domain/Port/Excluded 四元（`internal/scope/validator.go:18-50`）。
- confirmation agent 重放验证：期望 indicator 不出现则写 superseding finding（`agents/confirmation.go:18-35`）。

#### VulnClaw（已移植过 skill，可迁移性 3 但设计思想高价值）
- AgentState 证据模型：raw evidence 完整落盘 + active context 只注入 bounded high-signal preview（表单/参数/JS endpoint/sink/flag-like token），`MAX_STORED_EVIDENCE=240`（`vulnclaw/agent/agent_state.py:10-40`）。
- `correction_layer` 正则信号抽取：`_URL_RE`/`_FORM_RE`/`_JS_ENDPOINT_RE` 注入下一轮（`agent/correction_layer.py:1-30`）。
- 确定性 context_budget digest：**不调 LLM 摘要**，用结构化 AgentState 渲染 digest（`agent/context_budget.py:1-25`）。
- 证据级反幻觉闸门：声称的 flag/结论必须逐字符出现在真实工具输出（`agent/solver.py`）。
- skill resolver 确定性路由：typed-weight 3.0 + alias 1.0 + DIRECTORY_BOOST 1.5，无 LLM（`vulnclaw/skills/resolver.py:1-40`）。
- `_NO_PATH_MARKERS` + near-miss guard：高信号锚点未耗尽时不接受模型"判死"。

#### caveman/2（可迁移性 5，frontmatter/锁文件/漂移门）
- `skills-lock.json`：`source/sourceType/skillPath/computedHash` 供应链锁定（`caveman/skills-lock.json`）。
- `prompt_byte_budget` + `entry_condition`/`stop_condition`/`guardrails` frontmatter 字段（`skills/registry.json`）。
- `verbs-gate.mjs` 漂移门：从源派生真实 tool surface，扫描 skill 引用，fail-closed（`skills/verbs-gate.mjs`）。
- `native-core.md` 560 token 全局"宪法"层。

#### reverse-skill（业务 5 架构 5，路由器契约）
- 路由执行契约：① 平台 router 定 PRIMARY ② `case-init`→`scope.md`（auth 未 granted 禁止 ACT）③ 打开 PRIMARY SKILL.md ④ 工具路径只认 `tool-index.md` ⑤ 结论走 Evidence→Finding→Path（`skills/SKILL.md:11-21`）。
- ops 作战契约文档族：scope-contract/evidence-finding-path/role-map/timeline-workitem/sandbox-profile（`MASTER-ROUTING.md:59-73`）。
- 路由优先级表 + `verify-routing-coherence.ps1` 校验（`MASTER-ROUTING.md:77-90`）。
- Next-Step Menu Pattern：只在 genuine decision boundary 才提供 3-6 选项（`SKILL.md:100-120`）。
- skill-supply-chain 门闩：外部 skill/MCP 安装门禁（`SKILL.md:59`）。

#### PE-reverse-skill（Capability Provider 生命周期）
- `supports→plan→validate→execute→rollback→collect_artifacts` 生命周期 Protocol（`providers/base.py:70-106`）。
- 状态三态：unavailable/partial/failed，failed 不伪装成 graceful unavailable（`README.md:201-207`）。
- Evidence Manifest：相对工件路径 + SHA-256 + provenance，检测缺失/篡改/越界（`README.md:236-238`）。
- `reverse-instruct --dry-run`/`--allowed`/atomic write/backup 授权门禁。

#### stealth-browser-mcp（分区化工具面）
- 分区化 + 模块化加载：`section_tool(section)` 装饰器 + `apply_disabled_sections()` 运行时 `mcp.remove_tool`，Full(97)/Minimal(20)/Custom/Xpool-safe(83)（`src/server.py:82-96`）。
- BrowserManager idle reaper 三参数：超时/巡检间隔/孤儿清理（`README.md:298-300`）。
- HTTP bearer auth + 文件上传白名单（`README.md:296-301`）。

#### agent-skills（三层 evals）
- Tier 1 结构（frontmatter/命名/必需段，CI 免费 `validate-skills.js`）/ Tier 2 触发与路由（正例 top-k 排名、负例不排名、两 description 不碰撞，TF-IDF）/ Tier 3 行为（`evals/README.md`）。
- `trigger.positive/negative` eval case 格式（`evals/cases/*.json`）。
- `references/` 共享清单目录（跨 skill 共享 OWASP/PTES 检查清单）。

#### CyberSecurity-Skills（标准声明 + index.json）
- `index.json` 的 `meta.standards[]` 显式声明 PTES/OWASP Testing Guide v4.2/NIST SP 800-115/MITRE ATT&CK v15/等保 2.0（`index.json`）。
- `skill_query.py` CLI：`search --keyword`/`validate`（索引-文件一致性校验）。
- `id: 模块ID-技能序号` 编号方案 + `difficulty` ★评级 + `tools` 推荐工具字段（`AGENT_USAGE.md`）。

#### strix（防卡死机制）
- `TurnToolCallLimiter`：限制单次响应工具调用数，防退化生成上百次 poll/wait（`config/tool_call_limits.py:8-25`）。
- `tool_call_ids`：保持对话内 tool-call id 唯一（部分 provider 每轮重置计数器，重复 id 会导致 strict provider 拒绝整轮）（`config/tool_call_ids.py:8-25`）。
- 预算 warn bands `(0.70,0.85,0.95)` + subagent reserve 0.90（`core/hooks.py:30-60`）。
- SARIF 2.1.0 输出：CWE 归一化 + severity 压缩（`report/sarif.py:20-40`）。

### 4.2 共性工程优点（5 条）

1. **「薄编排 + 厚契约」统一 agent/工具接口**：Pentest-Swarm-AI 的 `Agent{Trigger,Handle,MaxConcurrency}` + autopentest-go 的 `BaseTool{name,aliases,isReadOnly,isDestructive}` + bettercap 的 `Module{Name,Handlers,Start,Stop}` 都走"接口契约 + 注册表 + 状态机外挂"，编排层不写业务逻辑。主项目 `LoadMarkdownAgentsDir` + `LoadToolsFromDir` 已有此精神，可补"事件驱动型 agent（trigger predicate）"。

2. **确定性安全层叠在 LLM 之前**：shellsafe 元字符拒绝（Pentest-Swarm-AI）+ HIGH_IMPACT 审批集（mcpstrike）+ 证据级反幻觉闸门（VulnClaw）+ TurnToolCallLimiter + tool_call_ids 去重（strix）——四件套纵深防御，无 LLM 在环。

3. **证据/内存「raw 落盘 + 高信号预览注入」双轨**：VulnClaw 的 AgentState.evidence（raw + hash + 证据号）+ correction_layer 正则抽高信号；autopentest-go 的三投影；Pentest-Swarm-AI 的 blackboard Finding + pheromone + supersede——**原始数据不进模型上下文，只进结构化高信号摘要 + 可回查编号引用**，替代粗暴截断。

4. **预算/限流/卡死防护分层**：per-campaign Budget + per-agent AgentBudget + idleTimeout 静默完成（Pentest-Swarm-AI）+ warn bands + subagent reserve（strix）+ 20 次连续 tool-call 上限（mcpstrike）——三段防卡死。

5. **声明式 playbook/skill + 确定性路由优先于 LLM 路由**：playbooks YAML + pheromones YAML（Pentest-Swarm-AI）+ skill 确定性 typed-weight 路由（VulnClaw）+ `{{arg|default}}` 占位符 + 全局变量自动填充（arsenal-ng）——**把可枚举的决策做成纯函数 + 配置，LLM 只做不可枚举的推理**。

### 4.3 对主项目最有价值的部分 + 适用前提

- **autopentest-go 的 `RunModelTurn` 单入口 + `ToolExecutionResult` 三投影**：主项目 `internal/multiagent/` 90+ 中间件但 turn 引擎可能分散，三投影可统一 `internal/tooloutput/` + `internal/multiagent/eino_tool_result_*`。前提：不换 Eino ADK，只收敛入口。
- **drission-rs 的 Agent Runtime 语义动作 + BrowserPool + Session 模式**：主项目 `internal/vision/` + `internal/c2/` + `internal/robot/` 可吸收，前提用 Go（chromedp/cdp-golang）重写。
- **Pentest-Swarm-AI 的 blackboard + shellsafe + scope**：主项目 `internal/attackchain/` 线性结构可升级为 blackboard，`internal/security/executor.go` 叠加 shellsafe。前提：不引 Postgres/Redis（用 `MemoryBoard` + SQLite）。
- **caveman 的 skills-lock + verbs-gate + prompt_byte_budget**：主项目 `internal/skillpackage/` 可直接加锁文件 + frontmatter 字段 + 漂移门。前提：用 graft 图谱派生真实 108 tool 集。
- **VulnClaw 的 AgentState 证据模型 + 确定性 digest + skill 确定性路由**：主项目 `internal/tooloutput/` + `internal/multiagent/eino_summarize*` + skill 加载器可吸收。前提：设计思想重写为 Go，不搬 Python。

### 4.4 不应照搬的内容

- **Python 运行时依赖**：VulnClaw/strix/mcpstrike/SkillClaw/PE-reverse 的 Python 脚本逻辑不可直接移植（主项目无 Python runtime，除桌面壳内嵌 Python 3.13.5 用于特定工具）。
- **Postgres + pgvector + Redis**（Pentest-Swarm-AI）：破坏主项目"单文件桌面"定位，用 `MemoryBoard` + SQLite 向量即可。
- **Docker sandbox + Caido + Playwright**（strix）：桌面端不跑 Docker。
- **FastMCP + OllamaBridge**（mcpstrike）：主项目用 Go MCP SDK。
- **Wails**（autopentest-go）：主项目是 Electron，不搬 Wails bindings（但 service 层设计可参考）。
- **`ncruces/go-sqlite3`**（autopentest-go）：主项目用 `mattn/go-sqlite3` CGO，不换。
- **AGPL-3.0/GPL-3.0 传染性许可**（Pentest-Swarm-AI/bettercap）：若闭源/商用需隔离。
- **caveman 的 token 压缩 persona**：对渗透报告有害（报告需完整叙述）。
- **TIOCSTI 终端预填**（arsenal-ng）：需 root/CAP_SYS_ADMIN，安全代价高。
- **WiFi/BLE/CAN/HID 无线攻击模块**（bettercap）：与 AI 渗透平台无关。

---

## 5. 差距分析

### 5.1 功能层差距

| 差距 | 主项目现状 | 参考亮点 | 影响 |
|------|----------|---------|------|
| **blackboard/stigmergic 协作** | `internal/attackchain/builder.go` 线性结构 | Pentest-Swarm-AI Board 接口 + Subscribe channel | 多 agent 无法涌现式协作，攻击链靠 LLM 单点规划 |
| **Capability Provider 生命周期** | yaml 工具仅 name/command/parameters 声明 | PE-reverse supports→plan→validate→execute→rollback | 工具无 rollback/validate 阶段，破坏性操作不可逆 |
| **浏览器 Agent Runtime** | `internal/vision/` 输出格式未定 | drission-rs observe() accessibility outline | LLM 看整页 HTML，token 浪费 + 决策不准 |
| **SARIF 输出** | 无（攻击链自有格式） | strix SARIF 2.1.0 + CWE 归一化 | 无法对接 GitHub code-scanning/ASPM |

### 5.2 架构层差距

| 差距 | 主项目现状 | 参考亮点 | 影响 |
|------|----------|---------|------|
| **turn 引擎分散** | `internal/multiagent/` 90+ 中间件但无单入口 | autopentest-go RunModelTurn + TurnSink | 桌面(Electron IPC)和 CLI 可能重复实现 turn |
| **工具结果无三投影** | `internal/tooloutput/` 单流 | autopentest-go Display/Evidence/API 三投影 | UI/证据/API 流耦合，修改一处影响多处 |
| **事件驱动型 agent 缺失** | agent 全是 Markdown 对话型 | Pentest-Swarm-AI Trigger() predicate | confirmation/验证类 agent 无法自动唤醒 |
| **campaign 级状态机缺失** | `internal/workflow/` Eino compose 图但无顶层状态机 | Pentest-Swarm-AI validTransitions map | 顶层状态无校验，非法跳转不报错 |

### 5.3 工程层差距

| 差距 | 主项目现状 | 参考亮点 | 影响 |
|------|----------|---------|------|
| **无 CI/CD** | `.github/workflows` 不存在 | ECC/agent-skills 都有 CI | 回归全靠人工，PR 无门禁 |
| **无静态检查** | 无 golangci/gosec/staticcheck 配置 | ECC hooks 体系 | 潜在 bug/安全问题无自动捕获 |
| **无 Dockerfile/Makefile** | 原生二进制 + NSIS | autopentest-go 桌面+CLI 共享引擎 | 容器化部署/开发环境标准化缺失 |
| **skill 锁文件缺失** | 31 skill 无锁 | caveman skills-lock.json + SkillClaw SkillIDRegistry | skill 供应链无验证，可被投毒 |
| **skill 漂移门缺失** | skill 可引用不存在工具 | caveman verbs-gate.mjs | skill 文档引用未交付工具，LLM 踩空 |
| **三层 evals 缺失** | 无 skill 触发路由测试 | agent-skills Tier 1/2/3 | 31 skill description 可能互相覆盖，渐进披露命中率无保障 |

### 5.4 性能层差距

| 差距 | 主项目现状 | 参考亮点 | 影响 |
|------|----------|---------|------|
| **证据 raw 进上下文** | 粗暴截断 | VulnClaw AgentState raw 落盘 + 高信号预览 | 长 HTML body 污染上下文，token 浪费 |
| **LLM 摘要递归** | `eino_summarize*` 若用 LLM | VulnClaw 确定性 digest（不调 LLM） | 摘要本身耗 token + 递归增长 |
| **浏览器→HTTP cookie 交接缺失** | 无 | drission-rs Session 模式 | 过盾/登录后仍开浏览器翻页，负载高 |
| **per-subagent 预算预留缺失** | `model_input_soft_budget_middleware.go` 单层 | strix warn bands + 0.90 reserve | 单子 agent 可耗尽全 campaign 预算 |
| **idle 静默完成缺失** | 无 | Pentest-Swarm-AI idleTimeout | swarm 卡死等待 |

### 5.5 安全层差距

| 差距 | 主项目现状 | 参考亮点 | 影响 |
|------|----------|---------|------|
| **WebShell SSRF 无私有 IP 过滤** | CRITICAL open | mcpstrike loopback-only + pinned TLS | 内网 SSRF 可达 |
| **无安全响应头** | CRITICAL open（H1） | stealth-browser bearer + 文件白名单 | XSS/clickjacking/MIME sniff 风险 |
| **无全局限流** | HIGH open（H10）仅 login+robot | Pentest-Swarm-AI per-agent ratelimit | API 暴力枚举/DoS |
| **session 无上限** | HIGH open（H7） | — | 内存耗尽 |
| **shellsafe 元字符拒绝缺失** | executor OS 级执行 | Pentest-Swarm-AI shellsafe.Parse | LLM-authored 命令注入 |
| **HIGH_IMPACT 审批集缺失** | RBAC 单道 | mcpstrike HIGH_IMPACT_TOOLS frozenset | 破坏性操作仅靠 RBAC，无操作风险第二道闸 |
| **证据级反幻觉闸门缺失** | `skills/anti-hallucination-pentest/` 有但未中间件化 | VulnClaw 逐字符验证 | agent 可声称未验证的 flag/结论 |
| **TurnToolCallLimiter 缺失** | 无 | strix | 退化生成上百次 poll/wait 卡死 |
| **local_mode 公网误开防护缺失** | `config.yaml:36 local_mode: true` | — | 若误暴露公网 = 全 API 免登录免 RBAC |

### 5.6 运维层差距

| 差距 | 主项目现状 | 参考亮点 | 影响 |
|------|----------|---------|------|
| **无 metrics/Prometheus** | 仅 trace/stdout | otel metrics | 生产可观测性不足 |
| **统一 home 缺失** | `data/logs/` 分散 | autopentest-go `~/.autopentest-go/` + 旧路径迁移 | 状态目录散乱 |
| **SARIF/ASPM 对接缺失** | 无 | strix SARIF | 无法对接企业 ASPM/GitHub code-scanning |

### 5.7 测试与真实用户体验差距

| 差距 | 主项目现状 | 参考亮点 | 影响 |
|------|----------|---------|------|
| **前端无测试** | 无 Playwright/cypress | agent-skills evals + superpowers TDD-for-skills | 11190 行 chat.js 无回归保障 |
| **audit/robot/skillpackage 零测试** | 0 测试文件 | — | 关键安全/资产包无覆盖 |
| **无真 E2E** | 223 test 仅 1 含 E2E 字样 | drission-rs/autopentest-go 有集成测试 | 登录→配置→对话→工具→结果链路无端到端验证 |
| **skill 触发路由无测试** | 无 | agent-skills Tier 2 TF-IDF 碰撞检测 | 31 skill description 可能互相覆盖 |

---

## 6. 可迁移 / 半可迁移 / 不建议迁移

### 6.1 可迁移（高优先，设计思想直接可落地为 Go）

| 项 | 分类 | 原因 | 证据 | 预期收益 | 成本 | 风险 | 主项目适配方式 | 最小实验 |
|----|------|------|------|---------|------|------|--------------|---------|
| **shellsafe 元字符拒绝** | 可迁移 | Go 同语言，纯函数 | Pentest-Swarm-AI `shellsafe.go:23-40` | 防 LLM 命令注入 | 低 | 低 | `internal/security/` 新增 `shellsafe.go`，executor 调用前过一层 | 单测覆盖 22 个注入 case |
| **scope validator** | 可迁移 | CIDR/Domain/Port 四元纯函数 | Pentest-Swarm-AI `validator.go:18-50` | 所有网络工具调用前统一闸门 | 低 | 低 | `internal/security/scope.go`，工具 yaml 加 scope 字段 | 单测覆盖 |
| **HIGH_IMPACT 审批集** | 可迁移 | frozenset 确定性 | mcpstrike `policy.py:8-25` | 破坏性操作第二道闸 | 低 | 低 | `internal/security/high_impact.go`，RBAC 之外叠加 | 单测 |
| **TurnToolCallLimiter** | 可迁移 | 纯函数 | strix `tool_call_limits.py:8-25` | 防退化卡死 | 低 | 低 | `internal/multiagent/eino_turn_tool_call_limiter.go` | 单测 |
| **tool_call_ids 唯一化** | 可迁移 | defaultdict[deque] | strix `tool_call_ids.py:8-25` | 防 strict provider 拒整轮 | 低 | 低 | `internal/multiagent/eino_tool_call_ids.go` | 单测 |
| **安全响应头中间件** | 可迁移 | Gin 中间件标准 | stealth-browser bearer | 修 H1 CRITICAL | 低 | 低 | `internal/security/security_headers.go`，app.go Use() | curl 验证头 |
| **WebShell SSRF 私有 IP 过滤** | 可迁移 | net.IsLoopback/IsPrivate | mcpstrike loopback-only | 修 C2 CRITICAL | 低 | 低 | `internal/handler/webshell.go` 加 isPrivateIP | 单测覆盖保留 IP 段 |
| **session LRU 上限** | 可迁移 | map + LRU | — | 修 H7 HIGH | 低 | 低 | `auth_manager.go` sessions map 加 LRU + max 1024 | 单测 |
| **skills-lock.json + frontmatter 扩展** | 可迁移 | caveman lockfile + 字段 | caveman `skills-lock.json`/`registry.json` | skill 供应链验证 | 中 | 低 | `internal/skillpackage/` 加锁文件 + frontmatter 加 prompt_byte_budget/entry/stop/guardrails | 现有 31 skill 生成锁 |
| **verbs-gate 漂移门** | 可迁移 | graft 派生真实 tool 集 | caveman `verbs-gate.mjs` | skill 不引用幽灵工具 | 中 | 低 | 用 graft 图谱派生 108 tool 名，校验 skill 引用 | 现有 skill 扫一遍 |
| **AgentState 证据双轨** | 半可迁移 | Python→Go 重写 | VulnClaw `agent_state.py:10-40` | raw 不进上下文，token 省 | 中 | 中 | `internal/tooloutput/` + `internal/multiagent/eino_assistant_output_accumulator.go` | 单工具结果对比 |
| **确定性 context_budget digest** | 半可迁移 | 不调 LLM | VulnClaw `context_budget.py:1-25` | 摘要不耗 token | 中 | 中 | `internal/multiagent/eino_summarize.go` 加确定性分支 | 对比 LLM 摘要 vs 确定性 |
| **skill 确定性路由预选** | 半可迁移 | typed-weight + alias | VulnClaw `resolver.py:1-40` | 降 LLM 调用 | 中 | 中 | `internal/skillpackage/` 加 resolver | 31 skill 路由命中率对比 |
| **blackboard (MemoryBoard)** | 半可迁移 | Go 接口 + SQLite | Pentest-Swarm-AI `board.go:9-50` | 多 agent 涌现式协作 | 高 | 中 | 新建 `internal/blackboard/`，不引 Postgres/Redis | 2 agent 协作 demo |
| **autopentest-go RunModelTurn 单入口** | 半可迁移 | 收敛入口 | autopentest-go `agentloop.go:1-100` | 消除桌面/CLI 重复 | 高 | 中 | `internal/multiagent/` 收敛 | 先审计是否有重复 |
| **drission-rs Agent Runtime + BrowserPool** | 半可迁移 | Go 重写 Rust 设计 | drission-rs `agent/mod.rs`/`pool/mod.rs` | 浏览器 token 省 + 高并发 | 高 | 中 | `internal/vision/` + `internal/c2/` 用 chromedp | 单页面 observe 对比 |

### 6.2 半可迁移（需改造或依赖主项目不存在的基础设施，择机）

- **autopentest-go 的 `ToolExecutionResult` 三投影**：需重构 `internal/tooloutput/`，中风险，建议先审计当前是否真有 UI/证据/API 流耦合。
- **Capability Provider 生命周期 Protocol**（PE-reverse）：需把 yaml 工具升级为带 plan/validate/rollback 的 capability interface，改动面大，建议只对"破坏性工具" subset 先做。
- **SARIF 输出**（strix）：需 attackchain 节点加 CWE 字段，中成本，对接企业 ASPM 才值得。
- **hooks 16 事件 Registry**（autopentest-go）：主项目已有 Eino 中间件链，若要做桌面级 session lifecycle hook 可参考，但可能与 Eino 中间件重叠。

### 6.3 不建议迁移

- **Postgres + pgvector + Redis**（Pentest-Swarm-AI）：破坏单文件桌面定位。
- **Docker sandbox + Caido + Playwright**（strix）：桌面端不跑 Docker。
- **FastMCP + OllamaBridge**（mcpstrike）：主项目 Go MCP SDK。
- **Wails**（autopentest-go）：主项目 Electron。
- **ncruces/go-sqlite3**（autopentest-go）：主项目已锁定 mattn/go-sqlite3 CGO。
- **caveman token 压缩 persona**：对渗透报告有害。
- **TIOCSTI 终端预填**（arsenal-ng）：需 root，安全代价高。
- **WiFi/BLE/CAN/HID 模块**（bettercap）：与 AI 渗透无关。
- **人物蒸馏角色扮演**（nuwa-skill）：渗透 skill 应是工具操作非人格扮演。
- **Python 运行时脚本**（VulnClaw/strix/mcpstrike/SkillClaw/PE-reverse 的 Python 逻辑）：主项目无 Python runtime（除桌面壳内嵌 Python 3.13.5 用于特定工具），逻辑需 Go 重写。

---

## 7. 优先级优化路线图（P0/P1/P2）

### 7.1 P0（影响安全、稳定性、数据正确性或核心用户路径）

| # | 做什么 | 为什么 | 涉及模块 | 前置依赖 | 预期收益 | 风险 | 数据迁移 | 兼容性 | 验证方式 | 回滚 |
|---|-------|-------|---------|---------|---------|------|---------|-------|---------|------|
| P0-1 | **WebShell SSRF 私有 IP 过滤** | C2 CRITICAL，内网 SSRF 可达 | `internal/handler/webshell.go` | 无 | 阻断内网 SSRF | 低 | 无 | 不影响 | 单测覆盖 10/172/192.168/169.254/::1 + curl 实测 | 移除过滤函数 |
| P0-2 | **安全响应头中间件** | H1 CRITICAL，XSS/clickjacking/MIME | `internal/security/security_headers.go`(新)+`app.go` Use() | 无 | 修 H1 | 低（需注意 CSP 不阻断现有 inline onclick H2） | 无 | 需测 inline onclick | curl -I 验证 6 头 | 移除中间件 |
| P0-3 | **err.Error() 脱敏** | C3 CRITICAL，405 处裸露 | `internal/handler/*.go` 10+ 文件 | 无 | 不泄露内部错误 | 低 | 无 | 不影响 | grep 复查裸露数↓ + 错误响应不含堆栈 | 逐文件回滚 |
| P0-4 | **local_mode 公网误开防护** | G1 引入新风险，`config.yaml:36 local_mode: true` | `internal/app/app.go` New() 启动断言 | 无 | 防误暴露公网=全 API 免登录 | 低 | 无 | 不影响 local_mode 单机 | 启动时若 host 非 127.0.0.1/0.0.0.0 + local_mode=true 则 fatal | 移除断言 |
| P0-5 | **全局限流** | H10 HIGH，除 login/robot 外无限流 | `internal/security/ratelimit.go`(扩)+`app.go` protected 组 | 无 | 防 API 暴力枚举/DoS | 低 | 无 | 不影响 | curl 暴力打 /api/skills 第 N 次 429 | 移除中间件 |

### 7.2 P1（明显提升质量、性能、可维护性和扩展性）

| # | 做什么 | 为什么 | 涉及模块 | 前置依赖 | 预期收益 | 风险 | 数据迁移 | 兼容性 | 验证方式 | 回滚 |
|---|-------|-------|---------|---------|---------|------|---------|-------|---------|------|
| P1-1 | **shellsafe 元字符拒绝** | LLM-authored 命令注入 | `internal/security/shellsafe.go`(新)+executor | 无 | 命令注入纵深防御 | 低 | 无 | 不影响 | 22 注入 case 单测 | 移除调用 |
| P1-2 | **scope validator** | 网络工具调用前统一闸门 | `internal/security/scope.go`(新)+工具 yaml | 无 | 越界扫描防护 | 低 | 无 | 工具 yaml 加 scope 字段 | 单测 CIDR/Domain | 移除校验 |
| P1-3 | **HIGH_IMPACT 审批集** | 破坏性操作第二道闸 | `internal/security/high_impact.go`(新)+RBAC | 无 | 操作风险分级 | 低 | 无 | 不影响 | 破坏性工具触发审批 | 移除集 |
| P1-4 | **TurnToolCallLimiter + tool_call_ids** | 防退化卡死 + strict provider 拒整轮 | `internal/multiagent/eino_*.go`(新) | 无 | 防卡死 | 低 | 无 | 不影响 | 单测 + 长对话压测 | 移除中间件 |
| P1-5 | **session LRU 上限** | H7 HIGH 内存耗尽 | `internal/security/auth_manager.go` | 无 | 内存有界 | 低 | 无 | 不影响 | 单测 max 1024 + LRU 淘汰 | 回退 map |
| P1-6 | **skills-lock.json + frontmatter 扩展** | skill 供应链验证 | `internal/skillpackage/` + 31 skill frontmatter | 无 | 防投毒 + 预算控制 | 低 | 锁文件新建 | frontmatter 加字段向后兼容 | 现有 skill 生成锁 + SHA256 校验 | 删锁文件 |
| P1-7 | **verbs-gate 漂移门** | skill 引用幽灵工具 | `internal/skillpackage/verbs_gate.go`(新)+graft | P1-6 | LLM 不踩空 | 低 | 无 | 不影响 | 现有 skill 扫一遍报违反 | 移除门 |
| P1-8 | **AgentState 证据双轨** | raw 不进上下文，token 省 | `internal/tooloutput/`+`internal/multiagent/eino_assistant_output_accumulator.go` | 无 | token 省 + 证据可回查 | 中 | evidence 表新增 | 不影响 | 单工具结果 token 对比 | 回退单流 |
| P1-9 | **确定性 context_budget digest** | 摘要不耗 token | `internal/multiagent/eino_summarize.go` | P1-8 | 摘要成本↓ | 中 | 无 | 不影响 | LLM 摘要 vs 确定性 token 对比 | 回退 LLM 摘要 |
| P1-10 | **skill 确定性路由预选** | 降 LLM 调用 | `internal/skillpackage/resolver.go`(新) | 无 | 命中率↑成本↓ | 中 | 无 | 不影响 | 31 skill 路由命中率对比 | 回退 LLM 路由 |
| P1-11 | **app.go 拆分** | C1 CRITICAL 2254 行 | `internal/app/` 拆 5-8 个文件 | 无 | 可维护性 | 中 | 无 | 不影响 | go vet/build + 路由数不变 | git revert |
| P1-12 | **RunDeepAgent 拆分** | H8 HIGH 582 行 | `internal/multiagent/runner.go` | 无 | 可维护性 | 中 | 无 | 不影响 | go vet/build + 行数↓ | git revert |
| P1-13 | **CI/CD + 静态检查** | 工程成熟度缺口 | `.github/workflows/`+`.golangci.yml` | 无 | 回归自动化 | 低 | 无 | 不影响 | push 触发 go vet/test/gosec | 删 workflow |
| P1-14 | **audit/robot/skillpackage 测试补齐** | 关键包零测试 | `internal/audit/*_test.go` 等 | 无 | 覆盖率↑ | 低 | 无 | 不影响 | go test 覆盖率↑ | 删测试 |

### 7.3 P2（非当前阶段必须的增强）

| # | 做什么 | 为什么 | 涉及模块 | 前置依赖 | 预期收益 | 风险 | 验证方式 |
|---|-------|-------|---------|---------|---------|------|---------|
| P2-1 | **blackboard (MemoryBoard)** | 多 agent 涌现式协作 | 新建 `internal/blackboard/` | 无 | 协作模式升级 | 中 | 2 agent 协作 demo |
| P2-2 | **drission-rs Agent Runtime + BrowserPool** | 浏览器 token 省 + 高并发 | `internal/vision/`+`internal/c2/` 用 chromedp | 无 | 浏览器自动化升级 | 中 | 单页面 observe 对比 |
| P2-3 | **Capability Provider 生命周期** | 工具 plan/validate/rollback | `internal/mcp/` capability interface | 无 | 破坏性操作可逆 | 中 | 破坏性工具 subset demo |
| P2-4 | **SARIF 输出** | 对接 GitHub code-scanning/ASPM | `internal/attackchain/` + CWE 字段 | 无 | 企业集成 | 低 | SARIF 校验器 |
| P2-5 | **autopentest-go RunModelTurn 单入口** | 消除桌面/CLI 重复 | `internal/multiagent/` 收敛 | 先审计重复 | 消除重复 | 中 | 重复代码行数对比 |
| P2-6 | **chat.js 拆分** | M1，11190 行 | `web/static/js/chat.js` 拆模块 | 无 | 前端可维护 | 中 | node --check + i18n 完整性 |
| P2-7 | **console.* 清理** | M3，265 处 | `web/static/js/*.js` | 无 | 生产洁净 | 低 | grep console↓ |
| P2-8 | **metrics/Prometheus 导出** | 生产可观测性 | `internal/einoobserve/` + otel metrics | 无 | 可观测性↑ | 低 | /metrics 端点 |
| P2-9 | **前端 Playwright E2E** | 真实用户路径验证 | `web/tests/`(新) | 无 | 回归保障 | 低 | 登录→配置→对话链路 |
| P2-10 | **三层 evals（Tier 1/2）** | skill 触发路由质量 | `internal/skillpackage/evals/`(新) | P1-6/P1-7 | 渐进披露命中率 | 中 | TF-IDF 碰撞检测 |

---

## 8. 全栈实施方案

### 8.1 前端改造建议

- **P0-2 安全响应头**：Gin 中间件加 CSP/X-Frame-Options/X-Content-Type-Options/Strict-Transport-Security/Referrer-Policy/Permissions-Policy。注意 CSP 需配合处理现有 inline onclick（H2），用 nonce 或重构为 addEventListener。
- **P2-6 chat.js 拆分**：11190 行拆成 chat-core/chat-input/chat-render/chat-tools/chat-files 等模块，保持 DOMPurify+marked 防护链。
- **P2-7 console.* 清理**：265 处替换为可选 logger 或删除。
- **P2-9 前端 E2E**：Playwright 覆盖登录→配置→对话→工具调用→结果回显。
- **i18n**：已对齐良好（zh 4350/en 4362，复数形式非真缺失），保持。

### 8.2 后端改造建议

- **P0-1/P0-3/P0-4/P0-5 安全 CRITICAL/HIGH**：见路线图。
- **P1-1/P1-2/P1-3 确定性安全层叠**：shellsafe + scope + HIGH_IMPACT 三件套，挂 executor 前。
- **P1-4 防卡死**：TurnToolCallLimiter + tool_call_ids 中间件。
- **P1-8/P1-9/P1-10 证据/digest/路由**：AgentState 双轨 + 确定性 digest + skill 确定性路由。
- **P1-11/P1-12 拆分**：app.go 2254 行拆 5-8 文件，RunDeepAgent 582 行拆。
- **P1-13/P1-14 工程化**：CI/CD + 静态检查 + 补测试。
- **P2-1 blackboard**：MemoryBoard + SQLite（不引 Postgres/Redis）。
- **P2-3 Capability Provider**：破坏性工具 subset 先做 plan/validate/rollback。

### 8.3 数据层改造建议

- **P1-6 skills-lock.json**：31 skill 生成锁文件（source/hash/path）。
- **P1-8 evidence 表**：raw evidence 落盘 + hash/size/证据号。
- **不换 SQLite**：保持 mattn/go-sqlite3 CGO + WAL + schema_migrations，不引 Postgres/Redis。
- **P2-4 SARIF**：attackchain 节点加 CWE 字段（schema 迁移）。

### 8.4 接口层改造建议

- **P0-2 安全响应头**：HTTP 中间件。
- **P0-5 全局限流**：protected 路由组加 RateLimitMiddleware。
- **P2-5 RunModelTurn 单入口**（如审计有重复）：收敛桌面 IPC 和 CLI。
- **P2-8 metrics**：/metrics Prometheus 端点。

### 8.5 配置管理建议

- **P0-4 local_mode 启动断言**：`app.go` New() 检查 host + local_mode 组合。
- **P1-2 scope**：工具 yaml 加 scope 字段（CIDR/Domain/Port/Excluded）。
- **P1-6 frontmatter**：skill SKILL.md 加 prompt_byte_budget/entry_condition/stop_condition/guardrails/standards/difficulty。
- **config.yaml 不动**（运行态），config.example.yaml 同步新增字段示例。

### 8.6 日志与监控建议

- **P2-8 metrics/Prometheus**：otel metrics 导出 + /metrics 端点。
- **保留 zap 结构化日志**。
- **P1-8 evidence 落盘**：raw evidence 可回查。

### 8.7 测试策略建议

- **P1-13 CI/CD**：push 触发 go vet/test/gosec + 前端 node --check + i18n 完整性。
- **P1-14 补测试**：audit/robot/skillpackage 三包从 0 到有。
- **P2-9 前端 E2E**：Playwright 真实用户路径。
- **P1-7 verbs-gate**：现有 skill 扫一遍报违反。
- **P2-10 三层 evals**：Tier 1 结构 + Tier 2 TF-IDF 碰撞检测。
- **不追求覆盖率数字**：核心逻辑覆盖正常/错误/边界/权限路径。

### 8.8 部署与运维建议

- **保持原生二进制 + NSIS**：不引 Dockerfile（桌面单机优先）。
- **P1-13 CI**：GitHub Actions 跑 vet/test/gosec。
- **P2-5 统一 home**（可选）：参考 autopentest-go `~/.autopentest-go/` + 旧路径迁移。

### 8.9 建议的实施批次和依赖顺序

```
批次1（P0 安全 CRITICAL，可并行，无依赖）：
  P0-1 SSRF过滤 | P0-2 安全头 | P0-3 err脱敏 | P0-4 local_mode断言 | P0-5 全局限流
  → 验证：单测 + curl + go vet/build

批次2（P1 确定性安全层，可并行，无依赖）：
  P1-1 shellsafe | P1-2 scope | P1-3 HIGH_IMPACT | P1-4 TurnLimiter | P1-5 session LRU
  → 验证：单测 + 长对话压测

批次3（P1 skill 体系，串行）：
  P1-6 skills-lock + frontmatter → P1-7 verbs-gate
  → 验证：锁生成 + 漂移扫描

批次4（P1 证据/digest/路由，串行，依赖批次2）：
  P1-8 AgentState双轨 → P1-9 确定性digest → P1-10 skill确定性路由
  → 验证：token 对比 + 命中率对比

批次5（P1 重构 + 工程化，可并行）：
  P1-11 app.go拆分 | P1-12 RunDeepAgent拆分 | P1-13 CI/CD | P1-14 补测试
  → 验证：go vet/build + 路由数不变 + CI 绿

批次6（P2 增强，按需）：
  P2-1 blackboard | P2-2 浏览器Agent | P2-3 Capability | P2-4 SARIF | ...
  → 各自独立验证
```

---

## 9. 实施授权状态

**当前未授权修改源代码。** 本节只说明建议优先批准的批次。

### 9.1 建议优先批准的批次

| 批次 | 影响范围 | 风险等级 | 预计验证方式 | 需单独审批事项 |
|------|---------|---------|------------|--------------|
| **批次1（P0 安全）** | 5 个 CRITICAL/HIGH 修复，局部改动 | L1-L2 | 单测 + curl + go vet/build | local_mode 断言需确认不影响桌面运行态 |
| **批次2（P1 确定性安全）** | 5 个新中间件/函数，局部 | L1 | 单测 + 长对话压测 | 无 |
| **批次3（P1 skill 体系）** | skillpackage + 31 skill frontmatter | L2 | 锁生成 + 漂移扫描 | frontmatter 加字段向后兼容，无破坏 |
| **批次4（P1 证据/digest/路由）** | tooloutput + multiagent | L2 | token 对比 + 命中率 | 无 |
| **批次5（P1 重构+工程化）** | app.go 拆分 + CI | L2 | go vet/build + CI 绿 | app.go 拆分需保证路由数不变 |
| **批次6（P2 增强）** | 按需 | L2-L3 | 各自独立 | blackboard/浏览器 Agent 改动面大 |

### 9.2 哪些事项需要单独审批

- **L3 高风险**（即使进入 Phase B 仍需额外确认）：
  - app.go 拆分若触及公共路由注册顺序（影响所有 API）。
  - blackboard 若替换现有 attackchain builder（影响攻击链生成）。
  - Capability Provider 若改工具 yaml schema（影响 108 工具加载）。
- **local_mode 断言**：需确认不影响用户当前 `config.yaml:36 local_mode: true` 运行态（断言只在 host 非 127.0.0.1/0.0.0.0 时 fatal，单机不受影响）。
- **CI/CD**：需确认是否允许在 `.github/workflows/` 新建文件（不推送不合并，仅本地）。

**不得在本阶段实施代码修改。** 等待用户确认路线图后进入 Phase B。

---

## 10. 需要我补充的信息

只列出会实质性影响下一阶段的缺失信息：

### 10.1 用户是否要优先实施哪个批次？

- **为什么需要**：批次1-5 共 20+ 项，一次全做工作量大，需知道优先级。
- **不提供会影响什么**：无法确定 Phase B 首批范围，可能做多了或做少了。
- **是否可临时假设继续**：可以假设"批次1（P0 安全）优先"，但需用户确认。
- **建议默认选择**：**批次1（P0 安全 CRITICAL/HIGH 修复）**——5 项都是局部、低风险、高收益、无依赖，最快闭环台账与真实代码的不一致。

### 10.2 是否允许创建本地 commit / 新增 CI 配置文件？

- **为什么需要**：批次5 涉及 `.github/workflows/`、`.golangci.yml` 新文件，批次1-4 涉及源码修改。
- **不提供会影响什么**：不知是否允许本地 commit（CLAUDE.md 禁止自动 commit/push，需明确）。
- **是否可临时假设继续**：可以假设"只改代码不 commit，等用户验收后再决定 commit"。
- **建议默认选择**：**只改代码 + 本地验证，不自动 commit/push**，用户验收后再决定。

### 10.3 local_mode 断言的边界条件是否可接受？

- **为什么需要**：P0-4 需在启动时判断 host + local_mode 组合，"host=0.0.0.0 + local_mode=true"是否 fatal？
- **不提供会影响什么**：无法确定断言严格度，可能误伤合法的局域网部署。
- **是否可临时假设继续**：可以假设"仅 host=0.0.0.0（非 127.0.0.1）+ local_mode=true 时 fatal，其他放行"。
- **建议默认选择**：**仅当 host 非 127.0.0.1/localhost 且 local_mode=true 时 fatal log + 拒绝启动**，其他放行。

### 10.4 是否有付费 API / 真实 LLM 调用预算用于验证？

- **为什么需要**：批次4（确定性 digest vs LLM 摘要对比、skill 确定性路由命中率）需真实 LLM 调用验证。
- **不提供会影响什么**：只能用静态对比 + 已有 glm-5.2 本地代理验证，可能不够。
- **是否可临时假设继续**：可以假设"用现有 glm-5.2 本地代理（127.0.0.1:15721）验证，不新增付费"。
- **建议默认选择**：**用现有本地代理验证，不新增付费 API 调用**。

### 10.5 黑名单是否覆盖时序问题？若不包含时序问题，是否需要调整批次4的子项顺序？

- **为什么需要**：批次4 的 AgentState 双轨 + 确定性 digest + skill 路由三者有时序依赖（双轨先于 digest，digest 先于路由），需确认顺序。
- **不提供会影响什么**：可能 digest 先于双轨导致 raw 已进上下文，digest 无意义。
- **是否可临时假设继续**：可以假设"P1-8 双轨 → P1-9 digest → P1-10 路由"严格串行。
- **建议默认选择**：**P1-8 → P1-9 → P1-10 严格串行**，已在批次4 标注。

---

## 11. 一页速查：你到底要参考什么

> 这一节是给决策者的 TL;DR。如果你只看一节，看这节。

### 你现在面对的是什么

主项目 CyberStrikeAI 已经是个能跑的 AI 渗透平台（Go+Eino+MCP+桌面免登录已闭环），但有三类真实问题：

**A. 台账说修了实际没修的安全洞（最该先堵）**
- WebShell SSRF 没拦私有 IP → 内网可达
- 没有任何安全响应头（CSP/X-Frame-Options 全缺）→ XSS/clickjacking
- 405 处把 `err.Error()` 直接吐给前端 → 泄露内部细节
- `local_mode: true` 运行态开着但没公网误开防护 → 误暴露=全 API 免登录
- 除 login/robot 外所有 API 无限流 → 暴力枚举

→ 这 5 项就是 **批次1（P0）**，全是局部、低风险、无依赖的修，最快闭环。

**B. 工程成熟度缺口**
- 无 CI/CD、无静态检查、无 Dockerfile/Makefile
- audit/robot/skillpackage 三个关键包零测试，前端无任何测试
- 223 个测试文件只有 1 个算 E2E

**C. 设计可借鉴的参考（26 个项目里挑出来的精华）**

| 你想解决什么 | 去参考谁 | 参考它什么 |
|------------|---------|-----------|
| 工具执行后结果太长撑爆上下文 | VulnClaw | AgentState 证据双轨：raw 落盘 + 只把高信号（URL/表单/JS endpoint/flag）注入上下文 |
| 摘要本身也调 LLM 烧 token | VulnClaw | 确定性 context_budget digest（不调 LLM，结构化渲染） |
| skill 互相覆盖/选不准 | VulnClaw + agent-skills | 确定性 typed-weight 路由 + Tier2 TF-IDF 碰撞检测 |
| skill 能被投毒/引用幽灵工具 | caveman | skills-lock.json（SHA256 锁）+ verbs-gate 漂移门 |
| LLM 生成的 shell 命令有注入 | Pentest-Swarm-AI | shellsafe.Parse quote-aware 元字符拒绝 |
| 网络工具越界扫描 | Pentest-Swarm-AI | scope validator（CIDR/Domain/Port 四元） |
| 破坏性操作只靠 RBAC 单道闸 | mcpstrike | HIGH_IMPACT_TOOLS frozenset 第二道审批 |
| agent 退化生成上百次工具调用卡死 | strix | TurnToolCallLimiter + tool_call_ids 唯一化 |
| 浏览器抓页面 token 浪费 | drission-rs | observe() 输出 accessibility outline 而非 HTML + Session 模式 cookie 交接 |
| 多 agent 不会涌现式协作 | Pentest-Swarm-AI | stigmergic blackboard + pheromone decay（用 MemoryBoard+SQLite，别引 Postgres） |
| 桌面和 CLI 可能重复实现 turn | autopentest-go | RunModelTurn + TurnSink 单入口 + ToolExecutionResult 三投影 |
| 工具破坏性操作不可逆 | PE-reverse-skill | Capability Provider 生命周期（plan/validate/execute/rollback） |
| 浏览器 MCP 工具面太大噪音多 | stealth-browser-mcp | 分区化 + `--minimal`/`--disable-*` 运行时裁剪 |
| 渗透报告想对接 GitHub code-scanning | strix | SARIF 2.1.0 + CWE 归一化 |

### 你需要做的决定（就一个）

**批准哪个批次进入 Phase B（改代码）？**

- 想最快见效 + 修真实安全洞 → **批次1（P0 安全 5 项）**
- 想安全纵深一次到位 → **批次1+2（P0 + P1 确定性安全层，10 项）**
- 想先看文档再定 → 现在你就在看，看完告诉我

无论选哪个，我都会：先输出本批次影响分析 → 实施 → 单测/curl/go vet 验证 → 独立 Critic 复验 → 修复 → 停在验收门，不自动 commit/push。

---

*文档结束。等待用户确认路线图后进入 Phase B。*
