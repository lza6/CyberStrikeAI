# CyberStrikeAI 对标分析与优化指南（结果计划指南 · v2 扩展版）

> 本文档基于「主项目 CyberStrikeAI」与「参考项目根2（473 项目）+ 视觉参考根1（11 项目）」的自动扫描、识别、筛选、差距分析得出。
> 生成日期：2026-09-03（v2，承接 v1 的 342 项目基线，扩展至 484 项目）。
> 遵循 spec.md 契约与 workflow_status.md 台账。级别标签：`已验证`/`静态确认`/`合理推断`/`待验证`。
> 本轮为 **ANALYSIS_ONLY**：只分析，不改源代码。等用户批准实施范围后进入 Phase B。

---

## 决策摘要

- **任务复杂度**：深度 / 实验验证型（跨 484 项目 + 5 条产品方向升级 + 多 agent 并行扫描）
- **判断理由**：主项目生产部署且工程化成熟，但用户提出"通用化扩展 + 小白易用 + 黑匣子打开 + 用户自建 skills 沉淀 + 多模态 + CL4R1T4S prompt 优化"5 条新方向，需在 v1（342 项目）基线上重扫新增 142 项目并产出新路线图
- **当前目标**：Phase A 自动识别 + 筛选 + 差距分析 + 路线图，在 `参考的结果计划指南.md` 落盘
- **授权范围**：读取分析主项目/参考项目；非破坏性验证；创建/更新本指南；**不修改源代码/配置/依赖**
- **已知约束**：① 生产部署，向后兼容不可破坏 108 工具/74 skill/16 agent；② CGO 依赖 mingw gcc（本机已装 C:\mingw64）；③ 付费 API 红线（真实 LLM 语义评测不伪造）；④ Windows 无 .sh 业务脚本；⑤ MCP 服务器（graft/codegraph/sequential-thinking/memory 等）本会话连接超时，回退 Grep/Read
- **关键未知项**：① 用户是否要本轮纳入"通用化扩展"（安全→通用平台）还是只做安全垂直深化；② CL4R1T4S prompt 优化的边界（全量重写 18 agent 还是增量补结构）；③ 多模态是否允许引入本地推理依赖
- **最脆弱的隐藏假设**：假设"安全垂直深化 + 通用 skill 包"可共存于同一 skills/ 目录而不污染安全 agent 的 system prompt——需 v2 验证
- **预计工作流**：9 子代理并行深扫（CL4R1T4S prompt / 多模态 / 电商办公内容 / 桌面壳UX / skills 体系 / 记忆RAG / agent 编排 / 安全深化 / 工程化）→ 主项目深审 → 整合 v2 报告 → 独立 Critic → 等用户批准
- **主要验证方式**：静态读代码 + 文件证据；非破坏性 `go vet`（不改代码）；curl 起 exe 验证基线（可选）
- **当前阻塞项**：无（9 子代理并行中，主项目深审子代理在跑）

---

## 一、项目识别结果（v2 增量）

### 1.1 主项目识别（与 v1 一致，增量补 v2 锚点）

| 维度 | 结论 | v2 锚点证据 |
|------|------|------|
| 项目类型 | AI 原生网络安全智能中枢（前后端分离 + 桌面壳 + 多 agent + 工具治理 + C2/WebShell + 攻击链） | `README_CN.md`、`spec.md` |
| 技术栈 | Go 1.25 + CloudWeGo Eino ADK + MCP(go-sdk v1.3.1) + Gin + SQLite 双驱动(mattn CGO + modernc pure-go `-tags sqlite_pure_go`) + Redis(可选) + 原生 JS + Electron 31 + Python 3.13.5(工具进程) | `go.mod`、`AGENTS.md`、`config.example.yaml` |
| 运行入口 | `cmd/server/main.go`（`-config config.yaml --https/--http`） | `cmd/server/main.go` |
| 模块划分 | `internal/` 38+ 包 | `ls internal/`（v1 记 38，v2 实测含 reactions/coordkit/agentmemory 缺失/memory/ctxindex/cost/roi/metrics/sarif/attackchain/blackboard/capability/skillpackage 等已在） |
| 核心业务流 | 自然语言意图 → Eino 多代理（deep/plan_execute/supervisor + coordinator DAG）→ MCP 工具执行（108 工具 yaml）→ HITL 审批 → 证据沉淀（SQLite + blackboard 内存版）→ 攻击链建模 + SARIF | `internal/multiagent/coordinator_orchestrator.go`、`internal/security/executor.go`、`internal/attackchain/`、`internal/sarif/` |
| 安全五闸 | shellsafe→scope→HIGH_IMPACT→HITL→TurnLimiter（全部 done） | `internal/security/{shellsafe,scope,highimpact}.go`、`internal/hitl/`、`internal/multiagent/eino_turn_limiter.go` |
| 反应式引擎 | `internal/reactions/engine.go`（移植 agent-orchestrator，订阅 blackboard 事件→匹配规则→触发动作；config `reactions:` 段 + applyDefaultReactions）**已落地** | `internal/app/app.go:73,787`、`internal/reactions/engine.go`、`internal/config/config.go:53,1687` |
| coordkit | `internal/multiagent/coordkit/`（coordinator_dag/message_bus/structured_output/schema，移植 open-multi-agent）**已落地** | `internal/multiagent/coordinator_orchestrator.go`、`coordkit/schema.go` |
| 工具数 | **108 个 yaml**（v1 记 106，v2 实测 108） | `ls tools/*.yaml \| wc -l` |
| skills 数 | **30 个 skill 目录**（v1 记 74 skill 含子文件，v2 实测 30 顶层 skill 包） | `ls -d skills/*/` |
| agent 数 | **18 个 agent md** | `ls agents/*.md` |
| 前端 | 原生 JS + ES module，chat.js 已拆分为 `web/static/js/chat/`（attack-chain/chat-core/conversations/history/input/render/tools 等，v1 J10 已 done） | `ls web/static/js/chat/` |
| 桌面壳 | Electron 31：main.js/tray.js/splash.html/config.html/ai-config.js | `desktop/src/` |
| 记忆/知识 | `internal/memory`（extractor/fusion/store_memory/temporal，4 层雏形已有）+ `internal/memdir` + `internal/knowledge`（eino retriever/embedder/indexer）+ `internal/blackboard`（纯内存 MemoryBoard）+ `internal/ctxindex`（bm25/rrf）+ `internal/database/provenance.go` | 各包 ls |
| 多模态 | `internal/vision`（client/preprocess/tool/path，analyze_image MCP 工具，vision.enabled + model 配置） | `internal/vision/tool.go` |
| prompt 体系 | `internal/agent/default_single_system_prompt.go`（单代理内置）+ `agents/*.md`（18 agent，YAML frontmatter id/name/description/tools/max_iterations）+ `internal/microagent`（三层 bundle global→user→repo，移植 OpenHands）+ `internal/projectprompt`（blackboard.go/shell_tools.go，纯字符串 leaf 包）+ `internal/promptassembly`（Manager + workspace_context 模板，移植 OpenHands PromptManager） | 各文件 |
| 工程化 | Makefile/golangci.yml(D)/Dockerfile 多阶段/CI/245+ 测试/skills-lock.json/verbs-gate | `Makefile`、`.golangci.yml`、`Dockerfile` |

### 1.2 参考项目扫描结果（v2 增量）

参考根2 `C:\...\智能渗透\参考项目` v2 实测 **473 项目**（v1 为 342，新增 131）；参考根1 `C:\...\agent\视觉分析\视觉参考项目` **11 项目**（视觉/视频专项）。

- **语言分布**（根2）：Go 26 / Node 169 / Python 169 / Rust 33 / 其他 76
- **去重副本**：`agency-agents`=main、`claude-context`=master、`context-mode`=main、`claude-mem`=main 等为同上游重复拷贝
- **两个不同 agent-orchestrator**：`agent-orchestrator`（Untrivial-ai，Go+sqlc）与 `agent-orchestrator-main`（ComposioHQ，TS）是不同项目，都保留

### 1.3 v2 新增高价值参考项目（子代理深扫中，待整合）

> 以下为 v1 已识别 Top10（agentmemory 20/agent-orchestrator-main 19/mem0 19/LightRAG 19/context-mode 18/OpenHands 17/open-multi-agent 17/Pentest-Swarm-AI 17/agent-orchestrator Untrivial 18/OpenHarness 18）之外的**新增方向**，由 9 子代理深扫后回填。

**新增方向分类**（对应 9 子代理）：
1. **CL4R1T4S 系统提示词**（25 家厂商：ANTHROPIC/OPENAI/GOOGLE/XAI/META/MISTRAL/MOONSHOT/CURSOR/WINDSURF/DEVIN/MANUS/REPLIT/LOVABLE/VERCEL V0/BOLT/CLINE/DIA/FACTORY/HUME/MINIMAX/MULTION/PERPLEXITY/SAMEDEV/ZAI/BRAVE/CLUELY）→ 对应"CL4R1T4S prompt 优化"新需求
2. **多模态/视觉/视频**（根1 全部 11 + 根2 的 VoxCPM/LuxTTS/VoiceMem/PaddleDetection/face-SDK/video-shotcraft/video-use/remotion/ffmpeg-*/whisper/elevenlabs/ai-video-gen/sora/veo/text-to-video/luma-imagegen/nano-banana/stability-ai/fal-*/vision-* 等）→ 对应"多模态扩展"新需求
3. **电商/办公/PPT/内容**（shopify/woocommerce/tw-ecom-*/odoo-*/pptx-*/2slides/guizang-ppt/nanobanana-ppt/morph-ppt/slidev/slides/officecli-*/docx/xlsx/pdf-*/baoyu-skills/social-*/reddit/twitter/linkedin/xiaohongshu/youtube/canva/figma/design-*）→ 对应"通用化扩展（非软件任务 agent 化）"新需求
4. **桌面壳/UX/可观测/小白易用**（Pake/Hermes-CN-Desktop/JeffBox/dsh-desktop/pi-agent-desktop/EverOS/METATRON/Learnova/tauri-*/auri-*/ccstatusline/claude-hud/hermes-hud/observability-*/telemetry/logging-*/dashboard-*/ui-ux-*/onboarding/ux-*/a11y/explain/tutorial-*/noob-mode/friction-detector）→ 对应"小白易用 + 黑匣子打开"新需求
5. **Skills 体系/用户自建**（Skills-main/agent-skills/SkillClaw/SkillX/skills-manager/skill-* 50+ 个 skill 工具/superpowers/ECC/everything-claude-code/claude-skills-main/book-to-skill/darwin-skill/EvoSkill）→ 对应"用户自建 skills 沉淀"新需求
6. **记忆/RAG/上下文**（agentmemory/mem0/memanto/mempalace/claude-mem/claude-subconscious/letta/graphiti/cognee/LightRAG/txtai/graphrag/semantica/khoj/rag-*/context-mode/context-engine/context-engineering-intro/token-optimizer/token-savior）→ 深化"让 AI 更懂用户"
7. **顶级 agent 框架/编排**（OpenHands/SWE-agent/SuperAGI/crewAI/autogen/OpenSwarm/OpenMAIC/agency-agents/agent-orchestrator×2/solo-orchestrator/all-agentic-architectures/open-multi-agent/open-swe/agent-framework-*/hermes-agent-*/langchain/langgraph/letta/microsoft-agent-framework）→ 深化编排
8. **安全渗透深化**（Pentest-Swarm-AI/strix/caveman/VulnClaw/bettercap/mcpstrike/AutoPentestX/pentest-harness/pentest-agents/Pentaract/Raccoon/Claude-BugHunter/CyberSecurity-Skills/Anthropic-Cybersecurity-Skills/AceShell/AD-PathFinder/AI2PentestTool/AutoSploit/Cairn/hexstrike-ai/hackingtool/bettercap/katana/arsenal-ng/nuclei/sqlmap/burp-*/ghidra/ida-reverse/jadx/radare2/cutter/x64dbg-mcp/captcha-*）→ 安全垂直深化
9. **工程规范/CI/工作流/长任务**（ECC/everything-claude-code/gsd-2/superpowers/LongHorizon-Harness/OpenHarness/learn-harness-engineering/meta-harness/harness-*/Continuous-Claude/loop-engineering/ralph/get-shit-done/agentic-*/n8n/zapier/inngest/temporal/bullmq/airflow/workflow-*/plan-*/planning-with-files）→ 工程化 + 长任务循环

> **状态**：9 子代理已派发（general-purpose），并行深扫中。完成后回填 §3 亮点、§4 差距、§5 迁移、§6 路线图。

### 1.4 最终选中高价值参考项目（v2 待整合）

v1 已定 Top5：agentmemory(20)/agent-orchestrator-main(19)/Pentest-Swarm-AI(17,Go 同栈)/context-mode(18)/open-multi-agent(17)。

v2 **候选新增**（待子代理报告确认分数）：
- **CL4R1T4S**（25 家系统提示词）— prompt 优化最高价值，对应"让 AI 更懂用户 + 黑匣子可观测"
- **claude-video-vision**（感知层而非解释层）— 多模态 video 感知范式
- **video-search-and-summarization**（NVIDIA VSS，VLM+RAG）— 视频理解 agent 化
- **officecli-*** / **pptx-official** / **2slides-ppt-generator** — 非软件任务 agent 化
- **superpowers** / **ECC** / **everything-claude-code** — skills/harness 工程范式
- **Pake** / **claude-hud** — 桌面壳 + 可观测 HUD

---

## 二、主项目现状总结（v2 增量）

### 2.1 v1 已记录现状（沿用，见 v1 §2）

架构分层清晰 / Eino ADK 三模式 + coordinator DAG + message_bus（coordkit 已落地）/ 安全五闸全 done / 反应式 reactions 已落地 / Cache-Aside memory|redis 可选 / Electron 外壳增强。

### 2.2 v2 新发现的"已有但未充分利用"锚点

| 能力 | 已有锚点 | 现状 | v2 缺口 |
|------|---------|------|---------|
| 反应式引擎 | `internal/reactions/engine.go` + `config.ReactionsConfig` + `app.go:787` 接入 | 已落地但默认 reactions 仅安全事件 | 未扩展到"用户行为→自动沉淀 skill"等非安全场景 |
| coordinator DAG | `internal/multiagent/coordkit/` + `coordinator_orchestrator.go` | 已落地（ParseTaskSpecs/FallbackSpecs/LoadSpecs/dispatchQueue） | 未用于非安全任务分解（如 PPT 生成步骤 DAG） |
| microagent 三层 bundle | `internal/microagent/registry.go`（global→user→repo，移植 OpenHands） | 已落地，支持 user 层覆盖 | user 层目录 `~/.cyberstrike/microagents` 是否默认接入？需验证 |
| memory 衰减雏形（**非 4 层分层**） | `internal/memory/{extractor,fusion,store_memory,temporal,exp,types}.go` | 有 extractor/fusion(三信号融合)/store_memory/temporal.go(仅 `exp(-0.02*daysOut)` 单层时间衰减) 衰减雏形；**无 working/episodic/semantic/procedural 分层架构**，非 agentmemory 4 层 | K11 需新建 4 层分层（非增量改造），详见 §4.5/§6 |
| ctxindex BM25+RRF | `internal/ctxindex/{bm25,rrf}.go` | 已有 | 是否接入会话压缩（context-mode 思想）？ |
| provenance | `internal/database/provenance.go` | 已有 | 是否在 blackboard Finding/漏洞带 source？v1 记"provenance 缺口"——需核实 |
| promptassembly | `internal/promptassembly/manager.go`（移植 OpenHands PromptManager） | 已有 workspace_context 模板 | 未对齐 CL4R1T4S 顶级 prompt 结构（身份/能力边界/工具规范/输出格式/拒绝/记忆/错误恢复/可观测） |
| vision | `internal/vision/{client,preprocess,tool,path}.go` + analyze_image MCP 工具 | 已有图像分析 | 无视频理解/生成、无多模态 agent 感知中间件 |
| 工具 yaml 108 个 | `tools/*.yaml` | 全安全工具 | **无非安全工具**（电商/office/ppt/social/多模态）——通用化扩展缺口 |
| skills 30 包 | `skills/*/` | 全安全 skill | **无非安全 skill 包**——通用化扩展缺口 |
| cost/roi/metrics | `internal/cost/{pricing,tracker}.go` + `internal/roi/` + `internal/metrics/` | 已有 | 是否前端有 token/$ 仪表盘？v1 记"无 token 仪表盘" |

### 2.3 v2 主项目主要问题（对照 5 条新需求）

| 问题 | 级别 | 对应新需求 | 锚点证据 |
|------|------|-----------|---------|
| **prompt 未对齐顶级模型结构** | P1 | ⑤ CL4R1T4S prompt 优化 | `default_single_system_prompt.go` 有高强度扫描/赏金心态/思考要求，但缺：能力边界显式声明、工具调用规范 schema、输出格式契约、拒绝策略分层、记忆上下文管理、错误恢复决策树、可观测元数据 |
| **18 agent prompt 风格不一** | P1 | ⑤ | `agents/*.md` 18 个，frontmatter 仅 id/name/description/tools/max_iterations，正文结构各异（recon.md 有"避免重复劳动/边渗透边记录"，其他 agent 可能无） |
| **无非安全工具/skill** | P1 | ① 通用化扩展 | `tools/*.yaml` 108 个全安全；`skills/*/` 30 个全安全；无电商/office/ppt/social/多模态工具 yaml |
| **多模态薄弱** | P1 | ④ 多模态扩展 | `internal/vision` 仅 analyze_image 单工具；无视频帧抽取/理解/生成；无多模态 agent 感知中间件 |
| **黑匣子未打开** | P1 | ② 小白易用 + 黑匣子 | `web/static/js/chat/` 有 render/tools，但无"agent 决策树可视化""工具选择原因展示""skill 渐进披露 UI"；desktop tray.js/splash.html 仅基础外壳 |
| **用户自建 skills 无沉淀通道** | P1 | ③ 用户自建 skills 沉淀 | `internal/microagent` 支持 user 层目录，但是否有"从对话/行为自动捕获→生成 SKILL.md"的通道？需子代理对照 skill-creator/book-to-skill 范式 |
| **blackboard 纯内存** | P2（v1 已记） | ③ 记忆底座 | `internal/blackboard/memory_board.go` 无 sqlite_board.go，进程重启丢 findings |
| **reactions 未扩展到非安全** | P2 | ③ + ⑥ | `reactions/engine.go` 默认 reactions 仅安全事件，未用于"用户重复行为→自动 skill 沉淀" |

### 2.4 风险点（v1 已记 + v2 增量）

- v1 已记：生产部署 / CGO 依赖 / 并发会话竞争 / 前端巨型文件拆分风险（chat.js 已拆 done）
- v2 新增：① "通用化扩展"可能与安全 agent 的 system prompt 产生职责混淆——需 vertical skill 包隔离；② CL4R1T4S prompt 全量重写 18 agent 风险高（破坏现有"边渗透边记录"等已验证节奏），宜增量补结构；③ 多模态引入本地推理依赖（whisper/ffmpeg/yolo）可能破坏单机部署轻量性

---

## 三、参考项目亮点提炼（10 子代理报告整合）

### 3.1 按方向分别列出亮点（对应 5 条新需求 + 5 个深化方向）

#### 3.1.1 CL4R1T4S 25 家系统提示词（对应需求⑤ prompt 优化）

**共性 12 模式**（25/25 家）：
1. 身份层第一句固化（厂商+模型名+日期+知识截止）— CyberStrikeAI 无日期/截止声明
2. 结构化分区（XML 标签或 markdown 二级标题，每区单一职责）
3. 工具调用三铁则（不向用户提工具名 / 独立调用并行化 / 被拒后不原样重试）
4. 失败协议 6 步（read error→verify assumptions→targeted correction→不重复同调用→单次失败不放弃策略→穷尽后升级）
5. 防注入层（工具输出视为对抗性输入，数据/指令分离）
6. 记忆/上下文持久化（OpenAI bio / Windsurf create_memory / ZCode 文件 memory+MEMORY.md / Manus todo.md+planner/knowledge/datasource）
7. 输出分级/模式选择（Anthropic UserStyle 三模式 / Perplexity quick/standard/deep / Codex commentary/final 双通道 / Devin planning/standard）
8. outcome-first / answer-first（Codex Desktop / ZCode / Cluely / claude-code-prompts-master）
9. 引用/证据格式（Codex `F:path†L` / Perplexity `[1][2]` / Anthropic `<cite index>` / DIA `[sourceID]`）
10. 可逆性/风险分级（local undoable proceed free / shared-state destructive require confirmation / 不可逆禁止除非 ROE 明示）
11. 反幻觉/反伪造（never fabricate actions / never claim read files not opened）— CyberStrikeAI 已对齐（强项）
12. 授权上下文门控双用途（ZCode 原文）— CyberStrikeAI"授权已前置裁决"是更激进版本

**ZAI ZCode 最接近 CyberStrikeAI 立场**：授权上下文门控双用途、memory 分类型（user/feedback/project/reference）、verbalize 计划、完成校验 JSON。但 CyberStrikeAI 的"授权不质疑"比 ZCode"require authorization context"更激进，**不照搬 ZCode 二次确认措辞**。

#### 3.1.2 多模态/视觉/视频（对应需求④）

**Top 8 高价值**：
1. **claude-video-vision**（感知层而非解释层）— ffmpeg 抽帧 + Whisper 转录，自适应 fps/分辨率/时间窗；LLM 不看视频，读帧+读转录。MCP server 形态。价值 5
2. **video-use**（两层读取 + 子代理并行渲染）— Layer1 音频转录常驻 ~12KB + Layer2 visual composite 按需 PNG；30000 帧×1500 token=45M 噪声 vs 12KB+少量图。价值 5
3. **NVIDIA VSS**（三层架构 + MCP 统一接口）— 实时视频智能→下游分析（轨迹/事件/告警富化+VLM 验证降误报）→agent 离线处理（MCP 搜索/问答/总结/片段检索）。价值 5
4. **edit-mind**（视频知识库索引）— 转录+帧分析+多模型 embedding 入向量库，自然语言搜视频场景。价值 5
5. **VideoPipe**（pipeline 节点组合）— C++ 节点式框架，流读取→解码→推理→跟踪→行为分析→OSD→编码→推送，2025/8 已支持 mLLM。价值 4
6. **VoxCPM**（tokenizer-free TTS）— 2B 参数 30 语言，RTF≈0.3，Apache-2.0 可商用。价值 3
7. **video-shotcraft**（recipe card + Remotion 编排）— 157 shot recipe cards，把运镜知识固化成可检索结构化中间表示。价值 4
8. **Open-Source-Face-Recognition-SDK**（本地人脸全栈）— 检测→landmark→embedding→比对，CPU 可跑。价值 3

#### 3.1.3 电商/办公/PPT/内容（对应需求① 通用化扩展）

**Top 10 高价值"非软件任务 agent 化"**：
1. **commerce-agents**（Anthropic 官方）— Backend 抽象（StorefrontBackend/MerchantBackend 14/18 async 方法）+ Provenance Gate（购物车只接受本次会话 catalog 工具返回过的 product_id）+ Merchant 全部写操作是 staged change + Grounding Rules（退换货→先调 search_policies）。价值 5
2. **commerce-builder plugin** — 4 命令把"造电商 agent"本身 agent 化（scaffold/add-flow/author-evals/review）。价值 5
3. **baoyu-skills**（20 skill）— 11 provider 图像 API 抽象 + EXTEND.md 分层配置（project/XDG/user）+ references/ 设计模式（方法论按需加载，SKILL.md 只写工作流）。价值 5
4. **z-skills**（18 skill）— skill 间引用链（web-pack→video-downloader→video-study）+ 每 skill 带 evals/。价值 5
5. **dashi-ppt-skill** — "产物即编辑器"+委托模式（HTML PPT 自带控制台，用户拖滑杆改；"都你来定"vs"仅内容委托"边界明确）。价值 5
6. **z-docx-format-brush** — 五层应用顺序（样式→段落→封面→表格→文字）+ verify_format.py 强制验证。价值 5
7. **open-design**（164 skill + 114 template + 70 design-systems + craft）— 四轴分层（skills/design-templates/design-systems/craft）+ od.craft.requires 按需注入 + lint-artifact 自动拦截 AI-slop。价值 5
8. **OJO-Design-Skills** — Style Register 五维度推导（Energy/Finish/Density/Weight/Seriousness 每值 cite 证据）+ 模糊词防火墙（premium/refined/elegant 不能作为理由）。价值 4
9. **z-grounded-source-qa** — 证据四级（Direct/Synthesis/Inference/Unsupported）+ 强制 3-8 个检索表达式改写。价值 4
10. **genoffice** — 开源 AI Office 套件（Word/Excel/PPT/PDF 共享引擎层）+ AI 面板 block 级编辑。价值 4

#### 3.1.4 桌面壳/UX/可观测/小白易用（对应需求② 黑匣子打开）

**Top 10 高价值**：
1. **claude-hud** — 状态栏即黑匣子摘要（Context 条绿→黄→红 + 工具活动行 + agent 状态行 + todo 进度），300ms 更新。价值 5
2. **clawdeck** — trace waterfall（turn→tool-call span 时间线）+ decision ledger（每个派生态带 `Why?` 原文）+ config map 灰显死配置 + READY/BLOCKED/UNKNOWN 三态。价值 5
3. **OpenMontage** — Backlot 活动故事板（chat 说啥 vs 生产在干啥）+ director skill + reviewer CHAI 规则 + onboarding 元技能分层（Zero-key→Full）。价值 5
4. **ccstatusline** — widget 化状态栏（model/git/token/context/skills/thinking/vim/speed/subagent）+ OSC8 可点击链接。价值 4
5. **hermes-hud** — 13 tab 意识监控（identity/memory/skills/sessions/cron/projects/health/costs/patterns/corrections/chat）+ snapshot diff。价值 4
6. **ui-ux-pro-max** — CSV 驱动决策规则库（ui-reasoning.csv/ux-guidelines.csv/motion.csv，每行 Do/Don't+Severity）。价值 4
7. **pi-agent-desktop** — 有界崩溃恢复（RELOAD_LIMIT=3/60s）+ CSP header builder + env-filter key 脱敏。价值 4
8. **OpenBot** — watch + take the wheel（人类驾驶期间 agent 动作被拒绝而非排队）+ secrets 不入 transcript + components not prose。价值 4
9. **Hermes-CN-Desktop** — supervisor 5s 轮询+crash-loop 上限 5 次+dashboard_restart_in_flight 互斥+session_log 防路径穿越。价值 3
10. **holaOS** — diagnostics-bundle 脱敏导出（10 个 SENSITIVE_KEY_PATTERNS 正则→REDACTED_VALUE）。价值 3

#### 3.1.5 skills 体系/用户自建（对应需求③ 用户自建 skills 沉淀）

**Top 8 高价值 skill 框架**：
1. **ECC continuous-learning-v2**（instinct 中间层）— PostToolUse hook 100% 观察→Haiku 抽取"instinct"（action+evidence+confidence）→项目隔离→`/evolve` 聚类成 skill→2+ 项目复现才晋升全局。价值 5
2. **ECC skill-evolution 健康度栈** — tracker.jsonl（5000 条）+ provenance（curated/learned/imported）+ versioning（.evolution/ amendments）+ dashboard 四面板。价值 5
3. **SkillClaw 全自动演化闭环** — session→summarizer→session_judge（加权打分）→aggregation→execution（improve/optimize/create/skip）→skill_verifier 发布闸（评分 agent 与改写 agent 分离）。价值 5
4. **darwin-skill-master** — 8 维 rubric（结构 60 + 实测 40，子 agent 带 skill vs 不带 baseline 对比）+ hill-climbing + git revert 棘轮 + results.tsv。价值 5
5. **agent-skills 三层 evals** — Tier1 结构 + Tier2 路由（TF-IDF 碰撞，改 description 而非改 eval）+ Tier3 行为（headless claude -p + grader）。价值 5
6. **ECC `/skill-create` + `/learn-eval`** — git 历史挖掘模式自动生成 SKILL.md + guarded-write（脱敏 PII/拒 prompt-injection）。价值 5
7. **SkillX 三级层次 + 检索** — Planning/Functional/Atomic + DBSCAN 聚类 + LLM merger + embedding retriever。价值 4
8. **skillroster Evidence/Plan/Receipt/Undo 治理** — 确定性 CLI + Core/On-demand/Explicit-only/Archived 四级 roster。价值 4

#### 3.1.6 记忆/RAG/上下文（对应需求③ 让 AI 更懂用户）

**Top 6 高价值**：
1. **agentmemory-main**（1:1 迁移蓝本）— 4 层（working→episodic→semantic→procedural）+ Ebbinghaus 衰减（exp(-λΔt)+访问增强，hot/warm/cold 阈值）+ 三流检索（BM25+vector+graph RRF_K=60）+ provenance（sourceObservationIds→session）+ 级联过期（supersede 传播到图邻居）+ token 高效（240 观测 19462→1571，92% 降）。价值 5
2. **context-mode**（token 效率蓝本）— SQLite+FTS5 双索引（porter+trigram）+ think in code（ctx_execute 跑脚本只返 verdict，96-98% 降）+ head60%+tail40% 截断。价值 5
3. **mem0**（主项目已镜像）— ADD-only 抽取 + 三流融合（semantic+BM25+entity，查询长度自适应 sigmoid）+ 时态推理（dated instance）+ FactInstance 版本链。价值 4
4. **LightRAG**（主项目已对齐 hybrid）— 5 模式（local/global/hybrid/mix/naive）+ entity VDB+relation VDB+graph + chunk_documents_for_rerank（480 token 分块+32 overlap）。价值 4
5. **SkillClaw**（skills 演化底座）— 集体 skill 演化，跨 session/agent/device 复合增长。价值 4
6. **openwiki Grounded Claims** — 每条 material fact 携带 versioned source evidence，证据变更时知道哪些命题需确认/重写/退役。价值 4

#### 3.1.7 顶级 agent 框架/编排（对应编排深化）

**Top 8 高价值**：
1. **agent-orchestrator-main**（ComposioHQ，主项目 K2 反应式引擎母本）— 8 PluginSlot + Lifecycle Manager 状态机轮询 + 递归 Task Decomposer（maxDepth=3）+ ReactionConfig（auto/notify/send-to-agent/auto-merge + retries/escalateAfter/threshold）+ ReactionTracker sendOnce dedup。3288 测试。价值 5
2. **open-multi-agent-main**（主项目 Coordinator 已移植母本）— 27 源文件/3 依赖，runTeam() coordinator 自动分解→DAG→并发→synthesize + Scheduler 四策略（round-robin/least-busy/capability-match/dependency-first）+ computeRetryDelay + Zod structured output + onTrace callback。价值 5
3. **OpenSwarm-main** — Worker→Reviewer→Tester→Documenter→Auditor pair pipeline + DAG workflow（onFailure rollback/retry/skip/abort/notify + cron trigger）+ conflictDetector Union-Find + StuckDetector 四阈值 + EventHub 500 事件 ring buffer。价值 5
4. **OpenHands-main** — StuckDetector（sameOutputRepeat=3/sameErrorRepeat=2/revisionLoop=4/monologue=6）+ EventStore 持久化+重放 + 多 runtime（docker/k8s/local/remote）。价值 4
5. **crewAI-main** — Flow 装饰器（@start/@listen/@router）+ SQLiteFlowPersistence + async HITL resume（SlackProvider）+ a2a 协议。价值 4
6. **mcp-agent-main** — DeepOrchestrator（planner→PlanVerifier→TodoQueue dedup→TaskExecutor→PolicyEngine max_consecutive_failures=3/min_verification_confidence=0.8/replan_on_empty_queue/budget_critical_threshold=0.9）+ Evaluator-Optimizer（QualityRating POOR/FAIR/GOOD/EXCELLENT）+ Temporal Executor。价值 5
7. **agent-framework-go**（Microsoft MAF Go，同 Go 生态）— GroupChat SelectNextAgent + Concurrent/Sequential workflow + JSON checkpoint（sessionCheckpointEntry 链式 parent）+ toolapproval 常驻"don't ask again"规则 + CompletionMarkerEvaluator。价值 5
8. **autogen-main** — MagenticOneOrchestrator Progress Ledger（pre-survey 四类事实 GIVEN/LOOK UP/DERIVE/EDUCATED GUESSES + 每轮 progress ledger 更新）+ DiGraph 条件边 + SwarmGroupChat HandoffMessage 主动交接棒 + 组合 termination（All/Any）。价值 4

#### 3.1.8 安全渗透垂直深化（对应安全侧补强）

**Top 8 高价值深化点**：
1. **Pentest-Swarm-AI**（Go 同栈）— blackboard 信息素衰减（TARGET 24h/PORT 1h/SESSION 短命）+ Trigger 谓词订阅 + idleTimeout 静默自动收尾 + cross_validate 多工具互证 + bugbounty dedup（CVE+title 0.85）。价值 5
2. **pentestkit**（XBOW 104/104 满分核心）— Playbook PROVE/RULE_OUT 表（SSRF 必须 OAST 回调或 unroutable 对照 / XSS 必须 alert/DOM marker / SQLi 必须数据/布尔/时间/OAST）+ verifier 4-axis（real/triggerable/impactful/general）+ stability 二次复跑 + evidence_level 四级（suspected<corroborated<reproduced<impact_proven）。价值 5
3. **strix** — SARIF 生产级（CWE 锚定+security-severity 0-10+fix_before/after+logicalLocations DAST 端点+versionControlProvenance 绑定 commit/branch+synthetic_location 兜底）+ Coverage Ledger 双源（agent-reported vs machine-observed，矛盾时后者覆盖）+ Threat Model amend-addendum（run-scoped，agent 追加而非覆写，收尾折叠）。价值 5
4. **pentest-agents** — chain-builder 递归链走（A→map 能力→search capability→next-bug 表→测试 B→B 确认则变新 A，3 候选失败即止损）+ 每步强制 search_writeups/search_techniques + correlator 固定链模板（open redirect+OAuth=token theft / SSRF+cloud metadata=IAM 盗）+ 7-Question Gate validator + MCP bounty-server 直投 HackerOne/Bugcrowd。价值 5
5. **hackingtool** — Operator Charter 四要素（Persona authorized-only + Safety no destructive + Injection `<scan_data>` 数据非指令 + Anti-fabrication closed-tag TAXONOMY）+ curated-first difflib（AI 填空首 token 必须在 curated USAGE 集合）。价值 4
6. **autopentest-go**（Go 同栈）— Evidence 即时捕获（工具结果 observed 瞬间原子持久化）+ isLiveToolEvidence（bash/shell 排除）+ evidence_grounding_test（无 grounded findings 不调 evidence agent）+ GoalManager 4 模式 + Attempt 计数渐进宽松。价值 5
7. **caveman-main** — evals 3-arm（baseline/terse/skill，关键洞察：真正 delta 是 skill vs terse 不是 vs baseline）+ Auto-Clarity（安全警告/不可逆操作/多步序列时自动退出压缩）。价值 4
8. **pentagi**（Go 同栈）— ErrFlowStateGuard 跳过参数重试 + execution monitoring mentor（5 同工具/10 总阈值）+ intelligent task planning（3-7 步）+ Graphiti 7 种搜索 + anonymizer.Replacer PII + 多租户。价值 5

#### 3.1.9 工程规范/CI/长任务工作流（对应工程化深化）

**Top 8 高价值**：
1. **agent-orchestrator** — CI 矩阵 11 workflow（gofmt gate + go-version-file + golangci v2.12.2 zero-findings blocking + api-drift check + 3-OS native e2e）+ lifecycle.Manager 同步 reducer（owner-generation-fenced write）+ reactions sendOnce dedup。价值 5
2. **loop-engineering** — LOOP.md/STATE.md/gate.yaml 三件套（denylist+autoMergeAllowlist 机器可读双子）+ loop-gate/loop-audit（0-100 评分）/loop-cost/loop-budget/loop-run-log + 5 primitives + multi-loop 协调 + failure-modes（S1/S2/S3）+ anti-patterns（6 反模式：same agent implements and verifies / no attempt cap 等）。价值 5
3. **gsd-2** — PR 风险分级器（FILE-SYSTEM-MAP.md→critical/high/medium/low tier，pull_request_target + base checkout + API file list）+ AI triage + 5-wave state machine + TOCTOU file locking。价值 4
4. **ralph** — 最简长任务循环（MAX_ITERATIONS + fresh context per iter + progress.txt append-only + Codebase Patterns 顶部 + branch archive + `<promise>COMPLETE</promise>` sentinel）。价值 4
5. **LongHorizon-Harness** — plan→act→verify→checkpoint/recover→repeat + 三角色（manager/executor/auditor）+ MAX_ROUNDS=1000/DEFAULT=25 + round_budget + 数据驱动 human gate `_TRIGGERS`（completed/max_rounds/needs_human/repeated_failure + allow_extra_rounds）+ O_NOFOLLOW 符号链接加固 + 有界 `_MAX_*_BYTES` + 流式 trajectory writer。价值 5
6. **Continuous-Claude-v3** — TLDR 5 层代码分析（AST/CallGraph/CFG/DFG/PDG，23314→1189 token，95% 节省）+ handoffs YAML per-workflow + Continuity Ledger + "Compound, don't compact"。价值 4
7. **ECC** — hooks 架构（pre:bash:dispatcher/config-protection/governance-capture/mcp-health-check/suggest-compact/continuous-learning）+ RULES.md must/must-never。价值 4
8. **superpowers** — verification-before-completion Iron Law（NO COMPLETION CLAIMS WITHOUT FRESH VERIFICATION EVIDENCE）+ test-driven-development（NO PRODUCTION CODE WITHOUT A FAILING TEST FIRST）+ 1% 规则。价值 3

### 3.2 共性优点（跨 484 项目提炼）

1. **单一事实源**：config 在 repo，runtime data 在统一 home（agent-orchestrator/context-mode/learn-harness）
2. **事件驱动 + pub/sub**：EventStream（OpenHands）/ message bus（open-multi-agent）/ FTS5 索引（context-mode）/ Flow 装饰器（crewAI）
3. **结构化输出 + auto-retry**：Zod（open-multi-agent）/ Pydantic（mem0）/ typed tool result / SchemaField（coordkit 已有）
4. **provenance/citation**：agentmemory/mem0/openwiki/commerce-agents Provenance Gate 都给每条记忆/claim 带来源
5. **可插拔后端**：LightRAG 图存储 / agent-orchestrator-main 8 PluginSlot / commerce-agents Backend 抽象 / baoyu provider 抽象
6. **质量门**：3288 测试 + mutation + coverage + security workflow（agent-orchestrator-main）/ loop-audit 0-100（loop-engineering）/ darwin 8 维 rubric
7. **反应式自动化**：reactions.yaml（agent-orchestrator-main）/ Flow @router（crewAI）/ DAG onFailure（OpenSwarm）/ cron trigger
8. **分层渐进披露**：SKILL.md skill 中间件（agentskills.io）/ references/ 方法论按需加载（baoyu）/ craft.requires 注入（open-design）
9. **可观测/可回放**：trace waterfall（clawdeck）/ decision ledger/`Why?`/EventStore 持久化重放/onTrace span
10. **委托模式/边界明确**：dashi"都你来定 vs 仅内容委托"/ OpenBot take the wheel / ReAct 四步循环

### 3.3 对主项目最有价值的亮点（Top 15 排序）

| 排名 | 亮点 | 来源 | 迁移性 | 主项目落点 |
|------|------|------|--------|-----------|
| 1 | instinct 中间层 + `/learn` + `/evolve` | ECC v2.1 | 高 | 用户自建 skills 沉淀闭环（需求③核心） |
| 2 | 4 层记忆 + Ebbinghaus 衰减 + 级联过期 + provenance | agentmemory | 高 | blackboard SQLite + memory 分层（需求③） |
| 3 | SQLite+FTS5 双索引 + think in code | context-mode | 高 | ctxindex + ctx_execute 工具（token 效率） |
| 4 | trace waterfall + decision ledger `Why?` + 托盘动态状态 | clawdeck/claude-hud/OpenMontage | 高 | 黑匣子打开（需求②核心） |
| 5 | CL4R1T4S prompt 12 模式（身份层/防注入/并行工具/outcome-first/工具前一句话理由） | 25 家 | 高 | default_single_system_prompt + 18 agent（需求⑤） |
| 6 | Backend 抽象 + Provenance Gate + staged change | commerce-agents | 高 | 通用化扩展 vertical 抽象（需求①） |
| 7 | open-design 四轴分层（skills/design-templates/design-systems/craft） | open-design | 高 | skills/ 目录重组（需求①①） |
| 8 | claude-video-vision 感知层而非解释层 + video-use 两层读取 | 两项目 | 高 | internal/vision 扩 internal/perception+videomem（需求④） |
| 9 | verifier 4-axis + evidence ladder + Playbook PROVE/RULE_OUT | pentestkit | 高 | penetration/impact-exfiltration 验证闸（安全） |
| 10 | SARIF 生产级 + Coverage Ledger 双源 + Threat Model amend | strix | 高 | sarif/attackchain 深化（安全） |
| 11 | StuckDetector + Scheduler 四策略 + retry/backoff | OpenHands/open-multi-agent | 高 | coordkit 策略层补强（编排） |
| 12 | PR 风险分级器 + gofmt gate + golangci v2 + loop-audit | gsd-2/agent-orchestrator/loop-engineering | 高 | CI 矩阵升级（工程化） |
| 13 | ralph-style autonomous loop + LongHorizon human gate `_TRIGGERS` | ralph/LongHorizon | 高 | 长任务循环 driver（工程化） |
| 14 | diagnostics-bundle 脱敏导出 + bounded auto-restart + onboarding 分层 | holaOS/pi-agent-desktop/OpenMontage | 高 | 桌面壳 + 小白易用（需求②） |
| 15 | dashi 产物即编辑器 + baoyu provider 抽象 + z-grounded-source-qa 证据四级 | dashi/baoyu/z-skills | 高 | 非安全 vertical skill 包（需求①） |

---

## 四、差距分析（v2 整合，对应 5 条新需求 + 5 个深化方向）

### 4.1 prompt 层差距（对应需求⑤）

| 差距 | 主项目现状 | 参考项目实践 | 可迁移性 |
|------|-----------|-------------|---------|
| 无身份层日期截止声明 | `default_single_system_prompt.go:8` 无日期/截止 | 25/25 家都有 | 高（P0 纯文本追加） |
| 无防注入层 | 工具输出未明示对抗性 | Brave Leo/claude-code-prompts-master/ZCode | 高（P0） |
| 无并行工具调用指导 | 单代理 prompt 未明示并行 | Codex/Grok/ZCode | 高（P0） |
| 非 outcome-first | 先思考后结论 | Codex Desktop/ZCode/Cluely | 高（P0，翻转推理块位置） |
| 工具前一句话理由弱 | 有 50-200 字思考但非 WHY/EXPECT/LINK 三段 | Cursor/Manus/Windsurf | 高（P0） |
| 无模式选择 | 单一"高强度扫描" | Perplexity/deep-research/Anthropic UserStyle | 中（P1，防偷懒） |
| 18 agent 大量重复 | 共享段未抽取 | ECC shared.md | 中（P1） |
| 无引用格式规范 | 发现输出有 Target/Indicator 但无工具输出锚点 | Codex F:path†L/Perplexity [1][2] | 中（P1） |
| 失败协议泛 | 现有 6 条较泛 | claude-code-prompts-master 6 步结构化 | 中（P1） |
| 无可逆性风险分级 | "非破坏性"太粗 | claude-code-prompts-master/Bolt/Replit | 中（P1） |

### 4.2 通用化扩展差距（对应需求①）

| 差距 | 现状 | 参考 | 可迁移性 |
|------|------|------|--------|
| 108 工具全安全 | `tools/*.yaml` 无非安全工具 | dashi/baoyu/z-docx-format-brush | 高（P0 加 yaml） |
| 30 skill 全安全 | `skills/` 无非安全 skill | open-design 四轴/baoyu 20 skill | 高（P0 加包） |
| 18 agent 全安全 | `agents/*.md` 无通用 agent | commerce-agents 角色 | 高（P1 加 agents/general/） |
| 无 vertical 抽象 | 无 Vertical interface | commerce-agents Backend/open-design 四轴 | 高（P0 奠基，主项目深审子代理建议） |
| scope_json 无 operation_domain | 仅 targets/exclude IP/域名（`security/scope_block.go:31` 核实无此字段） | commerce-agents Provenance Gate | 中（P1 新增字段） |
| skillpackage lock/verbs_gate 非递归 | `lock.go:54`/`verbs_gate.go:53` 用 ReadDir 只扫顶层 | — | 中（P0 改 WalkDir 才能支持子目录包，io.go 已递归） |
| 无 brand-systems | 无品牌契约 | open-design design-systems/ | 低（P2） |

### 4.3 多模态差距（对应需求④）

| 差距 | 现状 | 参考 | 可迁移性 |
|------|------|------|--------|
| 仅 analyze_image 单图工具 | `internal/vision/tool.go:45` | claude-video-vision/video-use | 高（P0 watch_video/transcribe_audio） |
| 无视频理解/生成 | 无 | VSS 三层/edit-mind 索引 | 高（P1 internal/perception+videomem） |
| 无音频/TTS | 无 | VoxCPM/whisper-flow | 中（P1 internal/voice） |
| 无多模态感知中间件 | 无 | VideoPipe 节点图/VSS 三层 | 中（P1） |
| 无视频知识库索引 | 无 | edit-mind ChromaDB | 中（P1 落 SQLite+FTS5） |
| config 无 multimodal 段 | vision 段仅 image_in | — | 高（P0 扩 config） |
| 无生成类工具 yaml | tools 108 全安全 | image_gen/video_gen/speech | 高（P0 加 yaml，付费 API Mock 红线） |

### 4.4 黑匣子/小白易用差距（对应需求②）

| 差距 | 现状 | 参考 | 可迁移性 |
|------|------|------|--------|
| 无 trace waterfall | chat-plan-progress 仅任务粒度 | clawdeck/claude-hud | 高（P0 复用 monitor.js polling） |
| HITL 无 Why 区 | hitl.js 有 payload 无 reasoning | clawdeck decision ledger | 高（P0 消费 buildReasoningRequestPayload） |
| 托盘静态 | tray.js 91 行无动态色 | claude-hud 状态色 | 高（P0 改 tray.js） |
| 无 onboarding | config.html 填完直接进主界面 | OpenMontage 分层 | 高（P1） |
| 无空状态引导 | dashboard 空白 | Learnova Empty 文案 | 高（P1） |
| splash 无阶段百分比 | indeterminate 动画 | hermes-hud neofetch | 高（P1） |
| 无 bounded auto-restart | main.js 30s 窗口弹框 | pi-agent-desktop RELOAD_LIMIT=3 | 中（P1） |
| 无诊断包导出 | logger.js 无脱敏 | holaOS diagnostics-bundle | 中（P2） |
| 无学习模式 | 无 | OpenBot components not prose/Learnova Explain | 低（P2） |

### 4.5 用户自建 skills 沉淀差距（对应需求③）

| 差距 | 现状 | 参考 | 可迁移性 |
|------|------|------|--------|
| 无 instinct 中间层 | microagent 有 user 层但无自动捕获 | ECC continuous-learning-v2 | 高（P0 PostToolUse hook） |
| 无 `/learn` `/evolve` | 无 | ECC `/learn`+`/skill-create` | 高（P0） |
| skill_stats 无演化趋势 | 后端 LoadSkillStats 有（`handler/skills.go:500`）前端未展示 | ECC dashboard 四面板 | 高（P1 前端展示） |
| 无 provenance（curated/learned/imported） | skills-lock.json 仅 SHA256 | ECC provenance.js | 中（P1） |
| 无 versioning amendments | 无 | ECC versioning.js | 中（P1） |
| 无 darwin 8 维评估 | evals Tier1-2 有 | darwin-skill-master | 中（P1 效果维度） |
| 无 skill 市场 | 无 | skills-manager/skillroster | 低（P2） |
| blackboard 纯内存 | `memory_board.go` 无 sqlite_board | agentmemory SQLite | 高（P0 接口已留缝） |
| memory 无 4 层衰减 | temporal.go 仅 event_date 衰减 | agentmemory Ebbinghaus | 中（P1） |
| 无级联过期 | Supersede 只翻 ReplacedBy | agentmemory cascade | 中（P1） |
| 无 provenance 引用链 | FactInstance 无 SourceObservationIDs | agentmemory verify.ts | 中（P1） |

### 4.6 编排差距（对应编排深化）

| 差距 | 现状 | 参考 | 可迁移性 |
|------|------|------|--------|
| Coordinator 无 Scheduler 四策略 | dispatchQueue 简单循环 | open-multi-agent 四策略 | 高（P0） |
| 无 retry/backoff | Task 有 MaxRetries 字段未消费 | open-multi-agent computeRetryDelay | 高（P0） |
| 无 StuckDetector | TurnLimiter 单一防线 | OpenHands/OpenSwarm 四阈值 | 高（P0 二级防线） |
| reactions 无 lifecycle 状态机 | 仅 Finding-driven | agent-orchestrator 状态转换轮询 | 高（P0） |
| 无 PolicyEngine | 无 | mcp-agent PolicyEngine | 中（P1） |
| 无 PlanVerifier | 无 | mcp-agent/deep-research | 中（P1） |
| 无 recursive decomposer | 无 | agent-orchestrator maxDepth=3 | 中（P1） |
| 无 pair pipeline | swarm 无 Worker→Reviewer | OpenSwarm 六阶段 | 中（P1） |
| 无 conflictDetector | 无 | OpenSwarm Union-Find | 中（P1） |
| 无 Progress Ledger | 无 | autogen magentic-one | 低（P2） |
| HITL 无 don't-ask-again | 无常驻规则 | agent-framework-go toolapproval | 低（P2） |

### 4.7 安全垂直差距（对应安全深化）

| 差距 | 现状 | 参考 | 可迁移性 |
|------|------|------|--------|
| 无 verifier 4-axis | penetration 无验证闸 | pentestkit 4-axis+evidence ladder | 高（P0） |
| SARIF 非生产级 | 无 logicalLocations/versionControlProvenance | strix 完整实现 | 高（P0） |
| 无 Operator Charter | 18 agent prompt 无统一契约 | hackingtool 四要素 | 高（P0） |
| 无 threat_model amend | attackchain 缺共享威胁模型 | strix run-scoped | 中（P1） |
| 无 coverage ledger 双源 | 无 | strix 双源 | 中（P1） |
| 无 chain-builder | lateral/impact 缺链发现引擎 | pentest-agents 递归链走 | 中（P1） |
| 无 OAST 回调 | 无 | pentestkit OAST | 中（P1） |
| swarm 非反应式 | 线性编排 | Pentest-Swarm-AI 信息素 | 低（P2） |

### 4.8 工程化差距（对应工程化深化）

| 差距 | 现状 | 参考 | 可迁移性 |
|------|------|------|--------|
| golangci v1 + 无 gofmt gate | v1 schema 5 linter | agent-orchestrator v2.12.2 | 高（P0） |
| 无 go-version-file | 硬编 1.25 | agent-orchestrator | 高（P0） |
| 无 PR 风险分级 | 无 | gsd-2 FILE-SYSTEM-MAP | 高（P0） |
| 无长任务 round loop | maxIterations=30 单会话内 | ralph/LongHorizon | 高（P1） |
| 无 human gate _TRIGGERS | hitl_wait 有 wait 无 repeated_failure | LongHorizon | 中（P1） |
| 无 budget kill switch | cost.Tracker 无预算执行 | loop-engineering loop-budget | 中（P1） |
| 无 loop-audit 评分 | CI 有 coverage 无 holistic | loop-engineering 0-100 | 低（P2） |
| reactions 无 auto-action | 仅 notify/log-only | agent-orchestrator auto-merge/create-incident | 中（P2） |

---

## 五、可迁移 / 半可迁移 / 不建议迁移（v2 整合）

### 5.1 可迁移（高收益、低成本、兼容现有）

| 项 | 来源 | 预期收益 | 实施成本 | 风险 | 适配思路 |
|----|------|---------|---------|------|---------|
| instinct 中间层 + `/learn` `/evolve` | ECC v2.1 | 用户自建 skills 沉淀闭环 | 中 | 低 | PostToolUse hook→instincts jsonl→聚类晋升，guarded-write 脱敏 |
| blackboard SQLite 持久化 | agentmemory | 进程重启不丢 findings | 低（接口已留缝） | 低 | sqlite_board.go，Board interface 不变 |
| 4 层记忆 + Ebbinghaus 衰减 | agentmemory | 记忆只增不减→有衰减 | 中 | 低 | FactInstance 加 Strength/AccessCount/LastAccessedAt，retention.go |
| 级联过期 | agentmemory | supersede 传播到图 | 中 | 低 | cascade_stale.go 遍历图邻居标 stale |
| provenance 引用链 | agentmemory | 审计可追溯 | 中 | 低 | FactInstance 加 SourceObservationIDs |
| trace waterfall + HITL Why | clawdeck/claude-hud | 黑匣子打开 | 中 | 低（L2） | chat/render.js + hitl.js 增量，复用 monitor.js polling |
| 托盘动态状态 | claude-hud | 小白持续可见锚点 | 低 | 低 | tray.js 91 行改，轮询 tasks/hitl |
| CL4R1T4S prompt P0 五项 | 25 家 | prompt 质量跃升 | 低（纯文本） | 低 | default_single_system_prompt 增量，不动授权立场 |
| vertical 抽象 | commerce-agents/open-design | 通用化扩展容器 | 中 | 中 | internal/vertical/ + security 首实现 |
| 非安全工具/skill yaml | dashi/baoyu | 通用化 | 低（加 yaml） | 低 | tools/dashi-ppt.yaml + skills/office/ |
| watch_video/transcribe_audio | claude-video-vision | 多模态 P0 | 中 | 低（外部二进制） | internal/vision/video.go+audio.go |
| verifier 4-axis + evidence ladder | pentestkit | 消除"理论可行"包装"已验证" | 中 | 低 | penetration/impact-exfiltration 输出过闸 |
| SARIF 生产级 | strix | GitHub code-scanning 不丢锚点 | 中 | 低 | sarif.go 补 logicalLocations/versionControlProvenance |
| Operator Charter | hackingtool | 18 agent 统一安全契约 | 低 | 低 | shared.md 共享段 |
| StuckDetector | OpenHands/OpenSwarm | 防退化二级防线 | 中 | 低 | eino_stuck_detector.go，配合 TurnLimiter |
| Scheduler 四策略 + retry/backoff | open-multi-agent | Coordinator 策略层 | 中 | 低 | coordkit/coordinator_specs.go 增强 |
| reactions lifecycle 状态机 | agent-orchestrator | 反应式自动化 | 中 | 中 | engine.go 补状态转换轮询 |
| golangci v2 + gofmt gate | agent-orchestrator | CI 矩阵升级 | 低（1h） | 低 | .golangci.v2.yml + ci.yml |
| PR 风险分级器 | gsd-2 | PR 自动 risk 评论 | 中 | 低 | scripts/pr-risk-check + FILE-SYSTEM-MAP.md |
| ralph autonomous loop | ralph/LongHorizon | 长任务循环 | 中 | 中 | internal/autonomous/ loop.go+progress.go |
| SQLite+FTS5 双索引 + ctx_execute | context-mode | token 96-98% 降 | 中 | 低 | ctxindex 迁 FTS5 + 新 MCP 工具 |
| bounded auto-restart | pi-agent-desktop | 桌面壳稳定 | 低 | 低 | main.js 改 exit 处理 |
| onboarding 分层 + 空状态引导 | OpenMontage/Learnova | 小白易用 | 中 | 低 | config.js + dashboard.js |

### 5.2 半可迁移（需适配或局部借鉴）

| 项 | 原因 | 预期收益 | 实施成本 | 风险 | 适配思路 |
|----|------|---------|---------|------|---------|
| 8 PluginSlot 全复刻 | Eino graph 非可插，全改破坏大 | 解耦 | 高 | 高 | 只抽 Tool/Memory/Notifier 三槽为 interface（已有 pluginslot 6 槽） |
| recursive decomposer | maxDepth=3 递归需 LLM 分类 | 任务分解 | 中 | 中 | coordinator_decomposer.go，requireApproval 走 HITL |
| pair pipeline | swarm 适配渗透场景 Executor→Critic→Validator→Reporter | 质量保证 | 中 | 中 | swarm/pair_pipeline.go，复用 worktree |
| conflictDetector Union-Find | 需 codegraph 依赖 | 并行安全 | 中 | 中 | swarm/conflict_detector.go |
| PolicyEngine | replan/stop/continue 决策 | 策略层 | 中 | 中 | coordinator_policy.go |
| chat.js 重写 | 已拆分 done，不宜再动 | — | — | — | 增量扩展 chat/render.js |
| scope_json operation_domain | 需新增字段+改 scope_block.go | 通用化硬闸 | 中 | 中 | 新增字段，向后兼容默认不限制 |
| skillpackage lock/verbs_gate 递归 | io.go 已递归但 lock/verbs_gate 非递归 | 子目录包 | 低 | 低 | lock.go/verbs_gate.go 改 WalkDir |
| evals Tier3 真实 LLM | 付费红线 | 路由质量 | 高 | 中 | 用户给预算+glm 本地代理 |
| 诊断包脱敏导出 | 需脱敏正则全覆盖 | 报障便利 | 中 | 低（安全红线） | holaOS 10 patterns + ai-config isKeyUnconfigured |
| Progress Ledger | 需改 Coordinator 系统提示 | plan/replan 上下文 | 中 | 中 | magentic-one 四类事实 |
| toolapproval don't-ask-again | 需改 hitl_middleware | HITL 效率 | 中 | 中 | Rule{Tool,Arguments} 集合 |
| DashScope reranker 已有 | 已对齐 | — | — | — | — |

### 5.3 不建议迁移

| 项 | 原因 |
|----|------|
| 整体推翻重写 | 主项目生产部署，工程化已成熟（1842 测试全绿） |
| 引入 Neo4j/Postgres 替换 SQLite | 破坏单机部署 + CGO 兼容（主项目 SQLite 双驱动已稳） |
| 引入 MQ（Kafka/RabbitMQ） | 单机桌面场景过度（进程内 blackboard pub/sub 够用） |
| 全盘 TypeScript 重写前端 | 原生 JS + i18n 已稳（chat.js 已拆分 done），重写风险大 |
| VideoPipe C++ 框架 | 用 Eino 中间件链替代，不引 C++ 依赖 |
| ChromaDB 视频向量库 | 用主项目 SQLite+FTS5 替代 |
| 实验性 P2P mesh | 单机足够，分布式非当前阶段 |
| Magentic-One 完整复刻 | Eino ADK + coordkit 已有等价编排 |
| ralph `--dangerously-skip-permissions` | 不可照搬此 flag（安全红线，主项目 fail-closed 立场） |
| 直接移植 dashi AGPL-3.0 引擎 | copyleft 强，需评估许可证或自研等价 |
| 真实付费多模态 API 调用 | 付费红线，默认预算 0，Mock 验证参数拼装/响应解析 |

---

## 六、优先级优化路线图（v2 整合，对应 5 条新需求）

### P0（必须先做，影响稳定性/安全性/核心体验/产品方向奠基）

> **Critic R1 修订**：P0 从原 16 项收敛为 3 项硬奠基（vertical 抽象 + blackboard SQLite + skillpackage 递归），原 13 项降 P1 按价值排序逐批做。理由：16 项 P0 并行需 16 个独立可回滚变更单元，超主项目并发会话协调能力（MEMORY 记 20 并发上限）且 P0 失焦。

| 项 | 做什么 | 为什么 | 改哪些模块 | 预期收益 | 风险 | 数据迁移 | 兼容性 | 对应需求 |
|----|--------|--------|-----------|---------|------|---------|--------|---------|
| **vertical 抽象（K0 奠基）** | `internal/vertical/` interface + security 首实现 + config `active_vertical: security` + `verticals:[]`；verbs_gate.go realTools 基线按 vertical 过滤 | 通用化扩展容器，后续所有 vertical 项依赖此 | internal/vertical/(新)、internal/skillpackage/verbs_gate.go、config | 架构奠基 | 中（L2，verbs_gate 加载逻辑改 WalkDir+vertical 过滤） | 无 | 向后兼容（默认 security，18 agent 全可见性回归测试必加） | ① |
| **blackboard SQLite 持久化（K0 奠基）** | sqlite_board.go，Board interface 不变；journal_mode=WAL；与 knowledge 共 `*sql.DB` 句柄 | 进程重启不丢 findings；memory 4 层（K11）与 blackboard SQLite **包级独立**（memory 改 internal/memory/types.go，blackboard 改 internal/blackboard/，仅共享 SQLite 句柄，可并行） | internal/blackboard/sqlite_board.go(新) | 数据不丢 | 低 | 自动（内存→db） | Board interface 不变，WAL 防并发写锁 | ③ |
| **skillpackage lock/verbs_gate 递归（K0 奠基）** | lock.go:54/verbs_gate.go:53 改 `os.ReadDir`→`filepath.WalkDir` 支持子目录垂直包 | vertical 抽象后 skills/security/、skills/office/ 等子目录包需递归扫描 | internal/skillpackage/lock.go、verbs_gate.go | 子目录包 | 低 | 无 | 向后兼容（io.go 已 WalkDir） | ① |

> **K0 三项是硬奠基**：vertical 抽象是 K3/K6/K8 的容器；blackboard SQLite 是 K11（memory 4 层）的 SQLite 句柄共享方（非代码依赖）；skillpackage 递归是 vertical 子目录包扫描前提。
>
> **vertical 抽象的 prompt 拆分移 K2.1 串行子节点**（L3 风险单独评审）：`default_single_system_prompt.go` 当前是单函数硬编字符串（line 8-119），18 agent 通过 frontmatter 加载，拆分需改加载链路可能破坏"边渗透边记录"等已验证节奏。K0 主节点只做 vertical interface+config+verbs_gate 过滤，**不碰 prompt 装配**；prompt 拆分在 K0 验证 18 agent 全可见性回归通过后，单独 K2.1 串行做。

### P1（高价值改造，明显提升质量和可维护性）

> **Critic R1 修订**：原 P0 13 项降 P1，按价值排序。每项带立场保护注/性能约束/验证边界（防 Mock 冒充真实集成）。

| 项 | 做什么 | 为什么 | 改哪些模块 | 预期收益 | 风险 | 数据迁移 | 兼容性 | 对应需求 |
|----|--------|--------|-----------|---------|------|---------|--------|---------|
| **CL4R1T4S prompt 五项** | 身份层+日期+截止 / 防注入层 / 并行工具指导 / outcome-first / 工具前 WHY-EXPECT-LINK | prompt 质量跃升+黑匣子可观测 | default_single_system_prompt.go | prompt 质量大跃升 | 低（纯文本，K2.1 prompt 拆分后做） | 无 | **立场保护**：①"授权不质疑"措辞不动，authorized-only 仅指操作范围非二次确认；②outcome-first 仅指最终回复先结论，工具调用前 50-200 字思考块保留不动（不破坏 ReAct 四步循环） | ②⑤ |
| **trace waterfall + HITL Why + 托盘动态** | 黑匣子打开最小可见闭环 | 小白看到 agent 每步在做什么/为什么/等什么 | chat/render.js、hitl.js、tray.js | 黑匣子打开 | 低（L2，复用 monitor.js polling） | 无 | 不动 chat.js 主结构；**托盘跨平台**：Win ico 叠加/macOS png template/Linux AppIndicator 兜底文本 | ② |
| **instinct 中间层 + `/learn` `/evolve`** | PostToolUse hook 捕获→instincts jsonl→聚类晋升→SKILL.md | 用户自建 skills 沉淀闭环 | internal/skillpackage/instinct_capture.go(新)、Eino 中间件 | 用户可沉淀工作流 | 中 | 无 | guarded-write 脱敏；**性能约束**：采样率配置（默认每 N 次捕获 1 次）+ 仅 high-confidence 工具结果 + 异步队列不阻塞主循环；**验证边界**：P0/P1 只验证管道+guarded-write 脱敏，instinct 质量 evals Tier3 在用户给预算后做（防 Mock 冒充） | ③ |
| **非安全工具/skill yaml** | tools/dashi-ppt.yaml+image-gen.yaml+wechat-publish.yaml / skills/office/ecommerce/content/ | 通用化扩展首批 | tools/、skills/ | 通用能力 | 低 | 无 | 不破坏安全 agent；**许可证**：dashi AGPL-3.0 需评估或自研等价，baoyu MIT 可移植，z-skills 部分专有需核对 | ① |
| **watch_video + transcribe_audio** | Go 调 ffmpeg/whisper.cpp 二进制 | 多模态 P0（零服务端） | internal/vision/video.go+audio.go | 多模态感知 | **中**（外部二进制，Windows 单机分发体积可能从 ~80MB 涨到 300MB+，需可选打包或运行时检测） | 无 | 复用 vision 模式 | ④ |
| **verifier 4-axis + evidence ladder** | penetration/impact 输出过闸+Playbook PROVE/RULE_OUT | 消除"理论可行"包装"已验证" | agents/penetration.md+impact-exfiltration.md、internal/security/verifier.go(新) | 验证严谨 | 中 | 无 | 新增闸门（fail-closed） | 安全 |
| **SARIF 生产级** | 补 logicalLocations/versionControlProvenance/security-severity/fix_before/after | GitHub code-scanning 不丢锚点 | internal/sarif/sarif.go | 合规审计 | 低 | schema 加字段 | 向后兼容 | 安全 |
| **Operator Charter** | 18 agent 共享段（Persona+Safety+Injection+Anti-fabrication） | 统一安全契约 | agents/_shared.md(新)、agents/*.md | 防捏造 | 低 | 无 | **立场保护**：Persona 段保留现有"授权不质疑"措辞，authorized-only 仅指操作范围非授权二次确认；Anti-fabrication closed-tag TAXONOMY | ⑤安全 |
| **StuckDetector** | 四阈值（sameOutputRepeat=3/sameErrorRepeat=2/revisionLoop=4/monologue=6） | 防退化二级防线 | internal/multiagent/eino_stuck_detector.go | 防卡死 | 低 | 无 | 配合 TurnLimiter；**误报防护**：recon/enumeration 类白名单豁免 sameOutputRepeat + 输出哈希前归一化（剥离时间戳/进度行） | 编排 |
| **Scheduler 四策略 + retry/backoff** | round-robin/least-busy/capability-match/dependency-first + computeRetryDelay cap 30s | Coordinator 策略层 | coordkit/coordinator_specs.go | 并发效率 | 低 | 无 | 向后兼容 | 编排 |
| **reactions lifecycle 状态机** | 补 SessionStatus 转换轮询（idle→running→tool_pending→hitl_pending→done→failed）+ escalateAfter 时长解析 | 反应式自动化 | internal/reactions/engine.go | 自动响应 | 中 | 无 | 向后兼容 | 编排③ |
| **golangci v2 + gofmt gate + go-version-file** | CI 矩阵升级 | 工程化对标顶级 | .golangci.v2.yml、ci.yml | CI 质量 | 低（1h） | 无 | 无 | 工程化 |
| **PR 风险分级器** | scripts/pr-risk-check + FILE-SYSTEM-MAP.md + pr-risk.yml（pull_request_target+base checkout） | PR 自动 risk 评论 | scripts/、.github/workflows/ | PR 治理 | 低 | 无 | 无 | 工程化 |
| **K2.1 prompt 拆分（vertical 后置）** | default_single_system_prompt 拆为通用骨架+安全垂直段 + 18 agent _shared.md 抽取 | 降低 prompt 漂移 | default_single_system_prompt.go、agents/*.md+加载器 | 维护 | **中（L3，破坏已验证节奏风险）** | 无 | **前置**：K0 vertical 切换后 18 agent 全可见性回归测试通过 + penetration.md"边渗透边记录"等节奏不破坏验证 | ⑤ |
| 4 层记忆 + Ebbinghaus 衰减 | working/episodic/semantic/procedural + retention.go（score=salience*exp(-λΔt)+σ*Σ(1/Δt_access)，λ=0.01/σ=0.3，阈值 0.7/0.4/0.15） | 记忆有衰减不噪声 | internal/memory/types.go+retention.go(新) | 记忆质量 | 中 | 无 | 向后兼容（**新建分层非增量改造**，Critic R1 修正：memory 现仅有衰减雏形非 4 层） | ③ |
| 级联过期 | supersede 传播到图邻居（concepts 重叠≥2 的 sibling 也标 stale） | 旧事实不污染新检索 | internal/knowledge/cascade_stale.go(新) | 检索准确 | 中 | 无 | 向后兼容 | ③ |
| provenance 引用链 | FactInstance 加 SourceObservationIDs+SourceFindingIDs；observations 表存 raw tool_use 快照 | 审计可追溯 | internal/memory/types.go、internal/database/provenance.go | 可追溯 | 中 | schema 加列 | 向后兼容 | ③ |
| internal/perception + videomem | `internal/perception/pipeline.go`(新) Eino 感知中间件链 + `internal/videomem/index.go`(新) 视频索引管线落 SQLite+FTS5 | 多模态 agent 感知跃迁 | internal/perception/(新)、videomem/(新) | 多模态架构 | 中 | 无 | 新增包；VLM 作告警验证器降误报（VSS alert verification） | ④ |
| ctx_execute + SQLite FTS5 双索引 | think in code + porter+trigram 双索引 | token 96-98% 降 | internal/ctxindex、internal/mcp/builtin | token 效率 | 中 | 无 | 向后兼容 | ③性能 |
| skill_stats 前端演化趋势 | 后端 LoadSkillStats 已有（`handler/skills.go:500`），前端展示 success-rate sparklines/failures 聚类 | skill 健康度可见 | web/static/js/skills.js | skill 演化 | 低 | 无 | 无 | ③ |
| skill provenance + versioning | .provenance.json（curated/learned/imported）+ .evolution/amendments.jsonl | skill 当有生命周期资产 | internal/skillpackage | skill 治理 | 中 | 无 | 向后兼容 | ③ |
| darwin 8 维评估 | 结构维度走 validate+evals，效果维度子 agent 带 skill vs 不带 baseline 对比（标 dry_run 当无子 agent） | skill 质量量化 | internal/skillpackage/evals | skill 质量 | 中 | 无 | 向后兼容 | ③ |
| recursive decomposer | maxDepth=3 + requireApproval 走 HITL | 任务分解 | internal/multiagent/coordinator_decomposer.go(新) | 复杂任务 | 中 | 无 | 向后兼容 | 编排 |
| pair pipeline | Executor→Critic→Validator→Reporter（适配渗透场景） | 质量保证 | internal/swarm/pair_pipeline.go(新) | 质量保证 | 中 | 无 | 复用 worktree | 编排 |
| conflictDetector | Union-Find 按代码图谱分析多 worker 冲突 | 并行安全 | internal/swarm/conflict_detector.go(新) | 并行安全 | 中 | 无 | 新增 | 编排 |
| PolicyEngine + PlanVerifier | replan/stop/continue（max_consecutive_failures=3/min_verification_confidence=0.8）+ LLM 二次审 plan | 策略层 | internal/multiagent/coordinator_policy.go(新) | 策略严谨 | 中 | 无 | 向后兼容 | 编排 |
| threat_model amend + coverage ledger 双源 | run-scoped 威胁模型（agent 追加非覆写）+ 双源覆盖（agent-reported vs machine-observed） | 合规审计 | internal/attackchain、tools/coverage(新) | 审计 | 中 | 无 | 向后兼容 | 安全 |
| chain-builder + OAST | 递归链走（A→B→C，3 候选失败即止损）+ OAST 回调作 proof | 链发现引擎 | agents/lateral-movement.md+impact-exfiltration.md、internal/security/oast(新) | 链发现 | 中 | 无 | 新增 | 安全 |
| ralph autonomous loop | fresh context per iter + progress.md append-only + `<promise>COMPLETE</promise>` sentinel | 长任务循环 | internal/autonomous/(新) loop.go+progress.go+sentinel.go | 长任务 | 中 | 无 | 新增；**禁用** ralph `--dangerously-skip-permissions`（安全红线，§5.3） | 工程化 |
| LongHorizon human gate _TRIGGERS | completed/max_rounds/needs_human/repeated_failure + allow_extra_rounds | HITL 升级 | internal/hitl/gate.go(新) | 防伪完成 | 中 | 无 | 向后兼容 | 工程化 |
| loop-budget + kill switch | daily_token_cap + CYBERSTRIKE_LOOP_PAUSE_ALL 环境变量/data/loop-pause.flag | 预算执行 | internal/cost/budget.go(新) | 成本控制 | 中 | 无 | 向后兼容 | 工程化 |
| onboarding 分层 + 空状态引导 + splash 阶段化 | 首启引导（OpenMontage Preflight Discovery）+ Empty 文案（Learnova）+ 三阶段百分比 | 小白易用 | config.js+dashboard.js+splash.html | 易用 | 低 | 无 | 无 | ② |
| bounded auto-restart | RELOAD_LIMIT=3/60s（pi-agent-desktop）| 桌面壳稳定 | desktop/src/main.js | 稳定 | 低 | 无 | 无 | ② |
| 引用格式规范 | 发现输出加 Source: 工具名@trace 序号 | 证据可溯源 | default_single_system_prompt+penetration.md | 审计复现 | 低 | 无 | 无 | ⑤安全 |
| 失败协议 6 步 + 可逆性风险分级 | 结构化失败协议 + 四级风险（只读/非破坏性写/破坏性写/不可逆） | 减少无效重试+精细授权 | default_single_system_prompt+penetration.md | 收敛 | 低 | 无 | 无 | ⑤安全 |
| vertical scaffold skill | `/scaffold-vertical-agent` 访谈→生成 backend+skills+roles | 把"造 vertical agent"agent 化 | skills/_common/scaffold-vertical-agent(新) | 自助扩展 | 中 | 无 | 新增 | ① |
| EXTEND.md 分层配置 | extends/<vertical>/EXTEND.md project 级覆盖 | 不改主配置加垂直 | extends/ 目录 | 灵活 | 低 | 无 | 向后兼容 | ① |

### P1 验证补充（Critic R1 修订）

- **vertical 切换回滚测试**：切到非 security 再切回 security，验证 18 agent 全可见性恢复 + skills-lock.json 一致 + verbs_gate 0 违规
- **instinct 验证边界**：P1 只验证管道（PostToolUse 捕获→instincts jsonl→guarded-write 脱敏）+ 采样率配置生效，instinct 质量 evals Tier3 在用户给预算+glm 本地代理后做
- **托盘跨平台**：Win ico 叠加色点/macOS png template image/Linux AppIndicator 兜底文本三路径各测

### P2（锦上添花，非当前阶段必须）

| 项 | 做什么 | 为什么 | 对应需求 |
|----|--------|--------|---------|
| Progress Ledger（magentic-one 四类事实） | Coordinator 系统提示增 pre-survey | plan/replan 上下文 | 编排 |
| JSON checkpoint parent 链 | sessionCheckpointEntry 链式 | fork/resume 多分支 | 编排 |
| toolapproval don't-ask-again | Rule{Tool,Arguments} 常驻规则 | HITL 效率 | 编排 |
| Evaluator-Optimizer wrapper | QualityRating 套在 Deep/Supervisor 外 | 高风险输出精炼 | 编排 |
| onTrace 可观测 callback | 结构化 span+timing+token+runId | 可观测 | 编排② |
| 诊断包脱敏导出 | 打包对话+日志+config（key REDACTED） | 小白报障 | ② |
| 学习模式 | 工具"?"图标+原理卡片 | 边用边学 | ② |
| brand-systems | BRAND.md 品牌契约 | 报告套品牌 | ① |
| 4 垂直 demo | taobao-merchant/shopify-store/office-report/content-creator | 可运行示例 | ① |
| 代理无关化 | skill 在 Claude Code/Codex/Cursor 跑 | 扩大用户基数 | ① |
| swarm 反应式调度 | dedup-triage/orchestrator 改 blackboard+触发谓词 | 反应式编排 | 安全 |
| Graphiti 知识图谱 | 跨 engagement 沉淀 attack graph | 跨会话学习 | ③安全 |
| execution monitoring mentor | 5 同工具/10 总阈值 | 小模型收益 | 安全 |
| preflight 5 检 | repo 存在/config 解析/code_path/credentials/target 可达 | 省 token | 安全 |
| loop-audit 0-100 评分 | holistic readiness | PR readiness | 工程化 |
| handoffs YAML per-workflow | 多会话共享 latest checkpoint | 多会话协调 | 工程化 |
| reactions auto-action | create-incident/rollback/snapshot-forensic | 反应式自动化 | 工程化安全 |
| mutation 测试 | token-optimizer-mcp | 测试质量 | 工程化 |
| voice 输出 | VoxCPM 本地 TTS | 语音 agent | ④ |
| video-shotcraft recipe card | 运镜知识固化 | 视频生成 | ④ |

---

## 七、全栈实施方案（v2 整合）

### 7.1 前端改造建议

1. **trace waterfall（P0）**：`chat/render.js` 渲染每条 assistant 消息时，工具调用渲染为时间线 span（起始/耗时/状态色），subagent 为嵌套 span，HITL 等待为虚线宽帽 span。复用 `monitor.js` polling + `chat-plan-progress.js` 签名 diff。数据源：后端已有 turn/tool_call 事件流
2. **HITL Why 区（P0）**：`hitl.js` 的 `hitlRenderContextBlocks` 加折叠区消费 `buildReasoningRequestPayload`（`render.js:251` 已构建未消费），展示"为什么请求此工具+风险等级+影响半径"
3. **托盘动态状态（P0）**：`tray.js` 91 行改 tooltip 动态（轮询 `/api/tasks/active`+`/api/hitl/pending` 显示"N 任务运行·M 待审批"）+ 图标叠加色点（绿/黄/红）。参考 Hermes tray.rs 左键单击收敛
4. **onboarding 分层（P1）**：`config.js` 保存后主窗首次加载检测已配置厂商→"你现在能做"卡片（2-4 能力+3 起手 prompt）。参考 OpenMontage Preflight Discovery
5. **空状态引导（P1）**：`dashboard.js` 空面板渲染 Empty 引导（"尚未开始扫描？从授权一个目标开始"+CTA）。参考 Learnova
6. **splash 阶段化（P1）**：`splash.html` 三阶段百分比（连接后端 0-40%/等待就绪 40-80%/加载界面 80-100%）+ 加载小贴士轮播
7. **skill 渐进披露 UI（P2）**：`skills.js` 小白模式只显示"常用 skill"5 个+"更多"折叠；agent 推荐时显示"此任务可能需要 X skill"提示
8. **学习模式（P2）**：chat 工具调用渲染时每个工具名旁加"?"图标，点开显示原理卡片（从 SKILL.md 摘要生成）
9. **不重写 chat.js**：11235 行已拆分 done，P0/P1 全部在 `chat/render.js`+`hitl.js`+`tray.js`+`config.js`+`dashboard.js`+`splash.html` 增量扩展

### 7.2 后端改造建议

1. **vertical 抽象（P0 奠基）**：`internal/vertical/` interface（Vertical{Name,DefaultSystemPrompt,AgentMdDir,SkillDir,ToolWhitelist,OnboardingDoc}）+ security 首实现 `verticals/security/`；config 增 `active_vertical: security`+`verticals:[]`；`default_single_system_prompt.go` 拆为通用骨架+安全垂直段；`verbs_gate.go` realTools 基线按 vertical 过滤
2. **blackboard SQLite（P0）**：`internal/blackboard/sqlite_board.go`，Board interface 不变，`*sql.DB` 与 knowledge 共库。表 `blackboard_findings`+FTS5 `blackboard_findings_fts`
3. **instinct 中间层（P0）**：`internal/skillpackage/instinct_capture.go` PostToolUse hook→`data/instincts/<project_hash>/*.jsonl`；`/learn`+`/evolve`+`/skill-create` skill；guarded-write 脱敏目标地址/凭据
4. **internal/perception + videomem（P1）**：`internal/perception/` Eino 感知中间件链（帧抽取/音频转录/物体检测/VLM 描述/嵌入生成节点）+ `internal/videomem/` 视频索引管线（落 SQLite+FTS5）。VLM 作告警验证器降误报（VSS alert verification）
5. **internal/autonomous（P1）**：`loop.go`（fresh context per iter+加载 progress.md+执行单步+验证+checkpoint）+`progress.go`（append-only+Codebase Patterns 顶部）+`sentinel.go`（`<promise>COMPLETE</promise>` 检测）。复用 fileCheckPointStore+cost.Tracker
6. **StuckDetector（P0）**：`internal/multiagent/eino_stuck_detector.go` 四阈值，挂 eino_adk_run_loop 轮次边界，触发 PublishHighImpactTool("agent-stuck") 走 reactions
7. **Scheduler + retry/backoff（P0）**：`coordkit/coordinator_specs.go` 补四策略+computeRetryDelay cap 30s
8. **reactions lifecycle（P0）**：`internal/reactions/engine.go` 补 SessionStatus 状态转换轮询（idle→running→tool_pending→hitl_pending→done→failed）+ escalateAfter 时长解析
9. **verifier 4-axis（P0）**：`internal/security/verifier.go`（新）penetration/impact 输出过闸；agents/penetration.md+impact-exfiltration.md 加 Playbook PROVE/RULE_OUT
10. **SARIF 生产级（P0）**：`internal/sarif/sarif.go` 补 logicalLocations/versionControlProvenance/security-severity/fix_before/after
11. **Operator Charter（P0）**：`agents/_shared.md` 共享段（Persona authorized-only+Safety no destructive+Injection `<scan_data>` 数据非指令+Anti-fabrication closed-tag TAXONOMY）
12. **4 层记忆 + 衰减（P1）**：`internal/memory/types.go` 加 Strength/AccessCount/LastAccessedAt；`retention.go` score=salience*exp(-λΔt)+σ*Σ(1/Δt_access)，λ=0.01/σ=0.3，阈值 hot/warm/cold=0.7/0.4/0.15
13. **级联过期（P1）**：`internal/knowledge/cascade_stale.go` supersede 时遍历图邻居标 stale
14. **ctx_execute + FTS5（P1）**：`internal/ctxindex` 迁 FTS5 双索引（porter+trigram）+ `internal/mcp/builtin` 新 ctx_execute 工具
15. **scope_json operation_domain（P1）**：`internal/security/scope_block.go` 加 operation_domain 字段（向后兼容默认不限制）
16. **skillpackage lock/verbs_gate 递归（P0）**：`lock.go`/`verbs_gate.go` 改 WalkDir 支持子目录垂直包
17. **non-安全工具/skill（P0）**：tools/dashi-ppt.yaml+image-gen.yaml+wechat-publish.yaml；skills/office/ecommerce/content/
18. **watch_video + transcribe_audio（P0）**：`internal/vision/video.go`+`audio.go` Go 调 ffmpeg/whisper.cpp 二进制

### 7.3 数据层改造建议

1. **blackboard 持久化（P0）**：SQLite schema `blackboard_findings(id,type,title,detail,severity,source,project_id,created_at,superseded_by)`+FTS5
2. **provenance（P1）**：FactInstance 加 SourceObservationIDs+SourceFindingIDs；observations 表存 raw tool_use 快照
3. **4 层记忆表（P1）**：observations（working）+summaries（episodic）+facts 升级（semantic）+skill_stats 扩展（procedural）
4. **级联过期（P1）**：knowledge_graph_nodes/edges 加 stale 字段
5. **统一 home（已 done）**：MigrateLegacyData 扩展源目录集合（tmp/prompts/playbooks/agents）

### 7.4 接口层改造建议

1. **vertical `/api/vertical/*`（P0）**：CRUD vertical 配置（RBAC: admin）
2. **instinct `/api/instincts/*`（P0）**：查询/晋升/删除 instinct
3. **autonomous `/api/autonomous/start`（P1）**：启动长任务循环
4. **reactions lifecycle（P0）**：扩展现有 `/api/reactions` CRUD
5. **ctx_execute MCP 工具（P1）**：参数 `{query,lang}` 返 verdict
6. **统一 turn（已 done J16 评估）**：runEinoADKAgentLoop 是汇聚点，TurnSink 接口作扩展点保留
7. **watch_video/transcribe_audio MCP 工具（P0）**：vision 扩展

### 7.5 配置管理建议

1. **active_vertical 段（P0）**：`active_vertical: security`+`verticals:[]`
2. **blackboard.driver（P0）**：`memory|sqlite`（默认 memory 向后兼容）
3. **autonomous 段（P1）**：`max_rounds`/`round_budget`/`fresh_context_per_iter`
4. **budget 段（P1）**：`daily_token_cap`/`daily_run_cap`/`per_run_token_cap`
5. **multimodal 段（P0）**：`image_gen`/`video_gen`/`audio` 各自 model/api_key/base_url
6. **extends/ 分层（P1）**：`extends/<vertical>/EXTEND.md` project 级覆盖
7. **config.example.yaml 同步**（不动 config.yaml 运行态）

### 7.6 日志与监控建议

1. **provenance（P1）**：每条 finding/漏洞带 source+source_finding_ids+source_observation_ids
2. **token 仪表盘（P1）**：前端显示 per-run token/$/turn（cost.Tracker 已有，前端展示）
3. **级联过期 Warn（P1）**：supersede 时标记依赖 KB 节点 stale+Warn
4. **反应式 runbook（P0）**：reactions 触发记 audit
5. **decision_trace（P0）**：`internal/observability/decision_trace.go` 结构化决策追踪（agent 每步{step,thought,tool_candidates,chosen,why,expected}）
6. **perception_trace（P1）**：每节点输出{node,input_summary,output_summary,duration_ms,tokens_consumed,model}落 SQLite
7. **paid_api_ledger（P0 红线）**：记录每次付费调用意图+是否批准+预算+实际花费，默认预算 0 拦截

### 7.7 测试策略建议

1. **新纯函数 100% 单测（P0）**：table-driven，匹配 internal/security 现有风格（verifier/scope_block 递归/autonomous loop/retention）
2. **darwin 8 维评估（P1）**：结构维度走 validate+evals；效果维度子 agent 带 skill vs 不带 baseline 对比（标注 dry_run 当无子 agent）
3. **E2E（P0）**：curl 实测（vertical 切换/instinct 晋升/autonomous loop 退出/HITL Why 展示/托盘动态色）+ Playwright（trace waterfall 渲染/onboarding 引导）
4. **evals Tier3（P1）**：用户给预算+glm 本地代理+assert 工具调用
5. **回归底线**：不新增任何 FAIL（基线 FAIL 集合记录于 workflow_status.md）
6. **caveman 3-arm（P1）**：skill vs terse vs baseline，tiktoken 计数，snapshot 提交 git
7. **双路径互验**：CGO=1 mingw / `-tags sqlite_pure_go`（主项目已有基建）

### 7.8 部署与运维建议

1. **Dockerfile（已 done）**：补 CGO_ENABLED=1 + mingw 在镜像内
2. **NSIS（已 done）**：打包含 local_mode+无 TLS+原生菜单
3. **统一 home（已 done）**：`~/.cyberstrikeai`
4. **CI（P0）**：`.github/workflows/ci.yml` 配 CGO_ENABLED=1+mingw setup action+gofmt gate+go-version-file+golangci v2
5. **pr-risk.yml（P0）**：pull_request_target+base checkout+API file list+step summary
6. **audit.yml（P2）**：PR 评论 readiness 评分 0-100
7. **bounded auto-restart（P1）**：desktop main.js RELOAD_LIMIT=3/60s
8. **诊断包导出（P2）**：托盘菜单"导出诊断包"（脱敏正则全覆盖）

### 7.9 建议的实施批次和依赖顺序

```
批次 K1（P0 验证基线，不改代码）→ go vet/build 双路径 + curl 安全基线复验
   ↓
批次 K2（P0 vertical 抽象奠基）→ internal/vertical/ + config active_vertical + default_single_system_prompt 拆分
   ↓ （K2 稳定后并行）
   ├── 批次 K3（P0 prompt P0 五项）→ default_single_system_prompt + Operator Charter
   ├── 批次 K4（P0 黑匣子三件套）→ trace waterfall + HITL Why + 托盘动态
   ├── 批次 K5（P0 blackboard SQLite + instinct 中间层）→ 用户 skills 沉淀底座
   ├── 批次 K6（P0 非安全工具/skill + lock/verbs_gate 递归）→ 通用化首批
   ├── 批次 K7（P0 多模态 P0：watch_video/transcribe_audio）→ vision 扩展
   ├── 批次 K8（P0 安全深化：verifier/SARIF/Operator Charter）→ 安全验证侧
   ├── 批次 K9（P0 编排：StuckDetector/Scheduler/reactions lifecycle）→ 编排策略层
   └── 批次 K10（P0 工程化：golangci v2/gofmt gate/PR 风险分级器）→ CI 升级
   ↓ （K3-K10 各自 done 后）
批次 K11+（P1 各项）→ 4 层记忆/级联/provenance/perception/videomem/autonomous loop/...
   ↓
批次 K12（P2 长期增强）
```

**依赖关系**（Critic R1 修正）：
- **K0 三项硬奠基**（vertical 抽象 + blackboard SQLite + skillpackage 递归）是其余所有项的前置
- **K2.1（prompt 拆分）是 L3 风险子节点**：依赖 K0 vertical 切换后 18 agent 全可见性回归测试通过 + penetration.md"边渗透边记录"等已验证节奏不破坏验证，单独串行评审
- **K5（blackboard SQLite）与 K11（memory 4 层）包级独立**：memory 4 层改 `internal/memory/types.go`+新建 `retention.go`，blackboard SQLite 改 `internal/blackboard/sqlite_board.go`，两者仅共享 `*sql.DB` 句柄（非代码依赖），**可并行**。原表述"K5 是 K11 底座"修正为"共享 SQLite 句柄但包级独立"
- K4（黑匣子三件套）独立可并行，复用 monitor.js polling 基建
- K9（StuckDetector）配合 K11（autonomous loop）形成长任务闭环
- K7（多模态）依赖 K0 vertical 稳定后进，外部二进制（ffmpeg/whisper.cpp）分发体积需 K7 实施时量化

---

*§3-§7 已基于 10 子代理报告整合回填 + Critic R1 修订。所有结论基于实际目录和文件证据，不臆造。未验证项明确标注。*

## Critic R1 审查与修订记录

**审查轮次**：1 轮独立 Critic（6 方面：需求完整性/逻辑正确性/边界失败/代码质量规范/测试覆盖/实际运行用户路径）
**总评**：REQUEST CHANGES → 已修订 → 等待复验

**已修复项**（本轮全部落地）：

1. **[BLOCKER] P0 16 项收敛** → P0 收敛为 3 项硬奠基（vertical 抽象 + blackboard SQLite + skillpackage 递归），原 13 项降 P1 按价值排序（§6 P0/P1 已重写）
2. **[MAJOR] memory "4 层雏形"过度表述** → §2.2 改为"有衰减雏形（temporal.go 仅 `exp(-0.02*daysOut)` 单层）非 4 层分层架构"，K11 标注"新建分层非增量改造"
3. **[MAJOR] K5→K11 依赖链修正** → §7.9 改为"blackboard SQLite 与 memory 4 层包级独立，仅共享 SQLite 句柄，可并行"
4. **[MAJOR] instinct P0 闭环验证边界** → P1 instinct 项注明"P0/P1 只验证管道+guarded-write 脱敏，instinct 质量 evals Tier3 在用户给预算后做"，§6 P1 验证补充段重复
5. **[MAJOR] Operator Charter + CL4R1T4S prompt 立场保护注** → P1 两项加"立场保护"：①"授权不质疑"措辞不动，authorized-only 仅指操作范围非二次确认；②outcome-first 仅指最终回复先结论，工具调用前 50-200 字思考块保留不动（不破坏 ReAct）
6. **[MAJOR] instinct PostToolUse hook 性能量化** → P1 instinct 项加"采样率配置（默认每 N 次捕获 1 次）+ 仅 high-confidence 工具结果 + 异步队列不阻塞主循环"
7. **[MAJOR] K2 vertical prompt 拆分移 K2.1 串行子节点** → K0 主节点只做 vertical interface+config+verbs_gate 过滤不碰 prompt；K2.1 prompt 拆分 L3 风险单独串行评审，前置依赖 18 agent 全可见性回归测试 + "边渗透边记录"节奏不破坏验证

**MINOR/NOTE 项已落地**：
- StuckDetector 误报防护：recon/enumeration 白名单豁免 sameOutputRepeat + 输出哈希前归一化（§6 P1 StuckDetector 行）
- blackboard SQLite 并发写锁：journal_mode=WAL（§6 P0 blackboard 行）
- watch_video 外部二进制分发体积：风险从"低"调"中"，~80MB→300MB+ 需可选打包/运行时检测（§6 P1 watch_video 行）
- 托盘跨平台：Win ico 叠加/macOS png template/Linux AppIndicator 兜底（§6 P1 trace waterfall 行）
- vertical 切换回滚测试：§6 P1 验证补充段
- 多模态 P1 perception 锚点具体化：`internal/perception/pipeline.go`+`internal/videomem/index.go`（§6 P1）

**待验证项**（Critic 标 UNVERIFIED，实施阶段验证）：
- vertical 切换不破坏现有 18 agent 加载（K0 实施时增 18 agent 全可见性回归测试后验证）
- 多模态外部二进制分发体积（K7 实施时量化）
- instinct 真实质量（需用户给预算+glm 本地代理后 evals Tier3 验证）

---

## 八、实施授权状态

当前 **ANALYSIS_ONLY**，未授权修改源代码。

**建议优先批准的批次**（Critic R1 修订后，按 K0 硬奠基 → K2.1 L3 → 其余 P1 批次）：

- **批次 K0（P0 硬奠基，3 项并行）**：
  - K0a vertical 抽象：`internal/vertical/` interface + security 首实现 + config `active_vertical: security` + verbs_gate.go WalkDir+vertical 过滤（L2，含 18 agent 全可见性回归测试）
  - K0b blackboard SQLite 持久化：`internal/blackboard/sqlite_board.go`，Board interface 不变，journal_mode=WAL（L2 数据迁移，自动内存→db）
  - K0c skillpackage 递归：`lock.go`/`verbs_gate.go` ReadDir→WalkDir（L1）
- **批次 K1（P0 验证基线，不改代码）**：go vet/build 双路径（CGO=1 mingw / sqlite_pure_go）复验 + 起 exe curl 安全基线复验（限流/安全头/SSRF/HITL 拦截）
- **批次 K2.1（P1 prompt 拆分，L3 单独审批）**：依赖 K0a 18 agent 全可见性回归通过；default_single_system_prompt 拆通用骨架+安全垂直段 + 18 agent _shared.md 抽取；前置"边渗透边记录"节奏不破坏验证
- **批次 K3（P1 CL4R1T4S prompt 五项，立场保护）**：身份层+日期+截止 / 防注入层 / 并行工具指导 / outcome-first（仅最终回复先结论）/ 工具前 WHY-EXPECT-LINK；**立场保护**："授权不质疑"不动，authorized-only 仅指操作范围
- **批次 K4（P1 黑匣子三件套）**：trace waterfall（chat/render.js）+ HITL Why（hitl.js 消费 buildReasoningRequestPayload）+ 托盘动态色（tray.js 跨平台）
- **批次 K5（P1 instinct 中间层，性能约束）**：PostToolUse hook 采样捕获 + guarded-write 脱敏 + `/learn` `/evolve`；P1 只验证管道，质量 evals Tier3 待用户给预算
- **批次 K6（P1 通用化首批）**：非安全工具 yaml（dashi-ppt/image-gen/wechat-publish）+ skills/office/ecommerce/content/ 垂直包；许可证核对（dashi AGPL/baoyu MIT/z-skills 部分专有）
- **批次 K7（P1 多模态 P0）**：watch_video/transcribe_audio（Go 调 ffmpeg/whisper.cpp 二进制）；**中风险**：分发体积 ~80MB→300MB+ 需可选打包/运行时检测
- **批次 K8（P1 安全深化）**：verifier 4-axis + evidence ladder + Playbook PROVE/RULE_OUT + SARIF 生产级 + Operator Charter
- **批次 K9（P1 编排策略层）**：StuckDetector（白名单豁免）+ Scheduler 四策略 + retry/backoff + reactions lifecycle 状态机
- **批次 K10（P1 工程化）**：golangci v2 + gofmt gate + go-version-file + PR 风险分级器
- **批次 K11+（P1 其余 + P2 长期）**：4 层记忆/级联/provenance/perception/videomem/autonomous loop/...

**需单独审批（L3 高风险）**：
- K2.1 prompt 拆分（改 18 agent 加载链路，破坏已验证节奏风险）
- CL4R1T4S prompt 全量重写 18 agent（若用户选全量而非增量）
- blackboard SQLite schema 迁移（虽 L2 但持久化数据）
- 多模态本地推理依赖引入（分发体积+依赖管理）

---

## 九、需我补充的信息

> Critic R1 审查后，§9 待用户决策项收敛为以下 5 项（其余可在合理假设下推进）：

1. **通用化扩展边界**（K0a 前置）：是否本轮纳入"安全→通用平台"转型，还是只做安全垂直深化 + 新增少量非安全 skill 包？（影响 K0 vertical 抽象是否做以及做多重）
2. **CL4R1T4S prompt 优化边界**（K2.1 前置）：全量重写 18 agent（高风险）还是增量补结构（低风险，保"边渗透边记录"等已验证节奏）？（K2.1 L3 风险需单独审批）
3. **多模态依赖策略**（K7 前置）：是否允许引入本地推理依赖（ffmpeg binary/whisper.cpp/yolo weights）？还是只走云 API（付费红线，Mock 验证）？（影响分发体积 ~80MB→300MB+）
4. **用户自建 skills 沉淀深度**（K5 前置）：是否要做"从对话自动捕获→生成 SKILL.md"的自动化通道（需 instinct 中间层）？还是只做"用户手写 SKILL.md 放 user 层目录"的半自动（microagent 已支持，K5 instinct 可缓做）？
5. **黑匣子打开范围**（K4 前置）：前端可视化做到哪层（决策树/工具原因/skill 披露/记忆调用全做，还是先做决策树+工具原因）？

> **关键约束重申**：立场保护——"授权不质疑"措辞不动（CL4R1T4S/Operator Charter 项）；outcome-first 不破坏 ReAct（工具前思考块保留）；ralph `--dangerously-skip-permissions` 不迁移（安全红线）；付费 API 默认预算 0（多模态/instinct 真实 LLM 评测属付费红线，Mock 验证）。

---

*§3-§7 + Critic R1 修订 + §8 实施授权 + §9 待决策项 已全部落地。下一步：等用户批准 §9 待决策项 + 实施批次范围，进入 Phase B（Builder 实施 + Evaluator 终验）。当前 ANALYSIS_ONLY，未改源代码。*
